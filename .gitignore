# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.bak

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# pptx-server 실험/디버그 파일 및 실제 원 데이터 (저장소에 올리지 않음)
pptx-server/_debug_*.py
pptx-server/analyze_*.py
pptx-server/diag_*.py
pptx-server/test_*.pptx
pptx-server/원본_*.pptx
pptx-server/원본_*.pdf
# 위 패턴에 안 걸리는 나머지 실험 스크립트도 명시적으로 제외
pptx-server/_check_templates.py
pptx-server/find_15row.py
pptx-server/verify_fix.py

# 이름표 자동부여 단계2 검증용 — 디자이너 원본·산출물 (프로젝트 루트, 절대 커밋 금지)
/*.pptx
/_verify_output/

# 9월 대조 검증용 — 실배포본 ZIP 등 대용량 파일(348MB 등) 보관 폴더.
# 저장소에 올리지 않는다.
/_검증_9월대조/

# �ǹ� �������������̳� ��� ���� (���� �� ������, ���� Ŀ�� ����)
/_samples/

# 작업 중간 산출물 — 소스가 아니라 세션용 임시 파일이라 저장소에 안 올림
# ※ CLAUDE.md·HANDOFF.md는 아래 패턴 어디에도 걸리지 않음(파일명이
#   "프롬프트_"나 "CLAUDE_CODE_"로 시작하지 않음). 패턴을 넓히지 말 것.
*.patch
프롬프트_*.md
CLAUDE_CODE_*.md
