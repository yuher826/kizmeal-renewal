# HANDOFF

> 이 파일은 항상 **"지금 상태"만** 담는다. 매 세션 끝에 최신 상태로 덮어쓴다.
> 과거 이력은 `git log HANDOFF.md`로 본다.

**최종 갱신:** 2026-08-11 (로컬 PC 세션 — 박스 이동 로직 제거 완료·실물 확인 통과,
표 높이·날짜 표기 신규 발견 2건 미해결)

---

## ⚠️ 최우선 확인 필요 (프로덕션 리스크)

- **송파크레오 계정 이메일이 임시 변경된 상태** — 현재 `yuher826+songpacreo@gmail.com`
  (실제 이메일 `dybmg@naver.com` 아님). **크레오에 정식 오픈 안내 전 반드시 원복.**
  - 위치: `/erp/branches/5cecbb7a-aaad-4f8b-b901-8ae4175c9475`
    → 담당자 계정 → 이메일 변경

---

## 정리 대상 (테스트용으로 만든 것들)

- **"테스트 매니저"** (`yuher826+erptest@gmail.com`) 관리자 계정 — 삭제 검토
- **송파크레오 이메일 원복** (위 최우선 항목과 동일 건)

---

## 오늘 완료된 것 (커밋 해시 포함)

- **크레오 3원 온보딩 + 임시원 배포 제외** — DB 온보딩(SQL) + `contract_type='temporary'` 적용
- **학부모 포털 게이트 완화** (`98377b8`) — pptx/pdf/jpg 중 하나라도 있으면 노출
  - ⚠️ **build만 통과, 실제 학부모(parent) 계정으로 크레오 원 PDF 노출은 브라우저 미검증.**
  - 확인 방법: `/parent` 학부모 계정 로그인 → 송파크레오 소속 아이 등록 → 식단표 메뉴 확인
- **크레오 식단표 직접 업로드 기능** (`1119f57`) — 원 포털(`board/customer`) 기준 **실전 검증 완료**
- **매니저 계정 생성 권한 확장** (`9cc9935`, `60f0926`) — UI 드롭다운 필터링까지 확인
  - ⚠️ 단, "관리자 계정 생성" 버튼을 끝까지 눌러 **실제 계정이 만들어지는지는 미검증**
- **/board/login 문구 수정** (`6a7cdd2`) — "학부모" 언급 제거
  - ⚠️ dev 재기동 후 실제 화면 반영 **[결과 확인 필요 — 세션 마지막 재기동 요청했으나 결과 미확인]**

### 2026-08-10

- **ERP 사이드바 메뉴 중복 활성화 버그 수정** (`7de1eb8`) — 하위 경로가 여러 메뉴 href의
  prefix에 동시에 걸릴 때 두 메뉴가 동시에 active 처리되던 문제. 가장 구체적인(긴) href
  하나만 active로 판정하도록 수정. **프로덕션 배포 확인 완료.**
- **next.config.mjs 로컬 dev 웹팩 캐시 경합(-4094 UNKNOWN) 이슈 해결** (`2d2e836`) —
  Windows에서 `next dev` 시 여러 워커가 웹팩 디스크 캐시(`.next/cache/webpack`)에 동시에
  쓰기 시도하며 나던 `-4094 UNKNOWN`, `pack.gz rename ENOENT` 에러. dev 전용으로
  `config.cache=false`(메모리 캐시만 사용) 적용, 프로덕션 빌드는 영향 없음(clean build로 확인).
  - 부수 조치: **알약 탐지 제외에 프로젝트 폴더 등록함**
- **`gen_form.py` 6월 서식 잔재 제거** (`cb82311`)
  - 기준폼이 6월 실데이터 파생이라 (a) 1주차 D열 선거일 회색 (b) 1·2·3주차 F열 🎂 +
    18행 생일 주황 (c) 5주차 D·E·F 해당없음 회색이 **다른 달에 눌어붙던** 문제
  - 각 주차 **B열(운영일)** · **5주차 D열(해당없음)** · **1주차 D열(공휴일)** 을 서식
    템플릿으로 캡처해 **열 통째 덮어쓰는** 방식. **색상 하드코딩 없음**
  - 검증: 7월(5주차 꽉참)·8월(경계) **6월 잔재 4종 0건**, 헤더 공백 2칸 코드포인트 대조
    일치, **실물 엑셀 확인 완료**
  - **공휴일 자동 판정은 의도적으로 넣지 않음** — 공휴일에 운영하는 원과 쉬는 원이 갈려
    코드가 일괄로 정할 수 없음. `holidays` 라이브러리 도입 검토했다 **철회**.
    대신 `--holiday 17:제헌절` 옵션 신설
- **연도 드롭다운 하드코딩 제거** (`2599bc9`)
  - `[2024~2030]` 등 하드코딩이 **4곳**에 흩어져 있었음. 범위 끝을 지나면 선택지가
    사라져 코드를 고쳐야 하는 구조
  - `lib/diet-utils.ts`에 **`getYearOptions()` 신설** → 현재 연도 기준 **작년~3년 후(5개)**
    동적 계산. 4곳 전부 이 헬퍼 호출로 통일
  - **예상 밖 발견**: upload 페이지 2곳은 `[2025,2026,2027]` 3개뿐이라 **2028년에**
    diet 화면(2030년)보다 **먼저 깨질 상태**였음. 함께 수정
  - URL `?year=2020` 같은 범위 밖 값 방어 추가
- **비상배포 역할 게이트 불일치 수정** (`7bf0a90`)
  - `app/erp/(protected)/review/page.tsx` **1213·1414행**
    `role === 'super_admin'` → `EMERGENCY_DEPLOY_ROLES.includes(role)`
  - 원인: 커밋 `1f76c91`(06-19)이 본문에 "비상배포 버튼 노출 조건 변경"을 명시했으나
    **board만 고치고 erp를 누락**. erp 게이트는 `17bb0d9`(06-12) 이후 blame 무변동
    → **의도적 축소가 아닌 실수로 확정**
  - 수정 전 상태: API(`deploy/route.ts:104`)는 manager 허용인데 erp UI에 버튼이 없어,
    **manager가 권한은 있고 실행 수단이 없었음**
  - **정책 확정**: super_admin 부재 시 아무도 비상배포를 못 하는 상황을 막기 위해
    **manager도 비상배포 가능**
  - 실물 확인: 매니저 계정으로 `/erp/review` 승인완료 항목에 🚨 비상배포 노출 확인,
    📧 배포는 미노출(일반배포 권한 없음, 의도대로)
  - 일반배포/일괄배포 게이트(`821·1203·1404`행 `DEPLOY_ROLES`)는 **무변동**

---

## ⚠️ 신규 미해결 — 오늘 발생, 운영 확정 필요

- **`generate-form.yml`이 `--holiday` 없이 `gen_form.py`를 호출함**
  (`.github/workflows/generate-form.yml:33`)
  → ERP에서 폼 생성 시 **공휴일 지정 경로가 없음**. 아래 둘 중 하나로 확정 필요:
  - ① ERP UI에서 공휴일을 입력받아 워크플로로 전달
  - ② 영양사가 생성된 파일 헤더를 직접 수정
- **엑셀 `✏️ 입력 가이드` 시트 r65~69 공휴일 처리 안내 보완 필요**
  - 헤더에 공휴일을 **어떤 형식으로 쓰는지가 빠져 있음**
    (형식: `수  7/17` 줄바꿈 `공휴일` 줄바꿈 `(제헌절)`)
  - r66 C열 **"시스템이 자동 잠금 처리"는 현재 코드와 불일치** — `gen_form.py`는
    `--holiday` 지정 시에만 잠금
- **배서영 영양사·권팀장에게 "공휴일은 영양사가 헤더에 직접 표시" 방식 공유**

### 식단표 준비 워크플로우 스테퍼 — 3건 (2026-08-10 발견)

- **① "양식 준비" 완료 판정이 디자이너 템플릿을 확인하지 않음**
  - 현재 판정: `app/api/diet-automation/check-form/route.ts` — Storage(`diet-files`)의
    `{year}/{mm}/blank_form_{yy}_{mm}_v6.xlsx` **존재 여부만 HEAD로 확인**
  - 그런데 카드 설명(`app/erp/(protected)/diet/page.tsx:43` `WF_STEPS[0].desc`)은
    **"디자이너 양식으로 빈 폼 생성"** 이라 **설명과 실제 판정이 어긋남**
  - 실제로 `gen_form.py`는 기준폼(`키즈밀_식단표_기준폼_v6.xlsx`)으로 **엑셀 빈 폼만**
    생성하며 **디자이너 PPTX 템플릿과 무관**. 디자이너 템플릿은 **④ PPTX 생성** 단계에서 사용됨
  - 확정 필요:
    - ⓐ 카드 설명 문구를 실제에 맞게 고칠지
    - ⓑ 판정에 그 달 디자이너 템플릿 존재 확인을 추가할지
      (ⓑ라면 `diet_templates` 테이블 조회 로직 신설 필요)
- **② "양식 받기" 다시 버튼 문구가 ①과 구분되지 않음**
  - 두 카드 모두 완료 시 `다시`로 표시되나 동작이 다름:
    **①의 다시 = 빈 폼 재생성 / ②의 다시 = 기존 파일 재다운로드**
  - 제안: **"다시 준비" / "다시 받기"** 로 구분
  - ⚠️ 구현 주의 — 라벨이 `diet/page.tsx:871`
    `{busy ? '진행 중...' : done ? '다시' : step.cta}` **한 줄에서 4단계 전부 공유**됨.
    따라서 ①②만 바꾸려면 `WF_STEPS`에 재실행용 라벨 필드를 추가해야 함
    (`aria-label`은 `:867`에 별도로 `'다시 실행'` 존재 — 함께 손봐야 함)
- **(참고, 버그 아님) ② "양식 받기"가 완료로 뜨는 것은 정상**
  - `form_downloads` 테이블에 해당 연월 다운로드 이력이 있으면 완료 처리
    (기록: `app/api/diet-automation/download-form/route.ts:52`,
     조회: `diet/page.tsx:263~267`)
  - 순서 강제도 정상 작동 중 — `diet/page.tsx:654` `wfProgress`
    (`!formExists ? 0 : !formDownloaded ? 1 : …`)

---

## board/erp 중복본 정리 — ✅ 완료 (2026-08-11, push까지 완료)

로컬 PC(.env.local 보유)에서 솔아 세션이 전달한 패치(`kizmeal-board-erp-cleanup.patch`)를
`git am`으로 적용 → 최종 검증 → push까지 마무리함. 패치 파일은 적용 후 삭제.

- 커밋 `d57a59b`(구 `2cb1d7d`): 선행작업①) erp→board 역참조 11곳 → erp 경로로 교체
  (`diet/page.tsx` 2곳, `history/page.tsx` 3곳, `review/page.tsx` 2곳, `upload/page.tsx` 4곳)
- 커밋 `cb5d21c`(구 `1c6a05e`): 선행작업②③④ 확인 완료 후 실제 삭제
  - ② `next.config.mjs` redirect 4줄 — 변경 없이 그대로 존치 확인
  - ③ `branch-profile/[branchId]/page.tsx:356,366` 링크도 `/erp/diet`로 교체
    (redirect 홉 제거. 이 페이지 자체는 삭제 대상 아니고 계속 살아있음)
  - ④ 삭제 직전 `curl -I`로 4개 redirect 경로 실측 — 전부 307, location 헤더 정확
  - **삭제**: `app/board/admin/diet-automation/{page.tsx,history/page.tsx,review/page.tsx,upload/page.tsx}`
- 커밋 `0ed8728`(구 `ffff5c8`): HANDOFF 갱신 (솔아 세션분)
- **로컬 PC 최종 검증 (2026-08-11)**:
  - `npm run build` — **정상 통과**. `/erp/diet`, `/erp/upload`, `/erp/history`, `/erp/review`
    등 전 라우트 빌드 성공, 삭제된 `/board/admin/diet-automation/*`는 산출물에서 제거 확인
  - 브라우저 확장(Claude in Chrome) 미설치 상태라 `next dev` 기동 후 curl로 대체 검증:
    - `/erp/diet`·`/erp/upload`·`/erp/history`·`/erp/review` → 404 없음, 미인증 시
      `/erp/login?next=...`로 307 (보호 라우트 정상 동작)
    - 구 URL 4종(`/board/admin/diet-automation`, `.../review`, `.../upload`, `.../history`)
      → 각각 대응하는 `/erp/*`로 307 리다이렉트 정상 확인
  - **⚠️ 로그인 후 화면 렌더 자체(실제 데이터 표시 등)는 브라우저 미검증** — 라우팅/빌드
    레벨 확인만 완료. 다음에 실사용 중 이상 있으면 보고
  - `git push origin master` 완료 (`a99c1b7..0ed8728`)

---

## (이력, 참고용) 정리 전 조사 메모

### 근거

- redirect 4개 ↔ board 파일 4개 **1:1 완전 대응, 누락 경로 없음**
  (board 트리에 하위 `[id]` 등 동적 경로 없음)
- **board 전용 API 호출 0건** — board만 부르는 백엔드 기능 없음
- board 전용 식별자 3개 전부 무해:
  - `pptxWeekNum`(주차 드롭다운) = **死 UI**. 선택해도 요청 본문에 안 실림
  - `fmt`(파일형식 뱃지) = erp에 `PDF_JPG_CODES` 방식으로 **재구현됨**
  - `progressSteps` = erp에 **4단계 워크플로우로 상위 구현 존재**
- 화면별 비교: **history 완전 동일**, upload diff 4줄, review는 **erp가 387줄 더 길고
  구조적 차이 0건**
- import하는 컴포넌트/상수는 다른 곳에서도 쓰므로 **고아 안 됨**

### 선행 작업 (삭제 전 필수)

1. **erp → board 역참조 11곳을 erp 경로로 교체 (최우선)**
   - `diet/page.tsx:666`(router.push), `883`(href)
   - `history/page.tsx:72`, `99`, `124`(hubHref 템플릿)
   - `review/page.tsx:2109`, `2145`(Link)
   - `upload/page.tsx:113`, `140`, `153`, `330`
   - ※ 파일 삭제만으로는 안 깨짐(redirect가 파일시스템 라우팅보다 먼저 평가됨).
     다만 불필요한 홉이 남고, **나중에 redirect를 걷어내면 전부 깨짐**
2. **`next.config.mjs` redirect 4줄은 유지** (외부 북마크·기존 이메일 링크 대비)
3. `app/board/admin/diet/branch-profile/[branchId]/page.tsx:356,366` 링크도 교체 검토
   (이 페이지는 **삭제 대상 아님**, 계속 살아있음)
4. 삭제 직전 `curl -I http://localhost:3000/board/admin/diet-automation` 로
   리다이렉트 실제 발화 실측

### 교훈

이 중복 구조 때문에 **실제 사고가 발생했음** — 위 비상배포 권한 버그(`7bf0a90`)가
**"살아있는 erp를 두고 죽은 board를 고쳐서"** 생긴 것. 오늘 사이드바·연도 수정 때도
매번 board가 따라붙었음.

---

## 식단표 자동화 — 디자이너 양식 전수 조사 (2026-08-11)

**조사만 한 것. 코드 변경 0건.** 근거와 재현 방법을 아래 남김.

### 조사 자료

- 디자이너(윤수경) 제공 **2026년 1~8월 양식 9개** (8월은 방학O/X 2종)
- 저장소 `pptx-server/templates/` 원별 47개 + 공용 1개 (6월분)
- 코드: `app_actions.py`, `validate_template.py`, `pptx_generator.py`, `gen_form.py`,
  `app/api/diet-automation/upload/route.ts`, `app/api/board/diet/templates/route.ts`

---

### ★발견① 폴백이 문지기를 무력화 — 디자이너 양식이 실제로 안 쓰이고 있음

**증상**: 디자이너 양식을 템플릿 관리에 올려도, PPTX 생성은 **로컬 6월 양식**으로 나감.
화면에 아무 표시 없이 조용히 대체됨.

**원인 (파일:줄번호)**
- `pptx-server/app_actions.py:333` `_resolve_template_path()`
  → 업로드본 검증 실패 시 `return TEMPLATE_PATH, '로컬(업로드검증실패: ...)'` (369행)
  → **로컬 6월 양식으로 조용히 교체**
- `pptx-server/app_actions.py:388~402` 문지기 블록
  → 검증 대상이 이미 로컬로 바뀐 뒤라 **항상 통과**, `sys.exit(1)`이 안 걸림
- `pptx-server/app_actions.py:41` `TEMPLATE_PATH = '.../2026년 6월 식단표(양식).pptx'`

**즉 문지기 앞에서 불량품이 정품으로 바꿔치기되어 문지기가 볼 게 없음.**
2026-06-23 설계 문서의 "이름표 누락 시 → 거부 + 재업로드 안내"가
실제로는 "누락 시 → 6월 양식으로 조용히 대체"로 구현됨.

**검증 결과 (실측)**: 디자이너 양식 9개 **전부 검증 실패**
```
1월~8월 9개 파일 × 전 슬라이드
누락 = ['MENU_TABLE','ALLERGY_BOX','ORIGIN_BOX','MATERIAL_BOX']  (4개 전부)
컬럼 = None  (MENU_TABLE을 못 찾아 표 구조 검사 자체 불가)
```
저장소 원별 템플릿 48개도 동일 — **이름표 부여된 것은 공용 양식 1개뿐(1/48)**.

**왜 아직 사고가 안 났나**: 현재 49개 배포본은 **수동 제작** 중이라 이 경로를 안 탐.
**자동 생성으로 전환하는 순간 터지는 지뢰.**

**재현 방법**
```bash
cd pptx-server
python3 -c "
from validate_template import validate_template
r=validate_template(open('<디자이너양식.pptx>','rb').read())
print(r['summary']); print(r['slides'][0]['missing'])
"
```

---

### ★발견② 디자이너가 박스를 이미 "그 달 빈칸"에 배치해서 줌

원산지 박스 top 좌표 실측:

| 양식 | ORIGIN top | 위치 |
|---|---|---|
| 6월 | 8,571,159 | 표 **하단** (5주차 빈칸) |
| **7월** | **1,304,882** | 표 **상단** (1주차 월·화 빈칸) |
| 8월 | 8,427,444 | 표 **하단** (5주차 빈칸) |

달력 규칙과 정확히 일치:
- 6월(6/1=월) → 5주차 수·목·금 빈칸 → 박스 하단
- 7월(7/1=수) → 1주차 월·화 빈칸, 5주차 꽉 참 → 박스 **상단**
- 8월(8/1=토) → 5주차 화~금 빈칸 → 박스 하단

**결론: 박스 위치를 코드가 계산할 필요 없음. 양식 그대로 쓰면 이미 제자리.**

**따라서 `pptx_generator.py:1187 _fix_allergy_only()`의 박스 이동 로직은 불필요.**
이 로직이 존재하는 이유는 발견①(6월 양식 강제 사용) 때문에 생긴 군더더기.
해당 함수의 하드코딩(전부 6월 실측값 기준):
```
ORIGIN_Y = 8_395_899          # 6월 절대좌표
BOX_DOWN_15 = 175_260         # 15행이면 그냥 이만큼 내림
ALLERGY_BOTTOM_MARGIN = 532946
SLIDE_HEIGHT = 10680700
'그림 3','그림 224','그림 193'  # 클립보드 아이콘 이름 나열
```
→ 발견① 해결 후 **제거 대상**. 단, 제거 전 실물 대조 필수.

---

### ★발견③ 방학 그림도 양식에 포함 — 8월은 방학O/X 2종

| 양식 | 방학 그림 | 위치 | 그림 수 |
|---|---|---|---|
| 7월 | `그림 25` + TextBox "여름방학" | top 8,318,736 (하단=5주차) | 5개 |
| 8월(방학O) | `그림 26` + TextBox "여름방학" | top 1,161,913 (상단=1주차) | 5개 |
| 8월(방학X) | **없음** | — | 4개 |

**운영 흐름 (대표 확인 완료)**
```
영양기획팀 → 디자이너에게 그 달 원별 방학 일정 전달
디자이너   → 방학O / 방학X 두 버전 제작
영양기획팀 → 원별로 O(방학 있음) / X(방학 없음) 골라 내용 채움
```

**결론: 방학 그림을 코드가 삽입·크기조정할 필요 없음. 원별로 양식만 선택하면 됨.**

**방학 종류 (대표 확인)**
- 여름(7·8월), 겨울 → O/X 양식 제공됨
- **봄 → CK엔 거의 없음. 양식 따로 안 받음. 고려 대상 아님**
- 겨울 O/X 형태는 12·1월 양식 나올 때 확인 필요 (미확인)

**주의**: 텍스트 탐색 시 `여름방학`이 아니라 **`방학`**으로 찾을 것.
겨울은 `겨울방학`으로 오므로 `여름방학` 하드코딩하면 깨짐.

---

### ★발견④ 도형 이름은 매번 바뀌지만 텍스트는 8개월 내내 불변

같은 8월 양식 안에서도 슬라이드마다 이름이 다름:
```
slide1: '그림 3'   / 'TextBox 8'   / 'TextBox 10'
slide2: '그림 169' / 'TextBox 170' / 'TextBox 171'
slide3: '그림 228' / 'TextBox 170' / 'TextBox 171'
```

반면 **텍스트는 1~8월 내내 동일**:
| 대상 | 불변 텍스트 |
|---|---|
| ORIGIN_BOX | `원산지 표기` |
| MATERIAL_BOX | `* 원재료 표시안내 *` |
| ALLERGY_BOX | `<알레르기 표시 안내>` (그룹 내부 FREEFORM) |
| 방학 | `여름방학` / `겨울방학` → **`방학`으로 탐색** |

**결론: 이름표 자동 부착은 도형 이름이 아니라 텍스트 기준으로 탐색해야 함.**

**주의**: `ALLERGY_BOX`는 **그룹(grpSp) 안에 로고와 함께** 들어 있음.
`python-pptx`로 `slide.shapes`만 순회하면 못 찾음 → 그룹 내부까지 재귀 필요.
(코드는 이미 대응됨: `pptx_generator.py:907` "ALLERGY_BOX는 그룹이라 grpSp까지 봐야 함")

> **이전 세션 오류 정정**: "8월 배포본 57개 전부 알레르기 박스 없음"은
> 분석 스크립트가 그룹 내부를 안 본 탓. **실제로는 48개 템플릿 전부 존재.**
> 이 항목은 미해결 목록에서 삭제할 것.

---

### 발견⑤ 양식 구조는 4월부터 정착

| 달 | 슬라이드 | 표 행수 | 원재료·알레르기 |
|---|---|---|---|
| 1·2월 | 5장 | 9~15행 | ❌ 없음 |
| 3월 | 2장 | 16행/10행 | ❌ 없음 |
| **4~8월** | **3장** | **15행/10행/5행** | ✅ 있음 |

슬라이드 3장의 정체 (표 1열 라벨로 확인):
- **15행** = 1주~5주 × (주/오전/오후) → 오전+오후 간식 원
- **10행** = 1주~5주 × (주/오전) → 오전 간식만
- **5행** = 1주~5주 → 간식 없이 중식만

**결론: 1~3월 호환 불필요. 4월 이후 구조를 기준으로 설계.**
슬라이드 크기는 9개 전부 동일 `7556500 x 10680700`.

---

## 이 조사로 인한 작업 재정의

### 불필요해진 것 (기존 계획에서 삭제)

| 기존 계획 | 삭제 사유 |
|---|---|
| 엑셀 v6에 "박스 위치 표시 칸" 신설 | 발견② — 디자이너가 이미 배치 |
| 박스 위치 자동 계산 로직 신설 | 발견② — 동일 |
| 방학 그림 삽입·크기조정 기능 | 발견③ — 양식에 이미 포함 |
| 엑셀에 원별 방학 기간 입력칸 | 발견③ — 원별 양식 O/X 선택으로 대체 |
| 알레르기 박스 실종 원인 규명 | 발견④ — 분석 오류였음, 문제 없음 |

### 신설·수정해야 할 것 (우선순위순)

**1. 템플릿 업로드 시 이름표 자동 부착** ← 최선행
- 위치: `app/api/board/diet/templates/route.ts` (POST)
- 현재: theme.xml에서 색·폰트만 추출, PPTX 내용은 손대지 않음
- 변경: 텍스트 기준(발견④ 표)으로 4개 도형 탐색 → `cNvPr name` 부여 → 저장
- 참고: `pptx-server/_apply_template_names.py`에 탐색 로직 이미 존재, 이식하면 됨
- 이유: 디자이너에게 이름표를 요구하는 방식은 **실제로 지켜지지 않음(0/9)**.
  사람 약속이 아니라 코드가 보장하는 구조로 전환.
- 확장: `VACATION_IMG`도 함께 부여하면 발견③ 토대 확보

**2. 폴백 시 화면 경고**
- 위치: `pptx-server/app_actions.py:369` + ERP 템플릿 관리 화면
- 현재: 로그에만 한 줄, 화면 표시 없음
- 변경: 폴백 발생 시 DB 기록 → 템플릿 관리 화면에 경고 배지
- **더 나은 방향**: 업로드 시점(1번)에 막으면 폴백 자체가 안 일어남.
  1번 완료 후에도 안전판으로 유지.

**3. `_fix_allergy_only` 박스 이동 로직 제거**
- 위치: `pptx_generator.py:1187`
- 조건: 1번 완료 후 (디자이너 양식이 실제로 쓰이게 된 뒤)
- 검증: 제거 전후 PowerPoint 실물 대조 필수

**4. 원별 방학 양식(O/X) 선택**
- 방학 있는 달에만 양식 2개가 올라옴
- 업로드 시 사람이 용도 지정 (파일명 의존 금지 — `여름방학O`/`겨울방학O` 등 변동)
- 원별로 O/X 지정 → `branch_profiles` 또는 월별 설정
- 방학 없는 달엔 UI 자체가 안 나타나게

**5. 위탁업장 `contract_type` 신설** (독립 작업, 작음)
- 현재: `permanent` / `temporary` 2종
- 추가: `consignment` (위탁업장 5곳 — 현장 직접 조리, CK 이동급식과 별개 사업)
- 처리는 크레오와 동일: 자동 생성 제외 + `BranchDietUploadCard`로 직접 업로드
- **★중요**: `branch_filters.py` / `pptx-eligibility.ts`가 현재
  **"temporary만 제외"** 방식 → `consignment` 빠뜨리면 위탁 5곳이 자동 생성에 섞임.
  **"permanent만 포함"으로 뒤집을 것.** 새 유형 추가 시 자동 제외되는 방향이 안전.
- 시점: 위탁 5곳 **등록 전에** 하면 SQL 한 줄, 등록 후엔 데이터 정리 작업

---

## 미확인 / 다음에 확인할 것

1. **7·8월에 디자이너 양식을 템플릿 관리에 실제로 업로드했는지**
   → 했다면 발견①이 이미 발생 중. 안 했다면 아직 지뢰 상태.
   확인: ERP 템플릿 관리 화면의 등록 이력
2. **겨울방학 양식이 O/X 2종으로 오는지** (12·1월 양식 나올 때)
3. **5행 슬라이드(중식만)를 실제로 쓰는 원이 있는지**
   → 8월 배포본 57개 실측에선 10행·15행만 나왔음
4. 위탁업장 5곳 원 정보 (등록 필요)

## 이번 조사에서 건드리지 않기로 한 것

- **원별 참조표를 DB에서 생성**: 현재 `gen_form.py`는 기준폼 스냅샷을 그대로 복사해
  DB와 어긋남(크레오 3원이 참조표에 없음). 다만 크레오는 `temporary`라 파이프라인
  제외 상태라 **당장 문제 없음**. 나중 과제로 기록만 함.
- **업로드 API의 원별 참조표 파싱**: 현재 시트 존재만 확인(`route.ts:23`),
  파싱은 주차 시트 5개 + 도시락 시트만(`route.ts:527~534`).
  발견③으로 방학 입력칸이 불필요해져 **지금은 파싱 신설 불필요**.

---

## 이름표 자동부여 (발견① 해결) — 단계1~3 진행 중 (2026-08-11)

`프롬프트_이름표_자동부여.md` 지시대로 단계별 진행. 각 단계 사용자 승인 후 다음 단계.

- **단계1 완료** — `pptx-server/template_namer.py` 신설(커밋 `4e841c4`). 텍스트 기준(도형
  이름 아님)으로 4개 도형 탐색. python-pptx(lxml) 기반 — ElementTree 재작성 방식
  (`_apply_template_names.py`)은 네임스페이스 훼손 확인되어 이식 안 함.
  저장소 templates/*.pptx 48개 검증: 47/48 통과(유일 실패는 목동POLY의 메뉴와 무관한
  부록 슬라이드 — 정상 동작), 콘텐츠 바이트 손실 없음(미디어 SHA-256 일치 확인).
- **단계2 완료** — 실제 디자이너 8월(방학O) 원본으로 이름표 부여 + 강동ECC 생성까지
  실물 PowerPoint 확인 완료(레이아웃 정상). ⚠️ **디자이너 원본 파일 자체와 산출물은
  커밋 금지** — `.gitignore`에 `/*.pptx`, `/_verify_output/` 추가해둠(커밋 `2c4b641`).
  - **발견(신규, 미해결)**: 생성 중 `remove_holiday_cover_pics()`(공휴일 셀을 가리는
    장식 그림 자동 제거 로직, `pptx_generator.py:788`)가 방학 그림("그림 26", 발견③의
    `VACATION_IMG` 후보)까지 함께 지울 수 있음이 확인됨. 테스트에 쓴 6월 픽스처
    데이터의 공휴일 셀 좌표가 우연히 방학 그림과 겹쳐서 걸린 것 — 실제 8월 운영에서도
    진짜 공휴일 셀이 방학 그림과 겹치면 똑같이 지워질 수 있음. **아래 "④ 원별 방학
    양식(O/X) 선택" 작업 때 `VACATION_IMG` 이름표를 부여하고, `remove_holiday_cover_pics`
    제거 대상에서 `sh.name == 'VACATION_IMG'`인 경우 제외하도록 반영 필요.**
- **단계3 완료** — `app_actions.py`(GitHub Actions)와 `app.py`(Render) 둘 다 연결.
  `_resolve_template_path()`에 이름표 자동 부여를 다운로드 직후·검증 직전에 연결.
  사전 검토 중 발견해 함께 반영한 5가지:
  1. **연/월 매칭 추가** — `diet_templates.is_active`는 테이블 전체 1개만 허용되는
     전역 플래그라, 필터 없이 `rows[0]`을 쓰면 "8월 생성인데 7월 템플릿이 active"
     상황을 그대로 썼음. `filters={..., year, month}` 추가로 해결.
  2. **active 다중 시 순서 보장** — `supabase_uploader.SupabaseREST.select()`에
     `order`/`limit` 파라미터 신설, `created_at.desc`로 최신 것만 사용.
  3. **임시 템플릿 파일 정리** — `main()`/요청 종료 시 `finally`에서 정리.
     GH Actions는 프로세스 단위라 원래 안전했지만, **app.py는 Render gunicorn
     워커가 장기 실행되므로 정리 안 하면 요청마다 임시파일이 실제로 누적됨**
     — Flask test_client로 요청 종료 후 파일이 실제 삭제되는지까지 확인함.
  4. **이름표 부여 결과 로그 출력** — 슬라이드별 부여/누락/경고를 그대로 출력.
  5. **폴백 시 `template_logs` 기록** — 다운로드 실패/이름표 부여 예외/검증 실패
     3가지를 `template_id` + `detail`(JSONB)로 구분 기록. 업로드 자체가 없는
     정상 상황은 기록 안 함. (신규 마이그레이션 불필요 — `template_logs.action`
     CHECK에 `'validate_fail'`이 2026-06-23 마이그레이션에 이미 있었음)
  - **공용화**: 로직을 `app_actions.py`/`app.py` 양쪽에 복붙하면 나중에 한쪽만
    고치는 사고가 난다(board/erp 중복본 정리 때 겪은 것과 동일 패턴) →
    `pptx-server/template_resolver.py`로 추출, 양쪽 다 이 모듈만 호출.
    (`resolve_template_path`/`log_validate_fail`/`cleanup_template_path`)
  - 검증: `app_actions.py`는 client(select/download_file/insert) 몽키패치로 6개
    시나리오(정상 사용/연월불일치/다중active/파싱실패/다운로드실패/컬럼불일치) 전부
    통과. `app.py`는 Flask test_client로 `/generate-from-json` 실제 호출 —
    "최종 사용 템플릿"이 업로드본으로 찍히는 것 + **요청 종료 후 임시 템플릿
    파일이 실제로 삭제되는 것**(장기 실행 프로세스 핵심 요구사항)까지 확인.
  - 커밋: `b14b329`(app_actions.py 최초 연결) → `67a5211`(공용 모듈 분리) →
    `79692d2`(app.py 이식, `/generate`·`/generate-from-json` 둘 다)
  - ⚠️ **`diet_templates.is_active`가 전역 1개 플래그라는 제약이 다음 항목(④ 원별
    방학 O/X 선택)의 전제를 깬다** — 자세한 내용은 아래 "④ 원별 방학 양식(O/X)
    선택" 항목 참고.
- **단계4(선택, 마지막)** — 업로드 시점 거부. `app/api/board/diet/templates/route.ts`
  POST가 **이미 부분 구현되어 있음**(JSZip으로 cNvPr name 4개 확인, `validation_result`
  DB 저장) 발견. 다만 (a) 실패해도 업로드는 그냥 진행(거부 없음), (b) 텍스트가 아니라
  "이미 이름이 붙어있는지"만 봐서 디자이너 원본은 지금도 무조건 "실패"로 뜸. "신설"이
  아니라 "기존 로직 업그레이드"로 재정의 필요 — 착수 시점에 다시 결정.
- **별도로 발견, 이번엔 손 안 댐**: 로컬 폴백 원본(`TEMPLATE_PATH`, 6월 공용 양식)
  자체가 예전 `_apply_template_names.py`(ElementTree 방식)로 이미 한 번 손댄 이력
  있음(`.bak` 파일 존재, 2026-06-23). 크기 차이가 -251KB로 예상보다 훨씬 큼(이미지
  재압축 가능성 높지만 미확인). 시간 될 때 실물로 한 번 열어볼 것.

---

## `_fix_allergy_only` 박스 이동 로직 제거 — ✅ 완료·실물 확인 통과 (커밋 `b0ec666`)

**제거한 것**
- `ALLERGY_BOX` 동적 배치
- `ORIGIN_BOX`/`MATERIAL_BOX` 강제 이동 (15행 `+175,260` 하향 / 10행 `8,395,899` 고정)
- 클립보드 아이콘 위치 보정 (`ORIGIN_BOX` 기준 정렬이라 기준점 소멸 → 함께 제거,
  단 리사이즈는 유지)

**남긴 것** (이번 범위 밖, 실측 근거 없음)
- 15행 행높이 정규화 + `graphicFrame` 크기 동기화
- 박스 폰트 크기 보정
- 로고 리사이즈 + 재배치

**좌표 검증** (8월 방학O 디자이너 원본 이름표부여본 + `test_data.json` 6월 픽스처)

| 원 | 박스 | 제거 전 | 제거 후 | 차이 |
|---|---|---|---|---|
| 강동E(15행·C) | ORIGIN | 8,603,277 | **8,428,017** | −175,260 |
| | MATERIAL | 9,400,463 | **9,225,203** | −175,260 |
| | ALLERGY | 10,150,203 | **9,900,789** | −249,414 |
| 목동E(10행·A) | ORIGIN | 8,395,899 | **8,382,815** | −13,084 |
| | MATERIAL | 9,180,001 | 9,180,001 | 0 |
| | ALLERGY | 9,989,340 | **9,853,671** | −135,669 |

제거 후 좌표 = 디자이너 원본 그대로. 코드가 더 이상 안 건드림.

**★ 실물 대조로 제거가 옳다는 것 확인** (같은 원끼리 비교, 사용자 PowerPoint 확인 완료)

목동ECC 실제 배포본 vs 우리 생성물:

| | 표 bottom | 알레르기 top | 원재료↔알레르기 겹침 |
|---|---|---|---|
| **실물(정답)** | 10,107,376 | 9,900,789 | **+52,410** (0.057in) |
| 우리 AFTER | 9,959,340 | 9,853,671 | +34,216 (0.037in) |
| 우리 BEFORE | 9,959,340 | 9,989,340 | −101,453 (안 겹침) |

- **실물도 겹친다. 우리보다 더 많이.** 그 상태로 49개 원에 배포되어 왔고 문제 없었음
  → 이 정도 겹침은 텍스트 내부 여백에 묻혀 화면상 무해
- **BEFORE는 실물과 방향이 반대**였음. 실물은 알레르기가 표를 파고드는데
  (표bottom 대비 −206,587) BEFORE는 표 아래로 밀어냈음(+30,000)
  → 밀어내던 코드가 오히려 실물과 다르게 만들고 있었다

강동ECC 실물도 동일 경향(원재료↔알레르기 겹침 +78,020 = 0.085in).

---

## ⚠️ 신규 발견 — 표 높이가 실물과 안 맞음 (미해결, 우선순위 높음)

**증상**: 생성 표 높이가 실제 배포본과 다르고, **원마다 방향까지 반대**다.

| 원 | 우리 표 bottom | 실물 표 bottom | 차이 |
|---|---|---|---|
| 강동ECC (15행) | 10,120,203 | 9,859,530 | **+260,673 (더 김)** |
| 목동ECC (10행) | 9,959,340 | 10,107,376 | **−148,036 (더 짧음)** |

**왜 중요한가**: 박스를 밀어내야 했던 근본 원인이 이것일 가능성이 높다.
표가 길어져 박스가 파묻히니 억지로 밀어낸 것 — 박스 이동은 증상 대응이었고
표 높이가 원인이다. 표 높이를 실물에 맞추는 게 근본 해결.

**원인 후보**: `_fix_allergy_only`에 **남겨둔** "15행 행높이 정규화 +
`graphicFrame` 크기 동기화" 로직. (`pptx_generator.py`)

**선행 조사 필요**: 왜 원마다 방향이 반대인지. 15행/10행에 서로 다른 보정이
걸려 있을 가능성. 실물 배포본 여러 개를 행 수별로 실측해서 정답값을 먼저 확보할 것.

---

## 신규 발견 — 날짜 표기 방식이 실물과 정반대 (미해결)

| | 날짜를 어디에 |
|---|---|
| **실물 (사람이 만든 것)** | 표 위에 떠 있는 **텍스트박스 유지** (`03` `04` … `31`, 19개) |
| **우리 코드** | 그 텍스트박스를 **전부 제거**하고 표 셀 안에 삽입 (`1⏎흑미밥…`) |

`replace_date_group()`이 날짜 텍스트박스를 비운다. 어느 쪽이 맞는지 결정 후 통일 필요.
실물이 정답이라면 우리 방식을 바꿔야 함.

---

## 다음 세션 최우선 작업

1. **표 높이 실물 맞추기** (위 "표 높이가 실물과 안 맞음" 참고) — 박스 문제의 근본 원인.
   실물 배포본을 행 수별로 실측해 정답값 확보 → `_fix_allergy_only`에 남긴
   행높이 정규화 로직 재검토
2. **날짜 표기 방식 결정·통일** (위 "날짜 표기 방식이 실물과 정반대" 참고)
3. **타입별 대표 원 5개(C/A/B/F/G) 실물 대조** — 실제 8월 데이터로 생성해
   배포본과 한 장씩 비교. "뭐가 빠졌나" 추측하지 말고 차이를 목록으로 뽑을 것
4. **원별 방학 양식(O/X) 선택** — 업로드 시 사람이 용도 지정(파일명 의존 금지),
   원별로 O/X 지정, 방학 없는 달엔 UI 자체가 안 나타나게.
   - **+ VACATION_IMG 발견사항 함께 반영**(이름표 부여 + `remove_holiday_cover_pics`
     제거 대상에서 예외 처리 — 위 "이름표 자동부여" 섹션 참고)
   - **⚠️ 착수 전 확인 필요 — `diet_templates.is_active` 제약이 이 작업의 전제를 깬다**:
     `is_active`는 (트리거로) 테이블 전체에서 **1개만** 허용되는 전역 플래그. 그런데
     "같은 달에 방학O/방학X 두 템플릿이 동시에 active"여야 원별로 골라 쓸 수 있음 —
     지금 구조로는 둘 중 하나만 active가 될 수 있어서 불가능. 착수 시 스키마부터
     바꿔야 함(예: `is_active`를 연/월 단위 유일 제약으로 바꾸거나, `variant`
     컬럼(`vacation_o`/`vacation_x`/`none`)을 추가해 `(year, month, variant)`
     조합별로 active 허용하도록 트리거 재설계).
   - **착수 전 사전 조사 필요**: 강동ECC·목동ECC는 8월에 방학이 없음(두 배포본 모두
     그림 4개 + 방학 텍스트 0건 = 방학X 양식. 방학O 양식은 그림 5개 + "여름방학"
     텍스트). **8월 배포본 49개를 훑어 방학 텍스트가 있는 원이 몇 곳인지 세어볼 것**
     — 소수면 예외 처리로 충분하고, 다수면 기본값 설계가 달라진다. 박스 좌표는
     방학O/X가 동일, 방학 그림 유무만 차이.
5. **위탁업장 `contract_type` 신설** (독립 작업, 작음) — 위탁 5곳 **등록 전**에 하면
   SQL 한 줄. `branch_filters.py`/`pptx-eligibility.ts`를 "temporary만 제외"에서
   **"permanent만 포함"으로 뒤집을 것** (새 유형 추가 시 자동 제외되는 방향이 안전)
6. **이름표 자동부여 단계4**(업로드 시점 거부) — 우선순위 낮음, 보류 중.
   `app/api/board/diet/templates/route.ts` POST에 이미 부분 구현된 검증 로직을
   "업로드 거부 + 텍스트 기준 검사"로 업그레이드할지 (위 "이름표 자동부여" 섹션 참고)

---

## 기타 미해결 (급하지 않음, 존재만 기록)

- **`weekly_menus.branch_id`를 `branches.id`로 잘못 조회하는 버그**
  - `app/api/branch-profiles/[id]/route.ts:65,73`
  - `app/board/(customer)/dashboard/page.tsx:96`
- **세션 충돌 storageKey 분리** 미착수 (ㄹ)
- **대치크레오 로그인 계정 미생성**
- **분당P 원산지 박스가 4주차 24일 식단칸과 겹침** (2026-06-25 발견, 미해결)
- **디자이너 양식은 매달 새로 제작되며 디자인뿐 아니라 구조도 바뀜**(방학 유무 등)
  → **특정 양식 하드코딩 금지**
- **덕양P·광교SLP 공휴일 운영 메뉴 미출력** (도시락·대체식 시트 미연결)
