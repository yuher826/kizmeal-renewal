# HANDOFF

> 이 파일은 항상 **"지금 상태"만** 담는다. 매 세션 끝에 최신 상태로 덮어쓴다.
> 과거 이력은 `git log HANDOFF.md`로 본다.

**최종 갱신:** 2026-08-10

---

## ⚠️ 최우선 확인 필요 (프로덕션 리스크)

- **송파크레오 계정 이메일이 임시 변경된 상태** — 현재 `yuher826+songpacreo@gmail.com`
  (실제 이메일 `dybmg@naver.com` 아님). **크레오에 정식 오픈 안내 전 반드시 원복.**
  - 위치: `/erp/branches/5cecbb7a-aaad-4f8b-b901-8ae4175c9475`
    → 담당자 계정 → 이메일 변경

---

## 정리 대상 (테스트용으로 만든 것들)

- **"테스트 매니저"** (`yuher826+erptest@gmail.com`) 관리자 계정 — 삭제 검토
- **송파크레오 이메일 원복** (위 최우선 항목과 동일 건)

---

## 오늘 완료된 것 (커밋 해시 포함)

- **크레오 3원 온보딩 + 임시원 배포 제외** — DB 온보딩(SQL) + `contract_type='temporary'` 적용
- **학부모 포털 게이트 완화** (`98377b8`) — pptx/pdf/jpg 중 하나라도 있으면 노출
  - ⚠️ **build만 통과, 실제 학부모(parent) 계정으로 크레오 원 PDF 노출은 브라우저 미검증.**
  - 확인 방법: `/parent` 학부모 계정 로그인 → 송파크레오 소속 아이 등록 → 식단표 메뉴 확인
- **크레오 식단표 직접 업로드 기능** (`1119f57`) — 원 포털(`board/customer`) 기준 **실전 검증 완료**
- **매니저 계정 생성 권한 확장** (`9cc9935`, `60f0926`) — UI 드롭다운 필터링까지 확인
  - ⚠️ 단, "관리자 계정 생성" 버튼을 끝까지 눌러 **실제 계정이 만들어지는지는 미검증**
- **/board/login 문구 수정** (`6a7cdd2`) — "학부모" 언급 제거
  - ⚠️ dev 재기동 후 실제 화면 반영 **[결과 확인 필요 — 세션 마지막 재기동 요청했으나 결과 미확인]**

### 2026-08-10

- **ERP 사이드바 메뉴 중복 활성화 버그 수정** (`7de1eb8`) — 하위 경로가 여러 메뉴 href의
  prefix에 동시에 걸릴 때 두 메뉴가 동시에 active 처리되던 문제. 가장 구체적인(긴) href
  하나만 active로 판정하도록 수정. **프로덕션 배포 확인 완료.**
- **next.config.mjs 로컬 dev 웹팩 캐시 경합(-4094 UNKNOWN) 이슈 해결** (`2d2e836`) —
  Windows에서 `next dev` 시 여러 워커가 웹팩 디스크 캐시(`.next/cache/webpack`)에 동시에
  쓰기 시도하며 나던 `-4094 UNKNOWN`, `pack.gz rename ENOENT` 에러. dev 전용으로
  `config.cache=false`(메모리 캐시만 사용) 적용, 프로덕션 빌드는 영향 없음(clean build로 확인).
  - 부수 조치: **알약 탐지 제외에 프로젝트 폴더 등록함**
- **`gen_form.py` 6월 서식 잔재 제거** (`cb82311`)
  - 기준폼이 6월 실데이터 파생이라 (a) 1주차 D열 선거일 회색 (b) 1·2·3주차 F열 🎂 +
    18행 생일 주황 (c) 5주차 D·E·F 해당없음 회색이 **다른 달에 눌어붙던** 문제
  - 각 주차 **B열(운영일)** · **5주차 D열(해당없음)** · **1주차 D열(공휴일)** 을 서식
    템플릿으로 캡처해 **열 통째 덮어쓰는** 방식. **색상 하드코딩 없음**
  - 검증: 7월(5주차 꽉참)·8월(경계) **6월 잔재 4종 0건**, 헤더 공백 2칸 코드포인트 대조
    일치, **실물 엑셀 확인 완료**
  - **공휴일 자동 판정은 의도적으로 넣지 않음** — 공휴일에 운영하는 원과 쉬는 원이 갈려
    코드가 일괄로 정할 수 없음. `holidays` 라이브러리 도입 검토했다 **철회**.
    대신 `--holiday 17:제헌절` 옵션 신설

---

## ⚠️ 신규 미해결 — 오늘 발생, 운영 확정 필요

- **`generate-form.yml`이 `--holiday` 없이 `gen_form.py`를 호출함**
  (`.github/workflows/generate-form.yml:33`)
  → ERP에서 폼 생성 시 **공휴일 지정 경로가 없음**. 아래 둘 중 하나로 확정 필요:
  - ① ERP UI에서 공휴일을 입력받아 워크플로로 전달
  - ② 영양사가 생성된 파일 헤더를 직접 수정
- **엑셀 `✏️ 입력 가이드` 시트 r65~69 공휴일 처리 안내 보완 필요**
  - 헤더에 공휴일을 **어떤 형식으로 쓰는지가 빠져 있음**
    (형식: `수  7/17` 줄바꿈 `공휴일` 줄바꿈 `(제헌절)`)
  - r66 C열 **"시스템이 자동 잠금 처리"는 현재 코드와 불일치** — `gen_form.py`는
    `--holiday` 지정 시에만 잠금
- **배서영 영양사·권팀장에게 "공휴일은 영양사가 헤더에 직접 표시" 방식 공유**

---

## 다음 세션 최우선 작업

**엑셀 v6에 "박스 위치 표시" 칸 신설 설계**

- 배경: 원산지/원재료/알레르기 박스는 **내용은 원별 고정이나 위치는 매달·매원 다름**.
  7월 실물에서 동탄POLY는 5주차 전체 방학, 송도POLY는 5주차 목·금부터 방학이라
  빈칸 위치가 서로 달랐음. 현재 `_fix_allergy_only`(`pptx-server/pptx_generator.py:1187`)의
  상대값 공식(`table_bottom + GAP`, `week5_top` + 오프셋)은 10행↔15행 사이
  **27~40만 EMU 오차**가 있어 매달 깨질 수 있는 구조.
- 방향: **코드가 빈칸을 알고리즘으로 추측하지 말고, 엑셀에 사람이 표시 → 코드가 읽어서
  배치.** 엑셀 v6가 아직 영양사 미사용이라 **지금이 폼 수정 적기.**
- 확정 필요:
  1. 표시 방식 — 주차 시트에 직접 표식 vs 별도 배치 시트
  2. 원별 차이 처리 — 49개 개별 vs 브랜드 단위 묶기
  3. 방학 그림도 같은 체계로 갈지

---

## 기타 미해결 (급하지 않음, 존재만 기록)

- **`weekly_menus.branch_id`를 `branches.id`로 잘못 조회하는 버그**
  - `app/api/branch-profiles/[id]/route.ts:65,73`
  - `app/board/(customer)/dashboard/page.tsx:96`
- **세션 충돌 storageKey 분리** 미착수 (ㄹ)
- **대치크레오 로그인 계정 미생성**
- **/board 라우트 이름 개명 검토** (장기 — 문구는 오늘 고쳤지만 경로 자체는 그대로)
- **분당P 원산지 박스가 4주차 24일 식단칸과 겹침** (2026-06-25 발견, 미해결)
- **디자이너 양식은 매달 새로 제작되며 디자인뿐 아니라 구조도 바뀜**(방학 유무 등)
  → **특정 양식 하드코딩 금지**
- **덕양P·광교SLP 공휴일 운영 메뉴 미출력** (도시락·대체식 시트 미연결)
