# HANDOFF

> 이 파일은 항상 **"지금 상태"만** 담는다. 매 세션 끝에 최신 상태로 덮어쓴다.
> 과거 이력은 `git log HANDOFF.md`로 본다.

**최종 갱신:** 2026-09-04(2차) — ★권한 2단계(`ErpUserContext` 신설)
완료★. 커밋 완료 — 빌드·실물 확인 전부 통과했다. ERP 안에서 "본인 admins 행"을 각 페이지가
따로 조회하던 곳이 실측 **9곳**(클라이언트 6 / 서버 3)이었다. 클라 6곳만
`useErpUser()`로 컨텍스트 조회로 바꿨고(서버 3곳은 React 컨텍스트가
안 닿아 그대로 둠), `ErpUser`에 `auth_id`·`access_scope`·`is_active`
3개 필드를 추가했다. 진짜 이유는 성능이 아니라 3단계 대비다 — 이제
`can_handle_cs`·`can_write_notices` 플래그가 추가돼도 `layout.tsx`의
select 한 줄만 고치면 6곳이 따라온다(아래 권한 섹션 갱신분 참조).
다음은 3단계(플래그 컬럼 2개 + `staff` 역할).

**2026-09-04(1차) 갱신:** ★권한 1단계(1층 매트릭스 + 라우트 가드 +
역할별 착지 경로) 완료★. `lib/erp-access.ts` 신설로 `canAccessErpPage`·
`landingPathFor`를 한 곳에 모았고, `middleware.ts`에 역할 가드를 추가해
영양사 계정으로 `/erp/admins`를 직접 치면 막히게 했다. 착지 하드코딩은
조사 단계에선 3곳으로 알았으나 실측 결과 **6곳**이었고, `ErpLoginForm`의
`next` 파라미터가 검증 없이 그대로 쓰이고 있어 director 로그인 시
무한 루프 방아쇠가 될 뻔한 것도 이번에 발견해 함께 막았다(아래 권한
섹션 갱신분 참조). 커밋 완료 — 빌드·diff·실물 확인 전부 통과했다.

**2026-09-02(3차) 갱신:** ★권팀장 5·7번 CS '미처리' 이름 충돌
정리 완료(`099f032`)★. 같은 `pending`이 한 화면에서 세 이름으로 불리고
있었고, '미처리'는 전혀 다른 개념(안 읽음)에도 쓰이고 있었다. `lib/types.ts`의
`STATUS_LABELS.pending='대기중'`에 표기를 맞췄다. ★후속 4건 미해결★(519행
하드코딩·0건 카드·카드 축 불일치·board/admin의 반대 의미 '미처리').

**2차 갱신:** 2026-09-02 — ★ERP 권한 설계 확정★. 조사 중
★ERP는 로그인만 하면 전 페이지 접근 가능★한 구멍을 발견했다(영양사 계정으로
`/erp/admins`가 주소창으로 열린다). 8/28에 결정해 두고 HANDOFF에 남기지
않은 「방식 B 2층 구조」를 과거 대화에서 되찾았고, 8/28의 숙제였던 1층
매트릭스를 오늘 다 채웠다. `designer` 역할 신설은 철회하고 `staff`+체크박스로
간다. 다음은 1단계(1층 매트릭스 + 라우트 가드 + 착지 경로) — 구멍부터 막는다.

**1차 갱신:** 2026-09-02 — ★커밋2 기준폼 첫인상 완료(`6de9bb5`)★.
컨테이너 대조 + 10월 전파 + Excel 실물, 3중으로 검증해 통과했다. 작업 중
★HANDOFF에 적혀 있던 전제 2건이 실측과 달랐고★(B열 열 폭 / 인쇄 축소),
★`gen_form.py` 신규 미해결 1건★(생성 빈 폼의 생일간식 칸이 8pt 연회색)을
발견했다. 반찬5 21개소는 ★커밋①이 문구만 지우고 서식을 남긴 잔재★로
확증됐다. 참조표 숨김은 ★방침을 뒤집어 하지 않기로★ 했다 — 오류 안내가
참조표를 가리키고 ⑧에서 별칭 열을 신설할 예정이라 더 자주 봐야 할 시트다.
다음은 권팀장 요청 1·4·7 + 5번 필터 정리.

**이월 항목**(위 순위와 별개로 남아 있는 것):
생성분 PPTX PowerPoint 실물 확인(날짜 숫자 위치·추석 이미지·박스 겹침)
+ ⑯생성 전 확인 팝업(활성 양식 없으면 차단) + ⑪진행률 잠김 + ⑩연월이
전월로 초기화되는 문제 + 도시락 팝업 이관 + ⑭`diet_review_items` →
검토·승인·배포 플로우 + `_check_box_overlap`의 MATERIAL_BOX↔ALLERGY_BOX
겹침 경고(4세션 연속 범위 밖) + 버그④ 송파랜 PDF 손상 + 슬라이드 구조
불일치 4건 + 이름표 부여 판정 UI 실물 확인(Chrome 확장 없어 세 세션
연속 미완료) + 활성화 게이트 신설 + 엑셀 빈칸 역산·방학/공휴일 두 축
통합 설계 + 역할×메뉴 노출 매트릭스(사람이 확정
필요) + RLS 섹션 안 신규 미해결 3건(a·b·c) + `audit_logs` 조회 화면
부재 + DELETE 개명·상수화 + 실데이터 점검(필수항목 미입력 원) + dev 서버
`.next` 오염 자동감지 + director 권한 갭 5순위(고객사 공지)
+ ★신규★ `gen_form.py` 생일간식 칸 8pt 연회색(아래 커밋2 섹션)
+ ★신규★ `'admin'` 레거시 role 잔재 청소 (아래 권한 섹션 "1단계 완료" 참고,
  DB 실계정 0건 확인함)
+ 템플릿 '업로드됨·활성화 대기' 배지
+ ★신규★ CS 후속 4건 — 519행 '대기중' 하드코딩 / 0건 카드가 눌림
  / 통계 카드 4개 축 불일치 / board/admin 의 '미처리'는 unread 기준
  (아래 CS 섹션)
+ ★신규★ CS 메시지 발신자 판정 오류 — 테스트 매니저 계정으로 쓴 답변이
  '키즈밀 관리자' 이름으로 저장됨. auth_id가 아닌 다른 기준이 섞인 것으로
  추정 (아래 CS 섹션, 권팀장 9번 착수 전 조사 필요)
+ ★신규★ 내부 메모가 대화 흐름에 그대로 섞여 표시됨 — 권팀장 9번
  "가운데 자물쇠 스타일 분리"가 왜 필요한지 실물로 확인됨 (아래 CS 섹션)
+ ★신규★ `scripts/dev-fresh.cjs`가 node 프로세스를 하나만 종료해
  `.next` 삭제가 `ENOTEMPTY`로 실패함 — 실제로 5개가 떠 있었다.
  전체 종료 + 대기 시간 필요 (아래 CS 섹션 뒤에 별도 기록).

**다음 세션 착수 지점:**
> "kizmeal-renewal 이어서. HANDOFF.md 읽고 시작하자.
> 권한 2단계 커밋 확인 후 3단계(admins 컬럼 2개 + staff 역할)부터."

---

## ✅ 권팀장 5·7번 — CS '미처리' 이름 충돌 정리 (2026-09-02, `099f032`)

### 무엇이 문제였나 — 버그가 아니라 이름이 겹친 것

같은 `pending` 상태가 한 화면에서 **세 가지 이름**으로 불리고 있었고,
게다가 '미처리'는 전혀 다른 개념에도 쓰이고 있었다.

```
통계 카드        '미처리'    status === 'pending'
드롭다운         '대기'      STATUS_FILTER_TABS
상태 변경 버튼    '대기중'    STATUS_LABELS (lib/types.ts)
오른쪽 버튼      '미처리만'   unread_count_admin > 0   ← 완전히 다른 개념
```

`unreadOnly`는 **"관리자가 아직 안 읽은 메시지가 있는 문의"**다.
문의를 열면 `unread_count_admin`이 0으로 리셋되므로(`page.tsx:472,474`)
★처리 여부와 무관하다★ — 읽기만 하고 답을 안 해도 목록에서 사라진다.
'미처리만'이라는 이름 때문에 권팀장이 놓칠 수 있는 구조였다.

### 적용 — `inquiries/page.tsx` 문자열 3곳만

| 행 | 전 | 후 |
|---|---|---|
| 519 | 통계 카드 `'미처리'` | `'대기중'` |
| 567 | 버튼 `미처리만` | `● 새 메시지만` |
| 443 | 주석 `미처리(unread)…` | `안 읽은 메시지…` |

`git diff --stat` = 1 file changed, 3 insertions, 3 deletions.
`InquiryStatus` 값·`statFilter`·`unreadOnly`·`filterStatus` 로직·카드
색상/순서/개수 전부 무변경. `npm run build` 통과.

### ★'대기'가 아니라 '대기중'으로 맞춘 이유★

처음엔 드롭다운과 같은 `'대기'`로 하려 했으나 **철회했다.**
두 필터는 같은 `status === 'pending'`을 AND로 보므로 **완전히 같은
기능**이다(`page.tsx:393`). 라벨까지 똑같이 만들면 "같은 필터가 두 개"로
보여 새로운 혼란이 생긴다.

조사 결과 `lib/types.ts:83-88`의 `STATUS_LABELS.pending`이 이미
`'대기중'`이었고, **상태 변경 버튼과 상태 변경 로그 메시지가 전부
이 상수를 참조**하고 있었다
(`InquiryDetailPanel.tsx:1099` — `상태가 '${STATUS_LABELS[status]}'(으)로…`).
즉 통계 카드만 그 밖에서 따로 `'미처리'`를 쓰고 있었던 것이다.
→ 상수 쪽 표기에 맞췄다. 드롭다운(`'대기'`)과는 글자가 달라 중복으로도
   보이지 않는다.

### ★후속 과제 3건 — 미해결★

**① 519행이 `'대기중'` 문자열 하드코딩 상태다**
원래는 `label: STATUS_LABELS.pending`으로 참조해야 한다. 지금은
`STATUS_LABELS.pending`을 바꾸면 통계 카드만 어긋나 오늘과 같은 문제가
재발한다.
→ 지금 안 한 이유: 통계 카드 4개 중 `pending`만 상태이고 나머지 셋은
  다른 축이다(아래 ③). 하나만 상수 참조로 바꾸면 네 줄의 모양이 달라져
  오히려 읽기 나빠진다. **6번·10번 용어 재설계 때 네 개를 함께 정리한다.**

**② 0건 카드가 눌린다 — 권팀장 5번의 나머지**
실물에서 `대기중 0`, `오늘 신규 0`인데도 클릭되어 빈 목록이 나온다.
`page.tsx:519~535`의 `button`에 `disabled={c.value === 0}` 계열 처리가
필요하다. 이번 커밋은 라벨만 다뤘으므로 범위 밖으로 뒀다.

**③ 통계 카드 4개의 축이 제각각이다**
```
대기중      오늘 신규    SLA 초과    이번달 처리
 상태        기간         경보        기간+상태
```
`대기중`만 상태다. 6·10번 용어 재설계 때 함께 봐야 한다.

**④ `app/board/admin/inquiries/page.tsx`에도 '미처리'가 있다**
★거기서는 `stats.unread` 기준 — ERP와 정반대 의미다★
(`:247` `미처리 {stats.unread}건`, `:261` 카드 라벨).
HANDOFF에 `app/board/admin/`이 리다이렉트된 죽은 코드라고 적혀 있으나,
파일을 열어보니 `'use client'`로 시작하는 살아 있는 컴포넌트였다.
★리다이렉트가 어디서 걸리는지 확인 전에는 죽었다고 단정할 수 없다.★
→ 이번 커밋에서는 건드리지 않았다. 죽은 코드인지 먼저 확인할 것.

**⑤ ★신규(2단계 실물 확인 중 발견)★ CS 메시지 발신자 판정 오류**
테스트 매니저 계정으로 로그인해서 쓴 답변이 화면에 **'키즈밀 관리자'**
라는 이름으로 저장·표시된다. 실제 작성자(매니저 본인)가 아니다.
`InquiryDetailPanel.tsx:690`이 `msg.sender_id !== currentAdmin.auth_id`로
"내가 쓴 메시지인지"를 판정하는데(2단계에서 컨텍스트로 옮긴 그 로직),
저장되는 이름 자체가 다른 기준으로 붙는 것으로 보인다 — **발신자
판정에 `auth_id`가 아닌 다른 기준(고정 문자열 또는 다른 컬럼)이
섞여 있을 가능성**. 원인 미조사.
→ 권팀장 9번(담당자별 이름 작게 노출, 카톡 그룹채팅 스타일)
  착수 전에 반드시 먼저 고쳐야 한다 — 이름이 틀리면 9번 기능 자체가
  무의미해진다.

**⑥ ★신규(2단계 실물 확인 중 발견)★ 내부 메모가 대화 흐름에 섞여 보인다**
일반 답변과 내부 메모(`is_internal`)가 채팅 화면에서 시각적으로
구분 없이 나란히 표시된다. 권팀장 9번 요청("내부 메모는 가운데
자물쇠 스타일로 분리 — 우리 쪽에 섞이면 실수로 발송할 위험")이
왜 필요한지 실물로 재확인된 것 — 설계는 이미 위 "권팀장 요청 10건"
섹션 9번에 있으나 구현 전이다.

### ★신규(2단계 실물 확인 중 발견)★ `scripts/dev-fresh.cjs`가 node 프로세스를 하나만 종료한다

`.next` 폴더를 지우고 dev 서버를 새로 띄우는 스크립트인데, 기존 node
프로세스를 종료하는 부분이 **첫 번째 하나만 죽이고** 나머지는 그대로
둔다. 이번에 실물 확인 중 실제로 **node 프로세스가 5개** 떠 있었고,
남은 프로세스가 `.next` 폴더를 잡고 있어 삭제가 `ENOTEMPTY`로
실패했다. 죽이는 대상을 전체 프로세스로 넓히고, 종료 후 파일 핸들이
풀릴 때까지 짧은 대기 시간을 넣어야 한다. HANDOFF의 기존 "dev 서버
`.next` 오염 자동감지" 항목과 같은 계열의 문제이나, 이번 건은 원인이
구체적으로 특정됐다(프로세스 킬이 불완전함).

---

## ★ERP 권한 설계 확정 + 1단계 완료 (2026-09-02 설계 / 2026-09-04 1단계)★

### ✅ 1단계 완료 (2026-09-04, 커밋 완료 — diff·실물 확인 통과)

**착수 전 SQL 실측 결과** — `super_admin` 2 / `manager` 2(그중 1건
`erp_only`) / `director` 1 / `nutritionist_ck` 1. `admin`·NULL·비활성
계정은 **0건**이다. `nutritionist_consignment` 실계정도 0건.

**실측 정정 — 착지 하드코딩 3곳 → 6곳**
설계 단계(9/2)에서 조사한 하드코딩은 `app/erp/page.tsx` /
`ErpLoginForm.tsx:39,87` / `ErpSidebar.tsx:64` 3곳이었으나, 착수해서
grep으로 전수 확인하니 아래 3곳이 더 있었다.
```
app/erp/not-found.tsx:18       클라이언트 링크 → '/erp/my-page' 리터럴로 고정
                                (원래 있던 '/erp/branches' 링크는 director 등에서
                                 죽은 링크가 되므로 함께 제거, 링크 하나만 남김)
app/board/admin/layout.tsx:24  erp_only 계정 → landingPathFor
app/board/login/page.tsx:31,82 erp_only 계정 → landingPathFor
```

**신규 발견 — `next` 파라미터 무검증 (진짜 루프 방아쇠)**
`ErpLoginForm.tsx`가 로그인 성공 후 `next` 쿼리 파라미터를 검증 없이
그대로 `router.push`했다. director가 `/erp/diet` 링크로 들어오면
middleware가 `/erp/login?next=/erp/diet`로 보내고, 로그인 폼이 `next`를
그대로 push하고, 새 역할 가드가 다시 막아 **무한 루프**가 될 뻔했다.
`next && canAccessErpPage(admin, next) ? next : landingPathFor(admin)`로
검증 후 사용하도록 고쳤다 — 착지 로직과 분리할 수 없는 한 묶음이라
같은 커밋에 넣었다.

**`canAccessErpPage` 시그니처를 `(role, pathname)`이 아니라 admin 객체로
잡은 근거** — 9/2 설계 메모(아래 "나중에 2층을…" 절)엔
`canAccessErpPage(role: string, pathname: string)`로 적혀 있었으나 실제
구현은 `canAccessErpPage(admin: {role, can_manage_templates}, pathname)`로
바꿨다. `/erp/diet/templates`가 role이 아니라 `can_manage_templates`
플래그로 열리기 때문이다(이미 존재하는 컬럼). 앞으로 2층 체크박스가
`can_handle_cs`·`can_write_notices` 2개 더 늘어날 예정이라 처음부터
admin 객체로 받는 편이 그때 시그니처를 또 안 바꿔도 된다.

**착지 검증 — 빌드 타임 스크립트 대신 구조로 없앰**
9/2 설계 메모는 "착지 경로는 그 역할의 허용 목록 안에 반드시 있어야
한다 — 빌드 타임 검증을 넣는다"였으나, 실제로는 별도 검증 스크립트를
만들지 않았다. `landingPathFor`가 각 역할의 우선순위 목록을
`canAccessErpPage`로 직접 걸러(`.find(p => canAccessErpPage(admin, p))`)
허용된 첫 경로만 고르고, 마지막 폴백이 전 역할 허용인 `/erp/my-page`라
**착지가 허용 목록 밖으로 나가는 것이 구조적으로 불가능**하다. 검증
스크립트가 따로 필요 없다.

**신규 미해결 — `'admin'` 레거시 role 잔재 청소**
DB 실계정 0건을 확인했으니(위 SQL 실측) 아래 4곳의 `'admin'` 취급은
전부 죽은 코드다. 이번 범위에서는 건드리지 않았다.
```
types/erp.ts:19            ErpRole 유니온에 'admin' 포함
app/erp/(protected)/layout.tsx:32   role 없을 때 ?? 'admin' 폴백
components/erp/ErpHeader.tsx        ROLE_BADGE.admin
components/erp/ErpSidebar.tsx       ROLE_BADGE.admin
lib/roles.ts                        ROLE_LABEL.admin
```

### ✅ 2단계 완료 (2026-09-04, 커밋 완료)

**실측 — ERP 안에서 "본인 admins 행" 조회 9곳**
```
클라이언트 6곳 (컨텍스트로 전환)
  diet/page.tsx:299~306                useEffect select('role')
  upload/page.tsx:50~61                checkAuth() select('role') + loadingRole
  admins/page.tsx:148                  Promise.all 안 select('*').eq('auth_id', …)
  files/new/page.tsx:94                제출 핸들러 안 select('id').eq('auth_id', …)
  inquiries/[id]/page.tsx:68           Promise.all 안 select('*').eq('auth_id', …)
  InquiryDetailPanel.tsx:812           Promise.all 안 select('*').eq('auth_id', …)

서버 컴포넌트 3곳 (그대로 둠 — 컨텍스트가 안 닿음)
  my-page/page.tsx, branches/new/page.tsx, branches/[id]/page.tsx
```
전체 admins 목록을 가져오는 3곳(`admins/page.tsx:146`,
`inquiries/[id]/page.tsx:63`, `InquiryDetailPanel.tsx:807`)은 성격이
다르다 — 담당자 드롭다운·관리자 목록용이라 "본인" 조회가 아니므로
제외했다.

**클라 6곳만 컨텍스트로, 서버 3곳은 그대로 둔 이유**
React 컨텍스트(`createContext`/`useContext`)는 클라이언트 전용이라
서버 컴포넌트에서 못 쓴다. 서버 3곳을 컨텍스트로 바꾸려면 그 페이지
자체를 클라이언트 컴포넌트로 바꿔야 하는데, 이번 범위를 벗어난다.

**`auth_id`를 `ErpUser`에 넣은 근거**
`InquiryDetailPanel.tsx:690`이 `currentAdmin.auth_id`를
`msg.sender_id`와 비교해 "내가 보낸 메시지인지"를 판정한다. 이게
없으면 이 파일은 컨텍스트로 못 바꾼다. `access_scope`·`is_active`는
이참에 layout이 이미 조회하는 값을 그대로 흘려보낸 것으로,
`ErpUser`를 쓰는 곳 어디서든 admins 행 원본 없이 판단 가능하게 했다.

**★middleware ↔ layout 중복 조회 제거 — 하지 않기로 뒤집은 근거★**
1단계 스펙에 "2단계 몫"이라 적어뒀던 항목이었으나 실측 후 뒤집었다.
middleware가 `role, can_manage_templates` 2개를, layout이 8개
(`id, auth_id, name, email, role, access_scope, is_active,
can_manage_templates`)를 조회해 페이지당 admins 조회가 2회다.
헤더로 넘겨 없앨 수 있지만 하지 않는다 — layout은 어차피 `name`·
`email`까지 필요해서 조회 자체를 없앨 수 없고, 헤더로는 2개 컬럼만
아끼는데 그 대가로 한글 이름 인코딩·헤더 크기 제한·middleware와
layout의 결합이라는 문제가 붙는다. 남는 장사가 아니다.

**`Promise.all` 구조분해 인덱스 함정**
`inquiries/[id]/page.tsx`와 `InquiryDetailPanel.tsx`는 `Promise.all`
배열 안에 "본인" 조회와 "전체 목록" 조회가 나란히 있었다. 배열에서
"본인" 항목 하나만 제거하니 뒤따르는 구조분해 변수명이 그대로
한 칸씩 밀릴 뻔했다 — 구조분해 변수 이름을 배열 순서에 맞춰
다시 세는 방식으로 피했다(이름으로 매칭되는 게 아니라 위치로
매칭되므로, 배열에서 하나를 빼면 그 뒤 이름들이 전부 한 칸씩
다른 쿼리 결과를 가리키게 된다).

**3단계 이점** — `can_handle_cs`·`can_write_notices` 플래그가
추가돼도 `layout.tsx`의 select 한 줄만 고치면 컨텍스트를 쓰는
6곳 전부에 자동으로 따라온다.

---

### ★현재 열려 있는 구멍★

`app/erp/(protected)/layout.tsx`의 가드는 **역할(role)을 전혀 보지 않는다.**
로그인 · `admins`에 있음 · `is_active` · `board_only` 아님 — 이 넷만 본다.
즉 **영양사 계정으로 주소창에 `/erp/admins`를 치면 관리자 관리가 열린다.**

`allowedRoles`는 딱 한 곳(`lib/erp-nav.ts:51` 관리자 관리)에만 있고,
`ErpSidebar.tsx:76`에서 **메뉴 표시 여부**로만 쓰인다. 메뉴에서 감췄을 뿐
문은 열려 있다.

`/erp/history`(배포 이력)는 **권한 체크가 아예 없다.**

### 방식 B 확정 — 두 층 구조

★2026-08-28 세션에서 이미 결정했고 유대표님이 동의하셨다. 그런데
HANDOFF에 남기지 않아 2026-09-02에 솔아가 모른 채 뒤집었다.
★결정은 반드시 HANDOFF에 남긴다★ — 이 항목 자체가 그 교훈이다.

```
1층  역할 → 메뉴      코드(lib/roles.ts)에 매트릭스. 거의 안 바뀜
2층  배정 → 체크박스   admins 컬럼 플래그. 사람마다 다름
```

전면 매트릭스(사람×메뉴×동작)는 채택하지 않았다 — 한 사람당 체크박스
48개가 되고, 대부분 칸이 현실에 없는 조합이며, API 13곳을 전부
다시 고쳐야 한다. 현실의 예외는 두세 개뿐이다.

★전면 매트릭스가 맞아지는 때 = 로드맵 4단계(외부 SaaS 판매).★
고객사마다 조직도가 다르면 우리 역할 5종을 강요할 수 없다.
B로 만들어두면 A로 넘어갈 때 버리는 게 없다 — 체크박스가 늘어날 뿐이다.

### 2층 체크박스 (신설 예정)

| 플래그 | 켜는 사람 |
|---|---|
| `can_handle_cs` | 영양사 3분 + CS 전담 직원 |
| `can_write_notices` | 영양사 또는 전담 직원 |
| `can_manage_templates` | 담당 1명 (이미 존재) |

근거 — CS·공지·템플릿은 전부 **"직무가 아니라 배정"**이다. role로 열면
영양사 계정이 늘어날 때마다 권한이 아무도 모르게 따라붙는다.

### ★`designer` 역할은 만들지 않는다★

2026-09-02에 디자이너(윤수경) 계정을 위해 `designer` role 신설을
검토했으나 **철회했다.** 8/28 원칙이 **"역할은 최소로, 권한은
체크박스로"**다. 역할을 사람 수만큼 늘리면 그 원칙이 깨진다.

```
윤수경 · staff        ☑ 템플릿 업로드
CS 전담 직원 · staff   ☑ CS 관리 대응  ☑ 고객사 공지 작성
```

**같은 역할, 다른 체크박스**로 둘 다 표현된다.
→ `staff`(일반 직원) 역할을 신설한다. 기본 권한은 로그인뿐이고
   실제 권한은 체크박스로만 준다.
→ ★디자이너 계정 발급은 2층이 끝난 뒤★. 지금 만들면 나중에 다시 옮겨야 한다.

`director`를 CS 직원에게 재사용하지 않는다 — 허이사님 전용 읽기
권한이라 의미가 섞이면 "디렉터가 대체 누구지?"가 된다.

### 템플릿 업로드 ↔ 활성화 분리

디자이너에게는 **업로드만** 준다. 활성화는 담당 영양사·super_admin.

근거 — 업로드는 **납품**이고 활성화는 "이 양식으로 49개 원을
만들겠다"는 **채택 결정**이다. 디자이너는 수정본을 여러 번 올린다.
활성화까지 넘기면 **마지막에 올린 것이 자동으로 최종**이 된다.
되돌리는 비용도 비대칭이다(클릭 한 번 vs Actions 49개 재생성).

※ 활성화가 곧 배포는 아니다 — `is_active`는 **PPTX 생성 시점**에
  `template_resolver.py`가 읽고, 뒤에 생성→검토→승인→배포가 남는다.
  그래도 위 근거로 분리한다.

구현 — `lib/roles.ts`에 `canUploadTemplate` 신설. 세 API 중 하나만 바뀐다.

```
POST   /api/board/diet/templates      → canUploadTemplate (신설)
POST   .../[id]/activate              → canManageTemplates (그대로)
DELETE .../[id]                       → super_admin (그대로)
```

★딸려오는 과제★ — "올렸는데 아무도 활성화 안 한 상태"가 생긴다.
템플릿 관리 화면에 **"업로드됨 · 활성화 대기" 배지**가 필요하다. 시점 미정.

### ★1층 매트릭스 완성본★ (8/28 숙제였던 `?` 칸을 9/2에 다 채움)

**기본 차단(deny by default)** 으로 구현한다. 허용 목록에 없는 새 페이지는
아무도 못 들어가므로 즉시 발견된다. middleware에 이미 적힌 원칙과 같다
— "열거식 금지, 새 페이지가 추가돼도 자동으로 보호".

| 경로 | super_admin | manager | director | 영양사(직영·위탁) | staff |
|---|:-:|:-:|:-:|:-:|:-:|
| `/erp/diet` | ✅ | ✅ | ❌ | ✅ | ❌ |
| `/erp/upload` | ✅ | ✅ | ❌ | ✅ | ❌ |
| `/erp/review` | ✅ | ✅ | ✅ | ✅ | ❌ |
| `/erp/history` | ✅ | ✅ | ✅ | ✅ | ❌ |
| `/erp/branches` | ✅ | ✅ | ❌ | ✅ | ❌ |
| `/erp/diet/templates` | ✅ | ❌ | ❌ | ✅ 담당자 | ☑ 플래그 |
| `/erp/notices` | ✅ | ✅ | ✅ 읽기 | ❌ | ☑ 플래그 |
| `/erp/inquiries` | ✅ | ✅ | ✅ 읽기 | ❌ | ☑ 플래그 |
| `/erp/files` | ✅ | ✅ | ✅ 읽기 | ❌ | ❌ |
| `/erp/admins` | ✅ | ✅ | ❌ | ❌ | ❌ |
| `/erp/brands` | ✅ | ✅ | ❌ | ❌ | ❌ |
| `/erp/stats` | ✅ | ✅ | ✅ | ❌ | ❌ |
| `/erp/my-page` | ✅ | ✅ | ✅ | ✅ | ✅ |

근거 메모
- `/erp/upload` = `UPLOAD_ROLES` — 페이지가 이미 그 상수를 쓰고 있다
  (`upload/page.tsx:7,28`). 새로 정한 값이 아니라 기존 값을 따라간 것
- `/erp/history` = `REVIEW_ALLOWED_ROLES` — 배포 이력은 "무엇이 언제
  누구에게 나갔는가"라 검토와 성격이 같다
- `/erp/my-page` = 전 역할 — 비밀번호 변경이라 막으면 안 된다
- 위탁 영양사(`nutritionist_consignment`)는 직영과 동일하게 둔다 —
  기존 상수들이 `NUTRITIONIST_ROLES` 전체를 묶어 다루는 것과 일관
- 템플릿 관리 manager ❌ — role이 아니라 배정 플래그로 여는 것이 설계
  의도다. 권팀장이 만지셔야 하면 `can_manage_templates`를 켜드린다
- ★허이사 "읽기"는 라우트 가드로 구현할 수 없다★ — 가드는 진입 여부만
  정한다. 작성 버튼 감추기·서버 API 차단은 별도 작업

### ★착지 경로 — 무한 리다이렉트 함정★ (✅ 1단계에서 해결, 위 절 참고)

현재 **모든 착지가 `/erp/diet` 하드코딩**이다.

```
app/erp/page.tsx        redirect('/erp/diet')
ErpLoginForm.tsx:39,87  next || '/erp/diet'
ErpSidebar.tsx:64       로고 → '/erp/diet'
```

director·staff는 `/erp/diet` 접근 불가 → 로그인 → 차단 → **무한 루프**.
★가드와 착지는 분리할 수 없다. 같은 커밋에서 해야 한다.★

```
super_admin · manager · director  →  /erp/inquiries
nutritionist_ck / _consignment    →  /erp/diet
staff                             →  플래그에 따라 (템플릿/CS/공지)
```

`lib/roles.ts`에 `landingPathFor(role)` 헬퍼 하나를 만들고 세 곳이
호출한다. 세 곳에 각각 분기를 쓰면 10번의 `assigned_admin_id` 3벌
중복과 같은 함정에 빠진다.
★착지 경로는 그 역할의 허용 목록 안에 반드시 있어야 한다 — 빌드 타임
검증을 넣는다.★

### 나중에 2층을 DB 기반으로 옮기기 쉽게

조회를 함수 하나로 통일한다. 부르는 쪽(middleware·사이드바)은 그대로 두고
속만 갈아끼울 수 있다.

```ts
export function canAccessErpPage(admin: { role: string; can_manage_templates?: boolean | null }, pathname: string): boolean
```
(1단계 구현 시 `role` 단독에서 admin 객체로 변경 — 위 "1단계 완료" 절 참고)

### 작업 순서

```
1단계  1층 매트릭스 + 라우트 가드 + 착지 경로   ✅ 완료 (2026-09-04, 커밋 완료)
       designer 제외, 기존 5개 역할로만
2단계  ErpUserContext 신설                     ✅ 완료 (2026-09-04, 커밋 완료)
3단계  admins 컬럼 2개 + staff 역할 신설         ← 다음
4단계  관리자 관리에 체크박스 UI  ★SQL 없이 화면에서 켜고 끄심★
5단계  CS·공지·템플릿 API 게이트
6단계  디자이너 계정 발급 (staff + 템플릿 업로드)
7단계  ERP 홈 대시보드 (역할별 카드) — 권팀장 요청의 근본 해법
8단계  권팀장 5·7번 미처리 이름 정리
```

4단계가 끝나면 HANDOFF의 묵은 과제 **"템플릿 담당자를 바꾸려면 SQL을
직접 쳐야 한다"**도 함께 사라진다.

### 권팀장 요청과의 연결

- **1번**(식단 관리 하단으로) — 증상이었다. 진짜 원인은 **`/erp` 홈이
  없어서** 로그인하면 남의 업무 화면(`/erp/diet`)에 떨어지는 것.
  `app/erp/page.tsx`가 `redirect('/erp/diet')` 한 줄뿐이다.
  1층 가드가 붙으면 각자에게 안 보이는 그룹이 사라져 순서 문제가
  대부분 풀리고, 7단계 대시보드가 나머지를 푼다
- **4번**(접근 권한) — 1단계 그 자체
- **5·7번** — ✅ 완료(`099f032`). 아래 CS 섹션 참조.
  버그가 아니라 '미처리'라는 이름이 두 개인 것이었다.
  `inquiries/page.tsx:519` 통계 카드 '미처리'(`statFilter='pending'`,
  상태가 대기중) vs `:567` '미처리만'(`unreadOnly`, 안 읽음).
  ★통계 카드 이름은 건드리지 않는다★ — `InquiryStatus` 용어 체계는
  6번·10번에서 통째로 다시 설계하므로 지금 바꾸면 두 번 바꾸게 된다.
  `unreadOnly` 쪽만 `● 새 메시지만`으로 바꾼다

### 참고 — 조사로 확인한 사실

- 모든 ERP 페이지가 `'use client'`라 페이지 안에서 서버 가드를 걸 수 없다.
  `middleware.ts`가 이미 `/erp/(.*)`를 매칭하고 Supabase 조회도 하고
  있으므로 **middleware에 역할 가드를 추가하는 것이 맞다**
- `MANAGER_CREATABLE_ROLES`가 `Object.values(ROLES)`에서 파생된다.
  `ROLES`에 새 역할을 넣으면 **매니저가 그 계정을 만들 수 있게 자동으로
  따라온다.** `staff` 신설 시 의도한 것인지 확인할 것(권팀장이 디자이너·
  CS직원 계정을 만드는 건 실무상 자연스러움)
- 메뉴에 없는 숨은 페이지 3개 — `/erp/upload` `/erp/history` `/erp/my-page`.
  앞의 둘은 `diet/page.tsx:728,1009`, `review/page.tsx:2145`,
  `history/page.tsx:99`에서 링크로 진입한다

---

## ✅ 커밋2 기준폼 첫인상 완료 (2026-09-02, `6de9bb5`)

### 적용한 것 — `pptx-server/_commit2_form_polish.py` (레포에 커밋함)

서식만 바꾸는 일회성 스크립트다. ★좌표를 하드코딩하지 않고 "지금 어떤
서식인가"를 보고 대상을 고른다★ — 주차마다 공휴일·해당없음 서식 표본이
섞여 있어 좌표로 찍으면 표본까지 덮어버린다.

| 항목 | 개소 |
|---|---|
| 반찬5 10행 `7pt/FFBDBDBD` → `9pt/FF212121` | 21 |
| A열 행 라벨 8pt → 9pt | 105 |
| 2~5주차 2행 높이 18.0 → 36.75 | 4 |
| D2 안내문 8pt → 10pt | 5 |
| A25 중복 문구 `■ 반찬5=없으면 빈칸` 삭제 | 5 |
| 범례 A25·A26 `7pt/FF616161` → `9pt/FF424242` | 5 |
| 12·16·21행 `customHeight` 해제 → 자동 높이 | 15 |
| `defaultRowHeight` 15 → 24 | 5 |
| 인쇄 `fitToWidth=1` / `fitToHeight=0` | 5 |

`defaultRowHeight`를 24로 올린 이유: 높이 고정만 풀면 **내용이 없는 빈
폼에서 15로 줄어 납작해진다**. 자동 높이의 출발점을 올려 "빈 폼은
넉넉하게, 내용이 길어지면 알아서 늘어나게" 두 가지를 함께 잡았다.
1~26행이 전부 명시 높이를 갖고 있어 고정을 푼 3개 행 외에는 영향이 없다.

### ★반찬5 21개소 — 커밋①의 잔재였다★

`3afda23`이 "없으면 빈칸" **문구만 지우고 서식은 남겼다.** 그 칸들이
`7pt` + 연회색 `FFBDBDBD`로 남아, 6월 실데이터 3주차 B10의
`유기농김자반`이 실제로 작고 흐리게 들어가 있었다. 조건 검색으로 잡힌
개수가 **정확히 21개소** — 커밋① 기록의 "21개소"와 일치해 인과가 확증됐다.

★교훈 — 안내 문구를 제거할 때는 폰트 크기·색도 함께 되돌린다.★
문구만 지우면 그 칸은 계속 죽어 있다.

### 검증 (3중)

1. **컨테이너 before/after 전수 대조** — 값 변경 5건(A25만), 폰트 141셀
   (21+105+5+10, 예상치와 정확히 일치), 채우기·테두리·줄바꿈 **0건**,
   열 폭·시트 구성 동일
2. **`gen_form.py 2026 10` 전파** — 운영일 22칸 전부 반영, 해당없음·
   공휴일 서식 표본 보존, 헤더 형식(`목  10/1` 공백 2칸) 회귀 없음.
   `build_blank_form`은 A1과 B~F열 3~24행만 손대므로 2행·25·26행·A열·
   열 폭·시트 숨김은 기준폼 값이 그대로 전파된다
3. ★**Excel 실물**★ — 1주차 16행 42자
   `[럭스,성북E,송파E,강동E-우리밀카스테라①②⑥,멸균우유②]`가 자동으로
   늘어나 온전히 표시됨. 우려했던
   `sheetFormatPr@customHeight="1"`이 행별 자동 높이를 막지 않는 것을
   실물로 확인했다 → 45.0 고정안은 폐기

### ★정정 — HANDOFF 전제 2건이 실측과 달랐다★

**정정① 열 폭은 B만 28.0이 아니라 B~F 전부 28.0이다.**

```xml
<col width="9"  customWidth="1" min="1" max="1"/>
<col width="28" customWidth="1" min="2" max="6"/>   ← B·C·D·E·F 전부
```

openpyxl `column_dimensions`가 `min~max` 묶음을 **첫 글자 키 하나로만**
노출해서 "C~F는 값이 없다 = 기본값"으로 보였던 것이다.
★열 폭은 반드시 XML `<cols>`로 확인한다.★
→ "C~F와 통일해서 13.0"은 성립하지 않아 **열 폭 변경은 하지 않았다.**

**정정② 글씨를 키워도 인쇄가 넘치지 않는다. 축소된다.**

5개 주차 시트 모두 `<pageSetUpPr fitToPage="1"/>`이고 fitToWidth/Height가
미지정이라 Excel 기본값 **가로 1장 × 세로 1장**이 적용된다. 즉 세로로
길어질수록 넘치는 게 아니라 전체가 축소 인쇄된다. 그래서
`fitToHeight=0`(세로 제한 해제)으로 바꿔 세로 축소를 없앴다.

가로 총폭이 149자폭이라 **인쇄 글씨를 키우는 진짜 지렛대는 폰트가 아니라
열 폭**이다. 다만 기준폼은 영양사의 **화면 입력용**이고(최종 인쇄물은
PPTX·PDF), 열을 좁히면 원별특이사항이 6줄로 부풀어 한 주가 한 화면에
안 들어온다. 그래서 열 폭은 건드리지 않았다.

### ★신규 미해결 — 생성 빈 폼에서 생일간식 칸이 8pt 연회색★

18행은 `8pt/FFBDBDBD/'—'`(비우는 칸)와 `9pt/FF212121`(생일주 금요일
입력 칸)의 **의도된 2상태 체계**다. 기준폼 1·2·3주차 F18은 이미 올바르다.
(작업 초기에 "18행도 9pt로 올린다"고 판단했으나 전수 스캔 후 철회했다)

문제는 `gen_form.py`가 B열(월요일)을 표본으로 F열까지 덮어쓴다는 것이다
— 모듈 docstring에도 "18행 상태(회색 '—')를 그대로 적용해 중립으로
리셋"이라 명시돼 있다. 그래서 **생성분에서는 생일주 금요일도 `8pt/'—'`가
되고, 영양사가 '—'를 지우고 입력하면 8pt 연회색으로 들어간다.**
6월에만 정상이었던 건 기준폼 F열 원본이 살아 있었기 때문이다.

→ 기준폼 서식이 아니라 `gen_form.py` 설계 이슈. 해결 방향 미정
  (F열만 별도 템플릿? 18행 색을 진하게? 생일주 판정 자체가 코드에 없음).

### 이번에 일부러 하지 않은 것

- **열 폭 변경** — 정정①로 명분 소멸. 화면 입력이 주 용도라 28.0 유지
- **18행 생일간식 서식** — 의도된 2상태 체계라 건드리면 안 됨
- **참조표 시트 숨김** — ★방침을 뒤집었다.★ `upload/route.ts:251`이
  원명 검증 실패 시 "원별 참조표에서 정확한 원명 확인 바랍니다"로
  안내하는데 숨기면 그 안내가 거짓말이 된다. 게다가 ⑧(원명 별칭)에서
  참조표에 별칭 열을 신설할 예정이라 **앞으로 더 자주 봐야 할 시트**다
- **가이드 시트 숨김** — 커밋3(가이드 두 층 분리)과 한 몸. 5줄 요약이
  1주차 시트에 들어가기 전에 숨기면 규칙을 볼 곳이 사라진다
- **예외규칙 행(13·17·22) 높이** — 6월 실데이터 최장 13자라 현재
  높이로 충분. 길어지면 원별특이사항과 같은 방식으로 푼다
- **A25·A26 행 높이(19.5)** — 9pt 승격 후 잘리는지 실물로 보고 정하려
  했고, 실물에서 문제없어 그대로 뒀다

### 커밋 메시지 규칙 (신설)

★`Co-Authored-By` / `Claude-Session` 등 트레일러를 붙이지 않는다.★
제목 한 줄(한국어)만 쓴다. 기존 커밋 이력과 형식을 맞추기 위함이다.
`6de9bb5`에 들어갔으나 이미 push되어 고치지 않았다(master 강제 push를
피함).

---

## 권팀장 요청 접수 + 우선순위 재편 (2026-09-01)

### 지금 순위

권팀장 「관리자 페이지 요청사항 (1차-260901)」 접수로 재편했다.
★1:1 게시판(CS 관리)을 먼저 실사용하므로 v6 기준폼보다 앞선다.★

```
0) v6 반찬5 + 도시락 예시            ✅ 완료 (3afda23)
1) 커밋2 기준폼 첫인상 정리           ✅ 완료 (6de9bb5)
2) 권팀장 5·7번 미처리 이름 정리       ✅ 완료 (099f032)
3) ★권한 1층 + 라우트 가드★           ✅ 완료 (커밋 완료, 구멍 막기)
   권팀장 1·4번이 여기에 흡수됨
3) CS 홈 + 9번 좌우 배치 + 내부 메모   ← 다음
4) 권팀장 2·3번 공지 발송 대상
5) 권팀장 6번 + 10번 (권팀장 답 받고)
6) 권팀장 8번 고객사 정보
```

### 권팀장 요청 10건 — 조사 완료, 착수 전

**1번** `lib/erp-nav.ts:16~55` 그룹 순서 변경. 5분

**4번** `ErpLoginForm.tsx:39,87` 두 곳 + `ErpSidebar.tsx:64`
★역할별 분기 결정★ — `manager`/`super_admin`/`director` → `/erp/inquiries`,
`nutritionist_*` → `/erp/diet`

**5·7번** ★버그가 아니다 — '미처리'라는 이름이 두 개다★
- 통계 카드 '미처리' = `statFilter='pending'` (상태가 대기중)
- 검색창 옆 '미처리만' = `unreadOnly` (`unread_count_admin>0`, 안 읽음)
- 5번 원인: 카드가 토글(`page.tsx:529`)인데 미처리 0건이라 누르면 목록이 빈다
- 조치: 0건 카드 `disabled`, 빈 목록 메시지 분기, 필터 표시 칩,
  '미처리만' → '● 새 메시지만' ★이름 변경(삭제 아님)★

**6번** `InquiryStatus` = `'pending|in_progress|resolved|closed'`
(`lib/types.ts:1,84~94`). ★10번과 얽혀 있다 — 반드시 함께 설계★

**8번** ★2026-06-05에 이미 논의된 숙제★ — "원 기본정보는 홈페이지 관리,
운영 프로파일은 운영 관리로 분리"가 결론이었으나 6/12에 반대로(원 정보
관리 메뉴 제거) 실행됐다. `BranchProfileForm.tsx` 섹션1(기본정보)만
성격이 다르다. 권팀장 요청과 같은 방향.
※ '고객사 NO'가 `sort_order`와 같은지 ★권팀장 확인 필요★

**9번** 고객사 좌 / 키즈밀 우.
★내부 메모는 가운데 자물쇠 스타일로 분리★ — 우리 쪽에 섞이면 실수로
발송할 위험이 있다

**10번** ★담당자 지정은 이미 구현돼 있다★
(`assigned_admin_id`, `InquiryDetailPanel.tsx:1107` 저장 · `1551` 드롭다운).
권팀장의 진짜 요구는 **"담당자 여러 명이 각자 답변하고 전원 완료 시
문의 완료"**다.
- 연결 테이블 `inquiry_assignees` 필요(담당자별 완료여부 포함)
- '완료'가 개인 완료 / 문의 완료 두 뜻이 된다 → 6번과 함께 설계
- ★`assigned_admin_id` 코드가 3벌 중복★ (2026-09-01 실측 확인)
  ```
  app/board/admin/inquiries/[id]/page.tsx:253
  app/erp/(protected)/inquiries/[id]/page.tsx:253
  app/erp/(protected)/inquiries/components/InquiryDetailPanel.tsx:1107
  ```
  하나만 고치면 나머지가 덮어쓴다. 3~4일 규모

### 신규 발견 — 미해결

**★고객사 화면의 내부 메모 차단이 클라이언트 필터다★** (2026-09-01 실측)
`app/board/(customer)/inquiries/page.tsx:81,116` — `is_internal`을 포함해
메시지를 **전부 받아온 뒤** 화면에서 거른다. 내부 메모가 이미 고객사
브라우저까지 도달해 있다.
→ 서버 필터로 바꿔야 한다. `messages` RLS 정책 존재 여부도 미확인.

**관리자 대화 검색에서 내부 메모가 빠진다** (`page.tsx:322`
`.eq('is_internal',false)`)
→ ★유대표님 결정: 검색에는 포함하되 '🔒 내부 메모' 표시를 붙인다★

**업로드 파서에 '없으면 빈칸' 방어 필터가 없다** (`route.ts:174,183`)
폼에서는 지웠으나 구버전 폼을 쓰거나 직접 타이핑하면 여전히 DB로 들어온다.
→ 영양사 실사용 후 어떤 실수가 나오는지 보고 설계한다.

**`gen_form.py`가 Windows 콘솔(cp949)에서 크래시한다**
마지막 안내문의 em-dash를 출력하다 `UnicodeEncodeError`.
→ `PYTHONIOENCODING=utf-8` 필요. ★파일 생성 자체는 성공한 뒤에 죽는다★

**가이드 시트에 도시락 작성법 설명이 없다** (A69 한 줄뿐) → 커밋3에서 보강

### 권팀장 회신 발송함 (2026-09-01)

확인 요청 4건: ①담당자 미응답 시 처리 ②담당자별 답변 방식
③배정 권한 범위 ④'고객사 NO' 정의

### 권팀장 답변 수령 (2026-09-01)

**1) 담당자 미응답** 휴가는 무관하다. 퇴사·보직변경·장기결근에 대비해야
한다.
→ ★타인이 배정을 취소하거나 강제 완료할 수 있어야 한다★

**2) 담당자별 답변** 카톡 그룹채팅처럼 이름을 작게 노출한다.
★고객사 화면에도 똑같이 적용★(원장 / 교수부장 / A반 담임).
→ ★9번 범위가 커졌다★ — 고객사측 발신자 구분을 지금 저장하고 있는지
   ★확인 필요★. 안 하고 있으면 스키마 작업이 붙는다

**3) 배정 권한** 누구나 가능

**4) 고객사 NO** 키즈밀 내부 번호가 존재한다.
「'26년도 신학기 운영관련 자료」 시트 순서 = 고객사 NO.
→ ★그 파일을 받아야 8번 착수 가능★ (현재 대기)

**※ 6번 '완료' 용어 구분은 답이 없다 — 재확인 필요.**
10번(다중 담당자)과 한 몸이라 이 답이 없으면 5순위를 설계할 수 없다.

### 「'26년도 신학기 운영관련 자료」 시트 실물 확인 (2026-09-01, 구글시트)

**★고객사 NO 체계 확인★**
- 위탁: `1`~`4` (로티스, 대치잉글리쉬파크, 송파MB, KIS)
- CK: `C-1` ~ `C-50` 형식
- ★현 시스템의 `sort_order`(1~49 단순번호)와는 다른 별도 체계다★
- ★CK 50개 vs 시스템 49개 — 차이 나는 원을 확인해야 한다★
  (C-42 광교알티오라 / C-48 (대치)잉글리쉬파크 / C-50 송파크레오가
  초록 표시 = 신규로 추정)

**★원명 표기 차이 실물 확인 — ⑧ 별칭의 근거★**
```
시트    찰리1호점 / 찰리2호점   강남 라이즈(띄어쓰기)   광교slp(소문자)
시스템  찰리1호   / 찰리2호                            광교SLP
```

기타 탭: `2027 계약·설명회 일정` / `신규원 기초정보` / `식단가` /
`식대 차감 비율` / `식대청구서 번호` / `노선표(확정)`
(`2026 계약·설명회 일정` 탭에는 연락처가 없다)

### `CK 고객사 현황표(2026년)` 탭 확인 완료 (2026-09-01)

실제 컬럼:
```
NO | 업장명 | 업장주소 | 고객사 연락처 | 비상연락망 | 계약만료일
   | 간식시간 | 중식시간 | 오후간식 | 배송시간(최대) | 이메일 | 비고
```
기준일이 **2026년 03월**이다 — 반년 전이라 ★최신성 확인 필요★.

**★★핵심 발견 — 원당 이메일이 여러 개이고 용도가 다르다★★**
```
노원ECC 4개
  kbm5490@naver.com       담당, 식단표, 식단사진
  tjsalsdl02@naver.com    식단사진
  vitasw@naver.com        식단사진, 계약관련
  eccjunggye@ybmsisa.com  세금계산서
```
- ★현재 구조(원당 이메일 1개 가정)와 충돌한다★
- `branch_contacts` 테이블 신설 필요 (원ID · 이메일 · 용도 · 담당자명)
- ★이메일 배포 기능의 전제 조건★ — "식단표 받는 주소"를 여기서 골라야
  한다. 즉 ★8번은 단순 조회 페이지가 아니라 배포의 선행 작업★이다

※ 간식시간·중식시간·배송시간은 **운영 정보**라 원 프로파일과 일부
  겹친다. 권팀장은 "성격이 다르다"고 했으나 ★경계 재검토 필요★
※ ★8번 규모 재산정★ — 기존 2~3일에서, 이메일 다중 관리를 포함하면 늘어난다
※ `위탁 고객사 현황표(2026년)` 탭(위탁 4개 원)은 아직 안 봤다

---


## 9월 49개 생성 버그③ — 공휴일 가림 그림 제거가 추석 안내를 지움 (2026-08-31 오후 2차)

### 발견 경위 — 실배포본 vs 생성분 대조

버그①②(아래 섹션) 수정 후 3차 재생성을 돌려 실물로 대조했다. 버그①②는
완전히 잡혔다:
```
1주차 [0,2]='1 / 현미밥 / 소고기육개장⑤⑥⑯ / …'  화~금 정상
2~5주차 중식 전부 정상
```
그런데 4주차(추석 연휴)에서 새 문제가 나왔다.

**실배포본 강동ECC**(영양사 수작업, 정답):
```
4주차 목·금 셀 = ''  ''            완전히 빈칸
그림 = 그림4 · 그림26 · ★그림31★    추석연휴 이미지 살아있음
  그림31 pos=(4675605, 6515405) size=(2685600 x 1785600)
```

**생성분 강남MB**(버그①② 수정 후 코드):
```
4주차 목·금 셀 = '24 / '  '25 / '   날짜만 찍힘
그림 = 그림4 · 그림26 · 그림167     ★그림31 없음★
```

GitHub Actions 로그에 제거 기록이 그대로 남아 있었다:
```
🗑 공휴일칸 가림 그림 제거: name='그림 31'
   pos=(4,673,066,6,525,565) size=(2,680,943x1,782,206)
```

`remove_holiday_cover_pics()`(`pptx_generator.py:793`, 호출부
`:1414`)가 디자이너가 추석 칸에 배치한 안내 이미지를 장식으로 오인해
제거한 것 — 47개 원(공휴일에 쉬는 원 전부) 식단표에서 "추석연휴" 안내가
사라지고 날짜 숫자만 남는 결과가 나왔다.

### ★6월 선거도장 ↔ 9월 추석안내, 정반대 의미★

이 함수의 원래 의도는 함수 docstring에 그대로 적혀 있었다 — "6월
지방선거일 선거 도장 그림" 제거. 6월엔 그 칸에도 급식이 있어서
날짜를 가리는 장식을 치워야 했다. 9월 추석은 정반대다 — 쉬는 날이라
안내 이미지가 그대로 남아 있어야 한다. 같은 함수, 같은 메커니즘
(공휴일 셀 위에 뜬 그림 제거)인데 6월과 9월에서 "옳은 동작"이 정반대다.

### ★그림 모양으로 의미를 판정하지 않는다 — 원칙 재확인★

8/28 세션에서 이미 한 번 확인된 원칙이 이번에 또 그대로 적용됐다 —
그때는 9월 추석 이미지를 방학 이미지로 오판했던 사례였고, 이번엔
"장식(치워야 함)"과 "안내(남겨야 함)"를 그림 좌표·모양만으로 구분하려다
같은 함정에 걸렸다. **그림 모양·좌표만으로는 의미를 판정할 수 없다** —
판정 기준은 항상 "그 원이 그날 실제로 무엇을 하는가"(운영 여부, 방학
여부 등 도메인 사실)여야 한다. 이번 수정도 그림 좌표 판정(조건 2~4)은
그대로 두고, 앞에 "그 원이 공휴일에도 운영하는가"(`_HOLIDAY_OPERATING`)
조건 하나를 추가하는 방식으로 풀었다 — 그림 판정 자체를 더 정교하게
만들려 하지 않았다.

### 수정

`remove_holiday_cover_pics()`에 `branch_name` 매개변수를 추가해 함수
맨 앞에서 `branch_name not in _HOLIDAY_OPERATING`이면 조기 반환하도록
했다. `_render_slide()`가 이미 `cfg`에서 `branch_name`을 뽑아 셀
단위로 같은 판정을 함수 내부에서 하는 기존 관례를 따랐다(호출부에 if를
씌우지 않음). `_HOLIDAY_OPERATING`은 새로 만들지 않고 기존 상수
(`{'덕양P', '광교SLP'}`)를 그대로 재사용했다.

### ★미확인으로 남김 — 쉬는 원 셀의 날짜 숫자 표시 여부

쉬는 원의 4주차 목·금 셀엔 지금도 `'24 / '`, `'25 / '`처럼 날짜가
찍힌다(실배포본은 완전 빈칸). 이번엔 건드리지 않았다 — 그림이 그
칸을 덮으므로 화면 결과는 같아질 수 있어, 그림 유지 수정 후 실물에서
날짜가 실제로 보이는지 확인한 뒤 다음에 판단하기로 했다.

### ★미해결로 남김 — MATERIAL_BOX ↔ ALLERGY_BOX 겹침 경고

`_check_box_overlap`이 내는 "겹침 감지! MATERIAL_BOX ↔ ALLERGY_BOX"
경고가 거의 전 원에서 뜨고 있다. 이번 세션(과 그 이전 세션도) 범위
밖으로 미뤄뒀다 — 조사조차 하지 않았다. 다음 세션 우선순위 후보.

---

## 9월 실배포본 대조 + 미해결·개선 목록 (2026-08-31 오후 2차)

### 버그③ 실물 검증 통과 (4차 재생성)

버그③ 수정(`984167a`) 후 4차 재생성으로 `_HOLIDAY_OPERATING` 분기가
정확히 작동하는지 실측했다:
```
쉬는 원   강동E 그림4·26·★31★·167 / 엘란 그림203·230·★233★·172
          송파랜 그림4·26·★31★·167    ← 추석 안내 이미지 유지됨
운영 원   덕양P 그림203·230·172 (233 제거) / 광교SLP 그림4·26·167 (31 제거)
```
쉬는 원은 추석 안내 이미지가 남고, 공휴일에도 운영하는 원(`덕양P`·
`광교SLP`)만 가림 그림이 제거된다 — 의도대로다.

### 9월 실배포본 대조 완료 — 오래 미뤄졌던 "정답 있는 검증"

44쌍을 대조했다. 40쌍이 22~23칸 차이, 4쌍은 슬라이드 구조 자체가
달라 비교 불가(아래 "슬라이드 구조 불일치 4건" 참고).
★차이의 실체는 "날짜 숫자" 하나뿐★이다:
```
생성분   '1\n현미밥\n소고기육개장⑤⑥⑯\n…'
실배포본 '현미밥\n소고기육개장⑤⑥⑯\n…'
```
메뉴·알러지·칼로리·원별대체는 한 글자도 다르지 않다(긴 메뉴명의
줄바꿈 위치 차이는 자동 줄바꿈이라 차이로 안 침). 즉 생성분은 날짜를
셀 안에 넣고, 실배포본은 표 밖에 둔다. ★어느 쪽이 맞는지는
PowerPoint 실물로 봐야 판단 가능 — 미확인★(다음 세션 1순위).

### PDF 개수 오해 정정

49개 중 PDF가 17개만 생성된 것을 버그로 의심했으나 아니었다.
`app_actions.py:470`의 `if not lo_missing and file_fmt.upper() != 'PPT'`
— 원별 `file_fmt` 설정에 따라 PPT 원은 애초에 변환하지 않는 설계다.

### 미해결로 남기는 것

**★버그④ — 송파랜 PDF 손상★** PDF 대상 원인데 생성된 파일이 열리지
않는다("이 파일을 열 수 없습니다"). 로그는 성공으로 집계 — 변환 후
유효성 검사가 없다. 배포 대상이 PDF이므로 그 원만 조용히 식단표를
못 받게 된다.

**★슬라이드 구조 불일치 4건★** 목동P 생성180칸 vs 실제90 / 송파P 60
vs 90 / 수지P 90 vs 60 / 수지P별관 90 vs 60. 원별 슬라이드 플랜
(`_SLIDE_PLAN`)과 실제 운영이 어긋난 것으로 보인다. 미조사.

**MATERIAL_BOX ↔ ALLERGY_BOX 겹침 경고** (위 섹션과 동일 건) 거의
전 원에서 발생(엘란은 4슬라이드 전부). 실물로 겹쳐 보이는지 미확인.

**★도시락 3건 미연동★** 9/22 동탄P `<추석이벤트>` / 9/23 운정P·수지P·
수지P별관 `<도시락>`. 도시락 시트를 생성기가 읽지 않는다(검증은 읽음).
★유대표님 결정: 시트를 없애고 ③엑셀 업로드 단계의 필수 팝업으로
이관한다. 건너뛸 수 없게, "없음"도 확정하게, 확인자·일시를 기록한다.
무엇을 물을지는 위 도시락 3건의 형태를 근거로 설계할 것.★

### v6 기준폼 개선 목록 (유대표님 지시)

✅ **① 반찬5 '없으면 빈칸' — 완료** (`3afda23`)
   주차 시트 10행 21개소(20군데인 줄 알았으나 실측 21개) + 도시락 시트
   6월 예시 3행 + A2 안내문 교체. 서식 차이 0건으로 검증했다.
   ★`✏️ 입력 가이드` C14~C16의 같은 문구 3개는 남겼다★ — 파서가 읽지
   않는 시트(route.ts:520~544)이고 영양사용 설명문이라 지우면 손해다.
★② 원별 참조표 시트 숨김 — 방침 변경, 숨기지 않는다★
   `upload/route.ts:251`이 원명 검증 실패 시 "원별 참조표에서 정확한
   원명 확인 바랍니다"로 안내하고, ⑧에서 참조표에 별칭 열을 신설할
   예정이라 **더 자주 봐야 할 시트**다. 커밋3에서 **가이드만** 숨긴다.
   (숨김 시트를 openpyxl/SheetJS 둘 다 정상 파싱하는 것은 실측 확인됨 —
    기술적으로 가능하지만 하지 않기로 한 것)
③ 입력 가이드 두 층 분리 — 자주 쓰는 규칙 5줄은 1주차 시트 하단,
   상세는 뒤로. 도시락 작성법 보강(아래 신규 발견)과 함께 커밋3
④ 도시락 시트 삭제(팝업 이관과 함께) — ★단독 삭제 불가★.
   `REQUIRED_SHEETS`(route.ts:20~23)에 들어 있어 시트를 지우면 업로드
   자체가 전부 거부된다. 함께 고칠 곳 6개소 확인됨. 팝업 이관과 한 몸
✅ **⑤ 원별특이사항 행 높이 자동 — 완료** (`6de9bb5`)
   ★wrap_text는 이미 True였다. 진짜 원인은 행 높이 고정★이었다.
   12·16·21행 `customHeight` 해제 + `defaultRowHeight` 15→24.
   Excel 실물에서 42자가 자동으로 늘어나는 것을 확인했다.
   ※ 예외규칙 행(13·17·22)은 이번 범위에서 제외했다 — 6월 실데이터
     최장이 13자라 현재 높이로 충분하다. 길어지면 같은 방식으로 푼다
⑥ B(생일) 기호 — ★결론 변경★. 유대표님 확인 결과 B는 "생일이어서
   제공"이라는 **사유 표시**다. 6월 실데이터로 검증해 보니 v6는 이미
   🎂헤더 + 생일간식 행으로 같은 정보를 담고 있다. 따라서 할 일은
   "B 문서화"가 아니라 ★"CK에서 옮길 때 B는 떼라"를 가이드에 명시★
   하는 것이다. B가 대괄호 밖에 있어 그대로 두면 48개 원 메뉴에
   'B'가 그대로 인쇄된다(실측 확인)
⑦ 교.추 메모 행 신설 — ★확인 전 착수 금지★. `snack_teacher_extra`가
   이미 존재한다(DB + board PDF). CK의 '교.추'와 같은 개념인지
   ★배서영 영양사 확인 필요★
⑧ 원명 별칭 허용 — 매핑이 아예 없다. `VALID_BRANCHES`가
   route.ts:8~18에 하드코딩돼 있고 참조표 D열과 이중관리 중이다.
   참조표에 '별칭' 열을 신설하는 방식을 검토한다.
   ★CK 표기 외에 운영자료 시트 표기도 다르다는 것이 실물로 확인됐다★
   — 위 "「'26년도 신학기 운영관련 자료」 시트 실물 확인" 참고
⑨ 검증 오류 위치 표기 통일 — 도시락 시트만 "6행"으로 나와 어디인지
   알 수 없다

### 운영·UX 문제 (오늘 실제로 겪은 것)

★⑩ 연월이 화면마다 8월(전월)로 초기화된다★ 템플릿 등록·엑셀 업로드·
   식단 검토·워크플로 전부. 오늘만 대여섯 번 다시 골랐다. 8월로 둔
   채 올리면 엉뚱한 달에 저장된다.
★⑪ 생성 진행률 모수가 49/53으로 흔들리고, 잘못 잠기면 빠져나올 수
   없다★ 완료됐는데 "진행 중"으로 남아 ④ 버튼이 잠긴다. 새로고침·
   ▷버튼 모두 안 됨. "테스트 초기화"는 8월로 되돌아가 쓸 수 없다.
   오늘 세 번 발생. 타임아웃 해제나 강제 재시도가 필요하다.
⑫ ④ PPTX 생성 실패 시 화면에 이유가 안 뜬다(500이 콘솔에만). ①양식
   준비는 alert이라도 뜨는데 ④는 무음이라 일관성도 없다.
⑬ `.env.local`에 `GITHUB_PAT`이 없어 로컬에서 워크플로 테스트가
   불가능하다. 오늘 두 번 걸렸다(①양식 준비, ④PPTX 생성).
⑭ ④-10 `diet_review_items` 자동 INSERT 미구현 — 검토 화면 월 선택에
   9월이 아예 안 나온다. 생성은 되는데 검토·승인·배포로 넘어갈 수
   없다.
⑮ ①양식 준비 카드에 "누르면 공휴일·방학 확인부터 시작합니다" 안내가
   없어 처음 쓰는 사람은 즉시 실행되는 줄 알고 망설인다.
★⑯ ④ 생성 전 확인 팝업 신설 — 활성 양식·엑셀·대상 원수·공휴일을
   보여주고 ★활성 양식이 없으면 진행을 차단★한다. 오늘 활성화를
   잊어 49개가 6월 양식으로 생성됐고 아무 경고도 없었다.★

### 다음 세션 착수 순서 → ★폐기(2026-09-01 재편)★

이 순서는 권팀장 요청 접수로 갈아엎였다. 현재 순위는 맨 위
"권팀장 요청 접수 + 우선순위 재편 (2026-09-01)" 섹션을 볼 것.

---

## 9월 49개 생성 버그 — 1일이 월요일이 아닌 달 (2026-08-31 오후)

### 증상

9월 식단표 49개가 생성됐으나 ①1주차가 통째로 비어 있고 ②2~5주차도
★중식만★ 전부 빈칸(간식은 정상)이었다. 에러 없음, GitHub Actions
성공. ★조용히 잘못된 결과★ — 화면 어디에도 실패로 안 뜨고 그냥 다른
결과가 나왔다.

### 실측 — 데이터는 완전히 정상이었다

DB `weekly_menus.menu_data`의 2주차 첫날 lunch를 직접 확인:
```json
{"bap":"흰쌀밥","guk":"콩나물국⑤","banchan1":"달콤치킨강정②⑤⑥⑩⑮⑯⑱",
 "banchan2":"파프리카야채무침⑫","banchan3":"유기농김자반",
 "banchan4":"배추김치⑨","banchan5":"",
 "exception":"[잉파-파프리카야채무침X]","nutrition":"449Kcal(탄56단21지20)"}
```
`day` 뼈대(`{"date":"2026-09-07","is_holiday":false,"is_birthday":false}`)도
정상. `build_lunch_lines()`를 이 데이터로 직접 호출해 7줄 정상 출력까지
확인했다 — **데이터·렌더링 함수 둘 다 무죄**, 문제는 그 앞뒤 두 곳:
업로드 파서(1주차 소실)와 PPTX 생성기(중식 템플릿 추출 실패)였다.

### ★공통 뿌리★ — "1일이 월요일이 아닌 달에서 B열(월요일)이 비면 시스템이 깨진다"

두 버그 다 같은 가정 위에 있었다 — "월~금 5칸 중 첫 칸(월요일, B열)엔
항상 데이터가 있다". 1일이 월요일이 아닌 달은 그 주(1주차 엑셀 행 /
디자이너 양식 표 전체)의 월요일 칸이 실제로 비어 있어 이 가정이
깨진다.

```
2026~2027 24개월 중 14개월이 "1일이 월요일이 아닌 달":
  2026: 1·4·5·7·9·10·12월
  2027: 1·4·6·7·9·10·12월
★다음 달인 10월도 해당된다★(1일=목요일).
```
6월(1일=월요일, offset=0)에서만 두 버그 다 우연히 정상 동작했던 것 —
그래서 지금까지 아무도 눈치채지 못했다.

### 버그 ① — 업로드 파서가 B열만 보고 주차 전체를 skip

`app/api/diet-automation/upload/route.ts` `parseWeekSheet()`(구 131~134행)
가 B열(월요일) 셀 하나만 보고 `'해당없음'`이면 그 주차 전체를
`is_skipped=true, days=[]`로 버렸다. 9월 1주차는 월=해당없음이지만
화~금 4일은 멀쩡히 있었는데도 통째로 날아갔다(DB 실측: 1주차
`is_skipped=true, days=[]` / 2~5주차는 5·5·5·3일 정상).

수정(`674c48a`): 5칸(월~금) **전부** 비었거나 '해당없음'일 때만 skip.
한 칸이라도 날짜가 있으면 살린다.

### 버그 ② — 생성기가 col1(월요일)에서만 중식 템플릿을 추출

`pptx-server/pptx_generator.py` `_init_table_cells()`(구 231~247행)가
표의 col1(월요일) 셀에서만 lunch/snack template을 추출했다. 9월 디자이너
양식 실측:
```
슬라이드1(15행) 행0 '1주'  col1 run수=0
                행1 '오전' col1 run수=0
                행2 '오후' col1 run수=0
                행3 '2주'  col1 run수=0
                행4 '오전' col1 run수=2  ← 여기서야 나옴
```
9월 1일이 화요일이라 디자이너가 B열을 비워둔 채로 표를 만들었다.
중식 행(1주~5주)의 col1은 전부 run수 0 → `lunch_tmpl`이 끝내 `None` →
중식 셀이 초기화되지 않아 전 주차 중식이 빈칸으로 남았다. 간식은
2주차(행4) col1에서 추출에 성공해 정상 출력됐다 — 그래서 "중식만"
빈칸이라는 특이한 증상이 나왔다.

수정(`daf16ea`): col1 고정 대신 데이터 열 전체(col1~n_cols-1)를
순회해 run이 있는 첫 셀에서 template을 추출. lunch_tmpl/snack_tmpl은
각각 독립적으로 찾는다. 9월 실물 표(slide1, 15행 6열)로 검증 —
기존 로직은 lunch_tmpl을 못 찾고(False), 수정 로직은 찾음(True).

### 회귀 테스트 (`d1ed36d`)

`pptx-server/test_upload_week_skip.py` — 지시된 3케이스 + 1일이
월~일 7가지 경우(2026-06/09/04/10/05/08/11) 전부 포함, 10건 전체 통과.
`pptx-server/test_init_table_cells.py` — 실제 `_init_table_cells()`를
호출(재구현 아님), col1~col5 어디에 데이터가 있어도 동작하는지 +
lunch가 비어도 snack은 독립적으로 찾는지 확인, 7건 전체 통과.

★두 테스트 모두 수정 전 코드(`daf16ea~1`)로 되돌려 실제로 실패하는
것을 확인한 뒤(진짜 회귀 테스트임을 검증) 원복하고 커밋했다 — 템플릿
추출 테스트는 7건 중 6건이 수정 전 코드에서 FAIL했다.

### ★미검증으로 남은 것 — 실제 49개 재생성 대조

코드 수정과 합성(synthetic) 테스트로는 검증했지만, **수정된 코드로
9월 49개를 실제로 재생성해 ①1주차가 채워지는지 ②중식이 전부 채워지는지
③9월 실배포본 48개와 대조되는지는 이번 세션에서 하지 못했다.** 이
파이프라인은 GitHub Actions(`app_actions.py`)를 통하고 pptx-server
쪽 코드 변경이라, 배포·재실행이 필요하다. 다음 세션 최우선.

---

## 이름표 부여 — A-2 전환 + 업로드/적용 화면 반영 (2026-08-31)

### ★정정 — 8/28 HANDOFF의 전제 2개가 틀렸다

`pptx-server/template_resolver.py:228 _prepare_template()`를 실물로
읽고 확인했다:
```
다운로드 → apply_template_names() → validate_template() → 사용
```
★생성 파이프라인에는 이름표 자동 부여가 이미 연결돼 있다★
(8/11 3단계 통합 — 아래 "이름표 자동부여 (발견① 해결)" 섹션 참고).

8/28 HANDOFF의 다음 두 문장은 틀렸다:
1. "template_namer.py가 업로드 흐름에 연결돼 있지 않다"
   → 업로드 흐름엔 맞는 말이지만, **생성 흐름**엔 이미 연결돼 있다.
   두 흐름을 혼동한 서술이었다.
2. "9월 양식을 활성화하면 MENU_TABLE을 못 찾아 49개 생성이 통째로
   실패한다" → 틀렸다. 생성 직전에 이름표가 붙는다.
   `pptx_generator.py:864 _get_table()`엔 "첫 has_table" 2순위
   폴백까지 있어 이름표가 없어도 통째 실패하지 않는다.
   → 진짜 실패 양상은 **조용한 6월 로컬 폴백**
   (`template_resolver.py:261`, 8/11 발견①과 같은 형태)이다 — 화면에
   "성공"으로도 "실패"로도 안 보이고 그냥 다른 달 양식이 쓰인다.

### A-1 폐기 · A-2 채택 사유

- **A-1(브라우저에서 zip을 다시 써서 이름표를 붙임)은 폐기.** 위 정정대로
  애초에 필요 없었다(생성 파이프라인이 이미 함). 설령 필요했더라도
  리스크가 컸다 — `_apply_template_names.py`(ElementTree 재작성)는
  `p:` 접두사 소실·네임스페이스 개명·수천 바이트 손실이 실측 확인된
  전례가 있고, JSZip으로 브라우저에서 XML을 다시 쓰는 것도 원리상
  같은 위험(zip 재압축·파일 순서 변경)을 안는다.
- **A-2(파일은 안 건드리고 "붙일 수 있는 양식인가"만 판정)를 채택.**
  목적은 조용한 6월 폴백을 활성화 버튼 누르기 전에 사람이 알아채게
  하는 것뿐 — 파일을 고칠 필요가 없으니 위 리스크 자체가 없다.
- **판정 규칙은 `pptx-server/template_namer.py`와 반드시 동일해야
  한다** — 코드가 두 언어(TS/Python)에 이중관리되므로,
  `lib/template-analysis.ts`의 `EXPECTED_TABLE_COLS`(=6)에
  `pptx-server/validate_template.py EXPECTED_COLS`와 값이 갈리면
  화면은 통과인데 생성 때 폴백된다는 경고 주석을 남겨뒀다.

### 커밋 2개

```
lib/template-analysis.ts에 판정 로직 신설
  checkNameabilityInSlide / checkTemplateNameability
세 화면(업로드·적용대기 카드·미리보기 모달) + DB 저장 연결
```

### 9월 양식 실측 판정 결과 (정답표 대조 완료)

`2026년_9월_식단표__양식_.pptx`(8,377,038바이트, 슬라이드 3장)를
Storage에서 내려받아(DB의 v3 행) `checkTemplateNameability`를 임시
tsx 스크립트로 직접 호출 — python-pptx 실제 부여 결과(정답표)와
**완전히 일치**했다:
```
slide1  canApply 4/4  표 15행 6열
slide2  canApply 4/4  표 10행 6열
slide3  canApply 4/4  표 5행 6열
경고 없음, ok = true
currentNames
  MENU_TABLE    "Google Shape;17;p2"  (3장 공통)
  ALLERGY_BOX   "Google Shape;19;p2"  (3장 공통)
  ORIGIN_BOX    "TextBox 169" / "TextBox 173" / "TextBox 194"
  MATERIAL_BOX  "TextBox 173" / "TextBox 174" / "TextBox 196"
```
검증 스크립트는 확인 후 삭제(커밋 안 함, 지시대로).

### ★미검증으로 남은 것 — UI 실물 확인

`npm run build` 통과 + `dev:fresh` 기동까지는 했다(정상 응답 확인).
9월 양식도 스크래치패드에 재다운로드해 두었다. 하지만 **이번 세션엔
Chrome 확장이 설치돼 있지 않아(사용자가 이번 세션에 설치를 보류)
브라우저 자동화로 화면을 직접 확인하지 못했다**:
- ①업로드 화면 — "이름표 자동 부여 가능 — 4개 전부" + 표 15/10/5행
  6열 표시 여부 **미확인**
- ③preview 모달 — 기존 9월 v3(nameability 필드 없는 레거시 행)를 열어
  "이름표 점검 정보 없음 (이 기능 도입 전 등록분)"이 깨지지 않고
  뜨는지 **미확인**
- ②pendingTemplate 카드 — 등록을 해야 보이므로 원래도 이번 세션
  범위 밖(지시사항에 명시)

코드·타입·빌드는 전부 통과했고 로직은 9월 실물 데이터로 대조
검증했지만, **화면에 실제로 그려지는 모습은 사람이 직접 확인해야
한다.** 다음 세션 최우선 — 또는 `/chrome`으로 확장을 켠 뒤 이어서
확인 가능.

### 다음 단계

1. 위 UI 실물 확인(①③, 가능하면 ②도 등록해서)
2. 활성화 게이트 신설 — 이번 세션 범위 밖으로 남겨둔 것. A-2는 "표시만"
   하고 막지 않으므로, `nameability.ok`가 false여도 지금은 활성화가
   가능한 상태다. 다음 단계에서 게이트할지·경고만 할지 결정 필요.
3. 9월 양식 활성화 → 49개 생성 → ★9월 실제 배포본 48개와 대조★
   (정답이 있는 검증. HANDOFF에 오래 남아 있던 "정답본 대조" 과제)
4. 2순위 — 엑셀 빈칸 역산 + 방학·공휴일 두 축 통합 설계(내용은 위
   "★다음 세션 최우선" 절의 "2순위" 문단 그대로 유효, 아직 미착수)

---

## 식단표 자동화 — 원본 양식 + 배포본 박스 구조 전수 실측 (2026-08-28)

디자이너 원본 1~9월 양식(9월은 오늘 오후 추가로 받음)과 7·8·9월 실제
배포본(50/57/48개)을 전수 실측했다. **조사만, 코드 변경 없음.**
★오후 실측에서 오전 기록의 오류 1건을 발견해 정정했다 — 아래
"배포본 3개월 전수 실측" 섹션의 "정정(오후)" 참고.

### 세 박스의 실제 구조 (실측 확정)

디자이너 6월 템플릿 48개 × 61개 슬라이드 전수:
- 원산지  = 최상위 TEXT_BOX (61/61)
- 원재료  = 최상위 TEXT_BOX (61/61)
- 알레르기 = ★그룹 안의 FREEFORM★ (61/61) — 텍스트박스가 아니라
  자유형 도형 + 아이콘이 그룹으로 묶여 있다. 8월에 "별도 도형으로
  안 잡힌다"고 기록된 원인이 이것이다. 미해결 질문 해소.
- 예외 2건은 목동POLY 슬라이드2·3(도형 3개짜리 부록)로 정상.

★★함정 — 그룹 안 도형의 left/top은 슬라이드 절대좌표가 아니다★★
실측: 그룹9 top=10,116,222 인데 자식 FREEFORM top=9,853,671.
자식 좌표는 그룹 내부 좌표계다. 알레르기를 다룰 때 자식 좌표를
슬라이드 좌표로 착각하면 엉뚱한 곳으로 간다. 반드시 그룹(최상위
조상)의 좌표를 쓸 것.

박스와 표 끝의 관계(6월 48개):
- 10행 22개: 표끝~알레르기 간격 편차 0 EMU (완전 일정)
- 15행 26개: 편차 88,429 EMU(≈0.25cm) — 디자이너 수작업 미세편차
- 알레르기는 표 끝 바로 아래에 붙는다. 빈칸으로 올라가지 않는다.
  6월은 5주차가 월·화뿐이라 하단 빈칸이 많은데도 그렇다.

★"박스를 빈칸으로 올려 붙인다"는 6/26 설계 개념은 실무에 존재하지
않는다. 8/11에 위치이동 로직을 제거한 판단이 옳았음이 재확인됐다.

### 디자이너 양식의 실제 구성

- 한 파일에 슬라이드 3장 = 주차가 아니라 ★행수별 변형★(15행/10행/5행)
- 방학 있는 달은 방학O·방학X 두 벌(8월이 그 예). 단 7월은 방학 그림이
  들어간 한 벌만 왔다 → 방학 없는 원은 영양사가 그림을 지웠다.
- 방학 그림 = 한 주(週)를 가로로 덮는 띠. 폭 ≈ 표 폭(월~금 5일 전체).
- ★방학 주차 위치는 디자이너가 그 달에 맞춰 배치해서 준다★
  실측: 7월 띠 top=8,318,736(마지막 주) / 8월 top=1,155,561(첫 주).
  코드가 주차를 계산할 필요 없다.
- 방학O가 방학X보다 박스가 약간 내려간다(10행 +50,800 / 5행 +40,640 EMU).
  15행은 동일. 이것도 디자이너가 잡아준 값이므로 손대지 않는다.

### 배포본 3개월 전수 실측

7월(50개): 월~금 전체 42 / ★화~금 6★ / 방학그림 삭제 2(SLP광교·송도POLY)
  화~금 6개 = 광명POLY·대치POLY매그넷·덕양POLY·송파POLY·위례POLY·중계POLY
8월(57개): 방학없음 54 / 월~금 3(SLP광교·잉글리쉬파크·찰리스빅레드하우스)
9월(48개): 방학 0(48개 전부 방학없음) — ★정정, 아래 "정정(오후)" 참고★

★정정 — HANDOFF의 "송도POLY 5주 목·금부터 방학" 기록은 사실과 다르다.
실측상 송도POLY 7월은 방학 그림이 아예 없다.
★SLP광교는 7월 방학없음 → 8월 방학. "학기 단위로 거의 고정"이라는
가정은 성립하지 않는다. 매달 재확인이 필요하다.
★원 구성도 달마다 바뀐다: 7월 50 / 8월 57 / 9월 48개.
8월에만 크레오 3개·특강 4개 등장. 행수도 3·5·7·10·13·15로 6종
(디자이너 양식은 15/10/5 3종뿐 → 영양사가 행을 가감해 쓴다).

★★정정(오후) — 9월 방학은 0개다★★
오전에 "9월(48개): 방학없음 47 / 수~금 1(알티오라 위례)"로 적었으나
틀렸다. 알티오라 위례의 그 그림은 방학 띠가 아니라 ★추석 그림★이다.
- 2026년 9월은 1일이 화요일이라 1주차 월요일이 빈칸이고,
  9/24(목)·25(금)이 추석 연휴다.
- 실측: 4주차·5주차 목·금 칸의 메뉴가 비어 있다(추석·월말).
- 그림 md5 9d976f5ed9 는 ★9월 디자이너 양식에 이미 들어 있다★
  (양식 L=3.12 폭=1.87일 = 목·금 2일치).
→ 9월 방학 원은 0개. 부분 방학 사례는 7월 6개(화~금)뿐이다.

★★정정(오후) — 그림 모양만으로 방학/장식/공휴일을 구분할 수 없다★★
띠·장식·공휴일 그림이 전부 가로로 긴 이미지라 좌표·크기 자동 판정은
신뢰할 수 없다. 9월 오판이 그 사례다. 판정하려면 md5를 그 달 양식의
방학 그림과 대조하거나 사람이 눈으로 봐야 한다.

★검증된 것(md5 대조 완료, 이 둘은 정확)★
- 7월 양식 띠 md5 4f85d1afb0 = 배포본 48개와 동일
- 8월 방학O 양식 띠 md5 c2c50eb016 = 배포본 3개(SLP광교·잉글리쉬파크·
  찰리스빅레드하우스)와 동일
→ 7월 48개(월~금 42, 화~금 6), 8월 3개(월~금)는 그대로 유효하다.

### 부분 방학 그림 처리 방식

7월 4일짜리 띠는 5일 그림과 ★동일 이미지★(md5 4f85d1afb0, 21894×5563px)를
srcRect로 잘라 쓴 것이다. 스케일(눌러 줄이기)이 아니라 크롭이다.
  강동ECC 5일  crop 없음
  광명POLY 4일  l=20005 (20.005%)
  덕양POLY 4일  l=19710
  중계POLY 4일  l=8758 + r=10960
→ 하루=20%인데 비율이 제각각. 영양사가 마우스로 대충 자른 결과다.
9월 알티오라 위례(당초 "수~금 방학"으로 오판)는 사실 ★추석 연휴
그림★이며, 양식(md5 9d976f5ed9)의 목·금 2일치 그림을 수·목·금
3일치로 늘려 쓴 것이다 — 방학 크롭 사례가 아니다(정정, 위 "배포본
3개월 전수 실측" 섹션의 "정정(오후)" 참고). 상세 분석은 아래 "원별
예외는 그림 조정으로 나타난다" 섹션 참고.

### 9월 양식 분석 — 빈칸 그림은 전부 디자이너 몫

9월 양식(슬라이드 3장 = 15/10/5행)에 이미 들어 있는 그림:
- md5 9747af716c  L=0.31 폭=0.93일 top=1,151,416
  → 1주차 월요일 한 칸. 9/1이 화요일이라 생긴 시작요일 빈칸을 덮는 그림
- md5 9d976f5ed9  L=3.12 폭=1.87일 top≈6,52만
  → 4주차 목·금. 추석 연휴 빈칸을 덮는 그림
7월 양식도 같다(md5 49fc585f92 금요일 한 칸 + 4f85d1afb0 방학 띠).
8월 방학O 양식도 같다(c2c50eb016 방학 띠 + f0407184ec 한 칸).

★★결론 — "빈칸에 그림 배치"는 코드가 할 일이 아니다★★
시작요일 빈칸·공휴일 빈칸·방학 빈칸 그림을 전부 디자이너가 그 달
양식에 넣어서 준다. 6/26에 설계했던 "코드가 빈칸을 찾아 박스·그림을
동적 배치" 개념은 실무에 존재하지 않으며, 8/11의 위치이동 로직 제거는
옳았다. **이로써 이 주제는 종결한다.**

### 원별 예외는 그림 조정으로 나타난다

알티오라 위례 9월: 양식의 추석 그림(목·금 1.87일)을 ★수·목·금 2.82일로
늘려서★ 썼다. 이 원만 9/23(수)도 쉬었다는 뜻이다.
7월 부분 방학 6개(광명POLY·대치POLY매그넷·덕양POLY·송파POLY·위례POLY·
중계POLY)도 같은 구조 — 양식의 5일 띠를 srcRect로 잘라 4일로 썼다.

★즉 원별 예외는 두 축 모두에서 발생한다★
  방학 축      양식 띠를 잘라 씀(7월 6개)
  공휴일 축    양식 그림을 늘려 씀(9월 알티오라 위례)
어제 만든 `branch_holiday_operations`는 "공휴일인데 운영한다"만 담는다.
알티오라 위례는 반대로 ★평일인데 쉰다★는 케이스라 지금 구조로는
담을 수 없다. 방학만 고치면 이 축은 또 못 담는다 — 두 축을 같이
설계해야 나중에 뒤집지 않는다.

### ★다음 세션 최우선 — ①이름표 자동 부여 ②엑셀 빈칸 역산+두 축 통합 설계

**1순위 — 이름표 자동 부여를 업로드 흐름에 연결.** 상세 배경은 아래
"템플릿 업로드 구조 전환" 섹션의 "디자이너 원본에는 이름표가 없다"
참고. 요약: 디자이너 원본 pptx는 도형 이름이 `Google Shape;17;p2`
같은 것뿐이라 그대로는 못 쓴다. pptx-server에 `template_namer.py`/
`_apply_template_names.py`가 이미 있으나 지금 업로드 흐름에는 연결돼
있지 않다.

★정정(2026-08-31)★ — 위 문단과 아래 0)~3), 그리고 "★신규 발견" 절의
"49개 생성이 통째로 실패한다" 서술 둘 다 ★틀렸다★. `template_resolver.py
_prepare_template()`가 다운로드 직후 이미 이름표를 자동으로 붙인다
(8/11 3단계 통합, 아래 "이름표 자동부여" 섹션 참고) — "업로드 흐름"과
"생성 흐름"을 혼동한 서술이었다. 브라우저에서 파일을 고칠 필요가 없다.
자세한 정정 내용과 채택된 방향(A-2)은 새 섹션
"이름표 부여 — A-2 전환 + 업로드/적용 화면 반영 (2026-08-31)" 참고.

0) 착수 전 실물 확인 — `template_namer.py`/`_apply_template_names.py`가
   정확히 무엇을 어떤 기준으로 붙이는지, `validate_template.py`의
   검증 기준과 일치하는지
1) 어디서 실행할지 결정 — 브라우저(JSZip으로 XML 수정 후 업로드) /
   GitHub Actions / 별도 경로. 브라우저면 서버 왕복이 없어 가장 싸다
2) 붙인 뒤 재검증 → 점검 결과에 ✅로 뜨는지
3) 9월 양식 활성화 → 49개 생성 → ★9월 실제 배포본 48개와 대조★
   (정답이 있는 검증. HANDOFF에 오래 남아 있던 "정답본 대조" 과제)

#### 착수 전 조사 결과 (2026-08-28 오후) — 위 0)의 답

`pptx-server/template_namer.py`를 읽었다. ★로직은 이미 완성돼 있다★
```
MENU_TABLE    표 도형 중 면적이 가장 큰 것 (표 여러 개면 경고)
ORIGIN_BOX    텍스트에 '원산지 표기'
MATERIAL_BOX  텍스트에 '원재료' + '표시안내'
ALLERGY_BOX   텍스트에 '알레르기 표시'
```
- 멱등이다 — 이미 이름표가 있으면 건드리지 않는다.
- 그룹 내부까지 재귀 순회한다(ALLERGY_BOX가 그룹 안에 있음 — 오늘
  오전 실측과 일치).
- 탐색 기준이 도형 이름이 아니라 ★텍스트★인 것이 핵심이다. 도형
  이름은 매달 바뀌지만 텍스트는 1~8월 내내 불변임이 실측 확인됐다.
- 이 텍스트 기준은 오늘 오전 48개 템플릿 실측과 정확히 일치한다.

**남은 문제 — 실행 위치.** `template_namer.py`는 python-pptx 기반이라
브라우저에서 돌지 않는다. ★그리고 파일 상단에 중요한 경고가 있다★ —
`_apply_template_names.py`(ElementTree 전체 재작성 방식)는 `p:` 접두사가
날아가고 MS 확장 네임스페이스가 `ns2:`/`ns3:`/`ns5:`로 개명되며 파일이
수천 바이트 줄어드는 부작용이 실측 확인됐다 — 이식 금지. python-pptx는
차이 ±100바이트, 네임스페이스 그대로. → ★브라우저에서 XML을 다시 쓰는
방식도 같은 위험이 있다.★

**A안 타당성 실험 결과 (2026-08-28 오후, 실물 검증 완료) — 채택 확정.**
9월 디자이너 양식(8,377,038바이트)으로 문자열 치환 방식(cNvPr의 name
속성만 정규식 치환, 나머지 XML은 무변경)을 실험했다. 결과 전부 통과:
```
이름표 4/4 부여      3개 슬라이드 전부(MENU_TABLE/ORIGIN_BOX/
                     MATERIAL_BOX/ALLERGY_BOX)
python-pptx 정상 열림
도형 수 155개 유지    원본과 동일
네임스페이스 접두사   a·a14·a16·p·p14 ★완전 동일★
XML 헤더             동일
slide XML 차이       -7자 (이름 길이 차이뿐)
파일 전체 차이       -2,724바이트 (zip 재압축 오차, 내용 무변경)
```
★`_apply_template_names.py`가 일으켰던 파손(p: 접두사 소실, ns2:/ns3:
개명, 수천 바이트 감소)이 ★전혀 없다★. `template_namer.py`의
python-pptx 방식(±100바이트)보다도 XML 변경이 적다.
→ ★A안 채택 확정.★ B(GitHub Actions)·C(로컬 실행)는 폐기한다.

**확정된 부여 규칙**(`template_namer.py`와 동일 기준):
```
MENU_TABLE    <a:tbl>을 포함한 p:graphicFrame 중 a:ext 면적 최대
              (9월 양식은 슬라이드당 표 1개뿐 — 실측)
ORIGIN_BOX    p:sp/p:pic 블록 텍스트에 '원산지 표기'
MATERIAL_BOX  p:sp 블록 텍스트에 '원재료' + '표시안내'
ALLERGY_BOX   블록 텍스트에 '알레르기 표시'
```
★블록 단위(`<p:sp>…</p:sp>`)로 잘라 그 안의 첫 `p:cNvPr name`만
치환한다. ★이미 이름표가 붙어 있으면 건너뛴다(멱등).

**다음 세션 착수 — 0단계는 끝났다.**
1) 위 로직을 `lib/template-analysis.ts`에 `applyTemplateNames(zip)`로
   구현. JSZip으로 slide XML을 읽어 치환 후 다시 쓴다. ★zip 재작성
   시 압축 옵션·파일 순서를 원본과 맞출 것★
2) 업로드 흐름에 연결 — 점검 → 이름표 부여 → Storage 업로드. 점검
   결과에 "이름표 자동 부여됨"으로 표시
3) 9월 양식 재등록·활성화 → 49개 생성
4) ★9월 실제 배포본 48개와 대조★ (정답이 있는 검증)

★9월 양식·배포본 파일은 세션 간 유지되지 않는다. 다음 세션에
유대표님께 다시 요청할 것.

**2순위 — 엑셀 빈칸 역산 + 두 축 통합 설계.**

실측: 방학·휴무 요일은 배포본에서 메뉴 칸이 비어 있다.
  강동ECC (5일 방학)  5주차 3행(중식·오전·오후) 전부 빈칸
  광명POLY(4일 방학)  5주차 월요일만 채워짐, 화~금 빈칸
띠 높이도 그 주 3행 합과 일치한다(띠 1,704,261 / 3행 합 1,718,359).

즉 ★영양사는 이미 엑셀에서 방학 요일을 안 채우고 있다★.
그러므로 O/X를 따로 입력받을 필요가 없다:

```
엑셀에서 빈 요일 감지  −  공휴일(public_holidays)  =  방학 구간 후보
      ↓
팝업으로 "○○원 화~금 방학 맞습니까?" 확인만 받음
      ↓
확정되면 코드가 srcRect로 그림 크롭
```

이 방향이 "실무자가 찾아 들어가지 않게, 팝업으로 확인만"이라는 확정
UX 원칙과 일치하고, 실무자 입력을 늘리지 않는다.
★단 안전장치 필수 — 영양사가 실수로 안 채운 칸도 방학으로 잡힌다.
그래서 자동 확정이 아니라 반드시 팝업 확인을 거친다.

순서가 중요하다 — 아래 2)(엑셀 역산)가 확정돼야 입력 UI가 정해진다.

0) 착수 전 실물 확인 — `VacationAssignmentPopup.tsx` 현재 구현,
   `has_vacation`을 읽는 코드 전수 검색. 오늘 세션에서 이 둘을
   확인하지 못한 채 프롬프트를 쓰려다 중단했다.
1) 두 축 통합 설계 — 방학(요일범위) + 원별 공휴일 예외(평일 휴무).
   따로 만들면 나중에 뒤집는다.
2) 엑셀 빈칸 역산 + 팝업 확인 구조
3) DB 마이그레이션(`has_vacation`은 지우지 않고 병행 유지):
   ```
   vacation_from_dow int  -- 1=월 ~ 5=금
   vacation_to_dow   int  -- NULL이면 방학 없음
   ```
   기존 데이터 마이그레이션: `has_vacation=true` → `from=1,to=5` /
   `false` → `NULL`. 무손실이다.
4) 그림 크롭 로직(srcRect) — `pptx_generator`.
   1일=20%. 7월 영양사 수작업은 19.71%·8.76%+10.96% 등 제각각이라
   코드가 계산하는 쪽이 더 정확하다.

### 참고

배포본 Egg 파일은 리눅스에서 못 연다(알집 전용). ZIP으로 받을 것.
Egg를 우회 추출하면 파일명이 날아가 어느 원인지 특정 불가.
ZIP은 cp437→cp949 재디코딩으로 한글 파일명 복원 가능.

디자이너 원본 양식 1~9월 + 배포본 7·8·9월을 모두 확보했다. 다음
세션에서 다시 필요하면 유대표님께 요청할 것(세션 간 파일은 유지되지
않는다).

Vercel 서버리스 요청 본문 상한 4.5MB. 파일 업로드는 항상 Storage 직접
업로드로 갈 것. 다른 화면(엑셀 업로드 등)도 같은 벽에 걸릴 수 있으니
대용량이면 점검할 것.

Node.js 20 종료(2026-10-01) 건 확인 완료 — kizmeal-renewal과
valueleader 모두 이미 24.x라 대상 아님. 경고에 걸린 1개는 오래
배포하지 않은 다른 프로젝트다. 조치 불필요.

유대표님 9/22~10/3 부재(샌디에이고). 그 기간 배포·대응 불가. 단
식단표 자동화는 마감을 두지 않기로 했다 — 10월 식단은 기존 수작업으로
가고, 완전해진 뒤 실전 투입한다.

---

## 템플릿 업로드 구조 전환 (2026-08-28 오후)

### 발단 — "양식 준비 완료"가 거짓말이었다

실무 점검 중 발견 — 식단 자동화 화면에서 8·10·12월이 "양식 준비
완료"로 뜨는데 디자이너 양식을 올린 적이 없었다. 조사 결과:
- `diet_templates`에 행이 6월 v1 하나뿐이고 그마저 year/month가 NULL
- `resolve_template_set()`은 구체적 year/month로 필터하므로 이 행을
  참조하지 않는다 → ★모든 달이 로컬 폴백(6월 양식)으로 생성되고
  있었다★. 7·8·9월 실제 배포본은 전부 영양사 수작업 결과이고,
  시스템이 그 달 진짜 양식을 쓴 적은 한 번도 없다.
- check-form API는 Storage에 빈폼 파일이 있는지만 보므로 "완료"로
  표시됐다. 화면이 거짓말을 하고 있었다.

※ 단 빈 엑셀 폼 생성(`gen_form.py`)은 디자이너 PPTX와 무관하다 —
①②가 완료로 뜨는 것 자체는 정상이었고, 진짜 위험은 ④ PPTX 생성이다.

### 막힌 지점 — Vercel 413, 시간 초과가 아니었다

9월 양식(8.1MB) 업로드가 3분 넘게 멈췄다. Vercel 로그 실측:
```
14:01:36 /api/board/diet/templates → 413
```
같은 시각 3월 양식(1.4MB)은 9초 만에 200. 즉 시간 초과가 아니라
★Vercel 서버리스 함수의 요청 본문 상한 4.5MB★에 걸린 것이다. 플랫폼
하드 제한이라 maxDuration으로는 해결되지 않는다(Pro 플랜이어도 동일).
★추정으로 maxDuration부터 올렸다면 못 고쳤을 것 — 로그 확인이
결정적이었다.★

### 해결 구조 — 파일이 Vercel을 거치지 않게 바꿨다

```
이전:  브라우저 → [Vercel 함수 4.5MB 벽] → Storage
현재:  브라우저 ─────────────────→ Storage (직접)
           └→ [Vercel 함수] JSON 수 KB만 → 검증 → DB
```

1) 파일 선택 → 브라우저가 JSZip으로 열어 검증·스타일·구조 분석
2) 양식 점검 결과 표시
3) `POST /api/board/diet/templates/prepare` → 경로·버전 발급
4) `supabase.storage.from('diet-templates').upload(...)` 직접
5) `POST /api/board/diet/templates`(JSON) → 파일 존재 확인 → DB insert
6) 확정 실패 시 방금 올린 파일을 `remove()`로 정리(고아 방지)

★권한 변경 불필요 — `makeSupabase()`가 원래 service role이 아니라
anon key + 사용자 세션이었다. 브라우저에서 같은 세션으로 올리므로
버킷 정책을 건드릴 필요가 없었다.
★경로의 버킷명 중복(`.from('diet-templates')` + path
`diet-templates/…`)은 ★그대로 유지했다★ — 기존 v1 파일이 그 자리에
있어 고치면 못 찾는다.

### 커밋 6개

```
c7c7953 검증·스타일 로직을 lib/template-analysis.ts로 이동
d93c99f 구조 분석 analyzeTemplate() 추가
c4454e9 API를 prepare(경로 발급) + 확정(JSON) 2단계로 분리
39e88b5 페이지를 직접 업로드 + 양식 점검 결과로 전환
52f5ffe 안내 문구를 점검 의미로 정리
b6d0258 가로 그림 판정 임계값 80% → 30%
```

### 가짜 미리보기 제거

기존 MiniPreview는 "키즈밀 튼튼 식단 2026년 7월", "샘플 메뉴①②",
요일·섹션이 전부 하드코딩이었다. 색상만 진짜였는데 화면은 "미리보기
확인 후 적용하세요"라고 안내했다 — 확인할 게 없는데 확인하라고 한
셈. 실제 검증·구조 분석 데이터로 대체하고 "양식 점검 결과"로 이름을
바꿨다. 항목마다 쉬운 한 줄 설명과, 실패 시 무엇을 해야 하는지
(디자이너께 확인 요청)까지 표시한다.

### 실물 검증 완료

```
3월 1.4MB  회귀 정상 (점검 결과 즉시 표시)
9월 8.1MB  ★413 → 등록 성공★
DB 확인    year=2026 month=9, file_path v3_2026-09_none_…
목록 화면  연·월별 그룹 + "활성 템플릿 없음" 배지 정상
```

### ★신규 발견 — 디자이너 원본에는 이름표가 없다

9월·3월 양식 모두 점검 결과가 ★이름표 4개 전부 없음★으로 나왔다.
디자이너 원본의 도형 이름은 `Google Shape;17;p2` 같은 것뿐이다
(2026-08-28 오전 실측과 일치).

즉 ★디자이너가 주는 파일은 그대로는 쓸 수 없다★ — 업로드 흐름에
이름표 자동 부여 단계가 반드시 필요하다. pptx-server에
`template_namer.py`/`_apply_template_names.py`가 이미 있으나 지금
업로드 흐름에는 연결돼 있지 않다.

★그래서 9월 양식은 등록만 하고 활성화하지 않았다★ — ~~활성화하면
`resolve_template_set`이 이 양식을 고르는데 `pptx_generator`가
MENU_TABLE을 못 찾아 49개 생성이 통째로 실패한다.~~

★정정(2026-08-31)★ 위 실패 시나리오는 틀렸다. `template_resolver.py
_prepare_template()`가 생성 직전에 이름표를 이미 붙이고,
`pptx_generator.py _get_table()`엔 "첫 has_table" 2순위 폴백까지
있어 통째로 실패하지 않는다. 실제 위험은 "조용한 6월 로컬 폴백"
(8/11 발견①과 같은 형태)이다 — 활성화 자체는 위험하지 않지만, 다음
단계(활성화 게이트)까지는 신중하게 간다.

다음 세션 1순위 작업 상세는 위 "식단표 자동화 — 원본 양식 + 배포본
박스 구조 전수 실측" 섹션의 "★다음 세션 최우선" 참고. ★단 그 절의
1)~3)단계 자체도 위 정정 대상이다 — 최신 방향은 "이름표 부여 — A-2
전환 + 업로드/적용 화면 반영 (2026-08-31)" 섹션을 볼 것.

---

## ✅ 해결 — RLS 미적용 2건 (2026-08-25 발견 → 2026-08-26 해결)

`enable_rls_admins_diet_review_260826.sql` 실행 완료. `admins`,
`diet_review_items` 두 테이블 모두 `rowsecurity=true`로 전환.

### 정정 (어제 기록 중)

`is_admin()`은 **`SECURITY DEFINER`라 재귀가 아니었다.** 어제 "b) 본문
미확인"으로 남겨둔 우려는 기우였음 — 확인 결과 함수 자체가 이미 재귀를
회피하는 구조였다. **실제 blocker는 `admins_super_admin_all` 정책
하나뿐**이었다(정책 본문이 직접 `admins`를 서브쿼리로 읽는 구조).
`is_super_admin()` 헬퍼 함수(`SECURITY DEFINER`)로 감싸 해결.

### 어제 기록에 없던 것

`diet_review_items`의 `admins_all` 정책은 어제 "컬럼 오류(`id` vs
`auth_id`)"만 적혀 있었는데, **역할 배열 자체도 불완전**했다.
`nutritionist_ck`/`nutritionist_consignment`가 빠져 있어 컬럼을 고쳤어도
두 역할은 여전히 매칭 안 되는 상태였을 것. `current_admin_role()` 헬퍼로
바꾸며 역할 배열을 `diet_review_items_admin_access` 정책에 새로 포함시킴.

### ★새 사실 — RLS를 켜도 앱 동작은 안 바뀐다

`diet_review_items`는 **앱이 전부 `service_role`로 접근한다.**
`diet-review`/`deploy`/`resubmit` 3개 라우트 모두
`db = serviceKey ? admin : supabase` 패턴이라, `service_role` 키가 있으면
RLS를 완전히 우회한다. 즉 **정책은 방어선이지 실제 동작 경로가 아니다** —
이번 RLS 활성화로 화면 동작이 달라질 여지 자체가 없었다(그래서 안전하게
켤 수 있었던 것이기도 함).

`admins`도 마찬가지로 **쓰기는 전부 `service_role`**(`admin`/`branch/route.ts`
계열)이고, **읽기만 anon 계열 클라이언트**를 거친다. 따라서 이번 RLS는
"anon 키로 직원 테이블을 직접 조회하는 경로"를 막는 것이 실질 효과이고,
ERP 자체 API 동작에는 영향이 없다.

### ※ 정정 복원 (이전 섹션 교체 때 소실됐던 메모)

조사 초기에 `branches`/`diet_templates`도 anon으로 읽힌다고 봤으나,
`pg_tables` 확인 결과 둘 다 `rowsecurity=true`였다. RLS는 켜져 있고
정책이 관대한 것으로 보인다 — 별개 사안으로 분리(이번 RLS 작업 범위 밖).

### 최종 검증 결과

- `pg_policies` 조회로 정책 4건 확인 완료(`admins` 3건 +
  `diet_review_items` 1건). `qual`/`with_check` 어디에도 `EXISTS` 서브쿼리
  0건 — 재귀 완전 제거 확인
- `admins_super_admin_all`은 `with_check`도 이번에 명시적으로 채워졌다.
  원래는 `with_check`가 `null`이라 PostgreSQL이 "`using`을 `with check`
  대신 쓰는" 암묵 동작에 의존하고 있었음 — 이번에 `is_super_admin()`으로
  양쪽 다 명시
- 브라우저 실물: 로그인 / 식단검토 49건(2026-06) / 관리자 관리 6명 전부
  통과

---

## ⚠️ 미해결 — RLS 활성화로 새로 드러난 관심사 3건 (2026-08-26)

- **a) `admins_select_all_admins`가 최소권한이 아니다** —
  이 정책이 `is_admin()` 기반이라 "활성 관리자면 누구나 전 직원 행을
  읽는다." `director`/영양사 등 낮은 권한 역할도 전 직원의 이메일·`auth_id`가
  조회된다. **anon 차단은 이번에 됐지만, 내부 역할 간 최소권한 분리는
  아직 안 됐다.** 별도 과제로 분리
- **b) 역할 배열이 두 곳에 따로 존재한다** —
  `lib/roles.ts`의 역할 상수와 RLS 정책의 역할 배열(`current_admin_role()
  in (...)`)이 서로 다른 파일에 독립적으로 존재한다. 나중에 `roles.ts`를
  고칠 때 정책도 같이 고쳐야 한다는 걸 아무도 기억 못 할 위험이 있다.
  이번에 `current_admin_role()` 함수로 한 겹 감쌌지만, **배열 값 자체는
  여전히 이중관리** — 근본 해결(예: 함수가 `roles.ts`와 동일한 소스를
  참조하도록 하는 것)은 안 됨. `lib/roles.ts`에 경고 주석은 추가함(`b4fa808`)
- **c) `service_role` 폴백이 지뢰가 됐다** —
  `db = serviceKey ? adminClient : supabase` 패턴에서 키가 없으면 anon으로
  폴백한다. RLS가 꺼져 있던 어제까지는 그래도 정상 동작했으나, **오늘부터는
  조용히 0건을 반환한다(에러 없음).** 키 누락을 에러로 만들지, 폴백을
  유지할지 결정 필요 — 미해결

### 확인 필요 (과제 아님, 질문)

저장소 `yuher826/kizmeal-renewal`이 **공개 상태다.** 인증 없이 전체
소스가 읽힌다. 키 커밋은 없으나(`.env` 계열은 `.example`만), 테이블
구조·권한 로직·HANDOFF 내용(직원 이메일 포함)이 공개된다. 의도한 것인지
확인 필요.

---

## ⚠️ 미해결(일부 해결) — ERP director 권한 갭 (2026-08-25 발견 → 저녁 1·2순위 해결)

- ERP 메뉴 8개 중 `allowedRoles`가 있는 건 "관리자 관리" 1개뿐
- `layout.tsx`는 `is_active` + `access_scope`만 보고 `role`은 안 봄
- API 다수가 `admins.is_active`만 확인하고 `role` 컬럼을 select조차 안 함
- `director`(허이사, 읽기 전용)가 실제로 가능했던 쓰기 작업 위험도순:
  1. ✅**해결(`8d0bf94`)** — 원 담당자 계정 이메일 변경 + 비밀번호 초기화
     조합 → 계정 탈취 경로
  2. ✅**해결(`8d0bf94`)** — 원 계정 정지 / 원 운영상태 변경 → 가용성 장애
  3. ✅**해결(2026-08-26, `397f8dd`/`cf3d8e0`/`f12b997`)** — 템플릿
     업로드/활성화/삭제
  4. ✅**해결(2026-08-27, `2bae9f0`/`7eef8c0`/`94bc5fd`/`780d1d7`/`dd4c835`)**
     — 원 프로파일 생성/수정
  5. **미해결** — 고객사 공지 작성/수정
- ★방어선은 3겹(메뉴 노출 / API 게이트 / RLS)이며 `allowedRoles`는 UX일
  뿐 보안이 아니다. URL 직접 입력으로 우회된다
- **다음 착수 대상은 5(고객사 공지)** — 단, 착수 전 `ErpUserContext`
  신설 + 역할×메뉴 노출 매트릭스 확정이 선행돼야 함(아래 "신규 과제" 참고)

### ✅ 1·2순위 해결 — 원 담당자 계정 관리 역할 게이트 (2026-08-25 저녁, `8d0bf94`)

- `lib/roles.ts`에 `BRANCH_ACCOUNT_ROLES = [super_admin, manager]` 신설.
  원 담당자 계정 초대·이메일변경·비번초기화·초대재발송·계정정지·
  원 운영상태 변경 6개 쓰기 API에 게이트로 적용
- 대상 라우트: `account`(POST) / `account/email`(PUT) / `account/reset`(POST) /
  `account/resend`(POST) / `account/status`(PUT) / `account/branch-status`(PUT)
- UI(`BranchProfileDetailClient.tsx`)도 버튼 6종 + 확인 모달 2개를
  `canManageBranchAccount`로 조건부 렌더(기존 `isSuperAdmin`/
  `ContractStatusButton` 패턴은 무변경 — 관심사가 다름)
- **브라우저 실물 검증 완료(양방향)**:
  - super_admin: 버튼 6종 전부 보임, 원 운영상태·담당자 계정 각각
    비활성화→재활성화 왕복 정상(403 없음)
  - director: 계정 관리 버튼 전부 안 보임. **담당자 계정 정보(이메일/
    상태/마지막접속/KOS ID)와 원 운영상태 배지는 정상 조회됨** —
    GET을 안 건드렸음이 실물로 확인됨
  - 좌측 메뉴 "관리자 관리" 미노출도 정상(기존 `allowedRoles`, 무변경)

**★설계 원칙 — "라우트가 아니라 메서드를 막는다".** `account/route.ts`와
`account/branch-status/route.ts`는 GET(조회)과 쓰기가 한 파일 안에 있다.
파일 단위로 막으면 director가 조회조차 못 하게 되어 "보안 강화"가 아니라
**기능 파손**이 된다. 이번엔 쓰기 메서드(POST/PUT)에만 게이트를 걸고
GET은 그대로 뒀다. **3·4·5순위(템플릿/원 프로파일/공지)를 이어서 고칠 때도
반드시 같은 방식으로 할 것** — 특히 원 프로파일(`branch-profiles/[id]`)도
GET/PUT이 한 파일에 있다.

**★`BRANCH_ACCOUNT_ROLES`를 `ADMIN_CREATE_ROLES`와 별도로 둔 이유** —
지금은 값이 같지만(`[super_admin, manager]`) 관심사가 다르다(관리자 계정
생성 vs 원 담당자 계정 관리). 하나를 재사용하면 나중에 한쪽 정책만
바꿀 때 다른 쪽까지 딸려 바뀌는 사고가 난다. **앞으로 새 권한 그룹이
필요할 때도 기존 상수 값이 우연히 같다고 재사용하지 말고 관심사 기준으로
새로 만들 것.**

**★검증용 원 — "테스트P"(`contract_type='temporary'`, 계정 이메일
`test@poly.com`)를 씀.** 실제 인물·조직과 무관한 순수 테스트 픽스처라
계정 정지·이메일 변경·운영상태 변경 같은 권한 검증에 안전하다(크레오
3원은 계약은 끝났어도 실제 이메일이 연결돼 있어 부적합 — 위 "임시계약
종료" 섹션 참고). **다음에 같은 종류의 검증이 필요하면 이 원을 쓸 것.**

### ✅ 3순위 해결 — 템플릿 관리 권한 게이트 (2026-08-26)

DB 마이그레이션은 사전에 실행 완료된 상태였고(2026-08-26), 이번 세션은
코드만 작업. `admins.can_manage_templates` 플래그로 게이트:

- **커밋 1**(`397f8dd`): `lib/roles.ts`에 `canManageTemplates`/
  `canDeleteTemplate` 헬퍼 신설
- **커밋 2**(`cf3d8e0`, API 1차 방어선): POST(업로드)/PATCH(활성화) →
  `can_manage_templates` 또는 `super_admin`. DELETE → `super_admin`만.
  GET(조회)은 "행 없으면 403" 동작을 그대로 두고 `admins` select만
  넓혔다(3안 방식 — 아래 참고)
- **커밋 3**(`f12b997`, UI 2차·편의): 업로드/활성화/삭제 버튼을 권한
  없으면 **숨기지 않고 disabled + 이유 안내**(문구 또는 title 툴팁).
  숨기면 "왜 버튼이 없지?" 문의가 발생한다

**★설계 판단 — role이 아니라 플래그를 쓴 이유.** 템플릿 담당은 "직무"가
아니라 "배정"이다. 영양사가 여러 명이지만 템플릿을 올리는 담당자는
따로 있다. role 기반으로 막으면 영양사 계정이 늘어날 때 권한이 자동으로
따라붙어 아무도 모르게 넓어진다. `super_admin`은 담당자 부재 시 대체
경로로 항상 통과.

**★조사(0단계)에서 나온 구조적 결정 — "3안"(GET 응답에 권한을 실어 보냄).**
`branches/[id]` 선례(페이지가 `admins`를 독립 조회)를 그대로 쓰려 했으나
기각했다:
- 1안(선례 그대로) — 템플릿 페이지는 482줄 `'use client'` 단일 파일이라
  서버 컴포넌트로 쪼개는 게 대수술이고, 권한 판단이 API와 페이지 두 곳으로
  갈라져 "roles.ts ↔ DB 정책 이중관리"(오늘 오전 기록)와 같은 종류의
  문제를 하나 더 만든다
- 2안(ErpShell Context 신설) — 아키텍처상 정답이지만 ErpShell을 쓰는
  모든 페이지에 영향을 준다. 오늘 범위가 아니다
- **3안(채택)** — GET `/api/board/diet/templates` 응답에
  `permissions: { canManage, canDelete }`를 실어 보낸다. 판단은 API 한
  곳에만 있고, 페이지는 그 결과만 받아 버튼을 disabled 처리한다. 파일
  구조도 안 바뀐다

**★실측 정정** — DELETE는 **이미 "활성 템플릿은 삭제 불가" 방어가
있었다**(`[id]/route.ts`, `is_active`면 400). 세션 중 "클릭 한 번으로
파이프라인 정지"로 과장 기술한 적이 있었으나 사실이 아니다.

**브라우저 실물 검증**:
- 목록 조회(GET) 정상 — `checkAdmin()`→`getAdmin()` 리팩터링이 GET을
  안 막았음 확인
- 업로드(POST) 차단 확인 — 테스트 매니저 계정(`manager`,
  `can_manage_templates=false`)으로 시도 시 토스트 "업로드 실패: 템플릿
  관리 담당자만 가능합니다"
- **활성화(PATCH)/삭제(DELETE)는 실물 미확인** — 현재 템플릿이 레거시
  1건뿐이고 이미 활성 상태라 두 버튼 자체가 렌더링될 대상이 없었다.
  실데이터를 건드려야 재현 가능해 중단함. PATCH는 POST와 동일하게
  `canManageTemplates()`를 쓰므로 같은 경로이고, DELETE는 더 좁은
  `canDeleteTemplate()`다 — 코드상 대칭이나, **템플릿이 2건 이상 되는
  시점(v6 양식 실전 투입 시)에 반드시 재확인할 것**

**★검증용 계정 — "테스트 매니저"(`yuher826+erptest@gmail.com`,
`manager`, `can_manage_templates=false`)를 씀.** 원래 "정리 대상(삭제
검토)"였으나, 권한 검증에 필수인 유일한 계정이라 용도를 "권한 검증
전용"으로 바꾸고 삭제 대상에서 제외했다(아래 "정리 대상" 섹션 참고).

### 신규 미해결

- **템플릿 soft delete(deleted_at) 미구현** — 지금은 hard delete라 과거
  배포분을 재현할 방법이 없다. `canDeleteTemplate`을 `super_admin`으로
  좁힌 것도 이 문제에 대한 임시 조치일 뿐, 정식 해법은 soft delete
- **`can_manage_templates`를 켜고 끄는 화면이 없다** — 현재는 SQL로만
  변경 가능. 담당자 교체 때마다 DB를 직접 만져야 한다
- **ERP 페이지가 권한을 알아야 할 때마다 각자 `admins`를 재조회하는
  구조다**(`branches/[id]` 선례). 권한 로직이 페이지마다 흩어진다. 근본
  해결은 `ErpShell` Context 도입이며 별도 과제. 이번엔 API 응답에 권한을
  실어 보내는 3안으로 회피했다(위 "★조사에서 나온 구조적 결정" 참고)

### ✅ 4순위 해결 — 원 프로파일 권한 게이트 (2026-08-27)

이전 세션에서 미결로 남겨뒀던 "PUT 게이트 범위(A/B/C)"를 B안으로
확정하고 5개 커밋으로 완결:

- **커밋 1**(`2bae9f0`): `lib/roles.ts`에 `BRANCH_PROFILE_EDIT_ROLES`/
  `BRANCH_PROFILE_CREATE_ROLES` 신설
- **커밋 2**(`7eef8c0`, API 1차 방어선): `[id]/route.ts` PUT과
  `route.ts` POST에 게이트 추가. GET 2개·DELETE는 무변경("라우트가
  아니라 메서드를 막는다" 원칙 유지)
- **커밋 3**(`94bc5fd`, 감사 기록): 생성/수정 시 `audit_logs`에 기록.
  값은 남기지 않고 **변경된 필드"명"만** 남김(개인정보 축적 방지),
  변경 필드 0개면 insert 자체 생략(노이즈 방지)
- **커밋 4**(`780d1d7`, UI 2차): 상세 화면 저장 버튼 — 권한 없으면
  **숨기지 않고 disabled + amber 배너 안내**(3순위와 동일 원칙).
  입력 필드 40개는 그대로 둠(저장이 막혀 있어 실제 변경 불가)
- **커밋 5**(`dd4c835`, 등록 화면): `branches/new`를 서버 컴포넌트로
  분리(`NewBranchProfileClient.tsx`로 내용 이동, 리팩터링 없음).
  권한 없으면 폼 자체를 **숨김** — 등록 화면은 조회할 내용이 없어
  disabled보다 숨김이 맞다고 판단(상세와 반대 처리, 의도적)

**★결정 근거 — PUT = `super_admin` + `manager` + 영양사 2종(B안).**
간식 구성·슬라이드 장수·영문 표기·배포 이메일은 원래 영양사가 49개
업장에 수동으로 채워 넣던 값이고, 실제 배포 실행자도 영양사다
(`DEPLOY_ROLES`). 자동화가 담당자를 바꾼 게 아니다.

**★결정 근거 — POST(신규 등록) = `super_admin` + `manager`만.** 원
신규 등록은 계약 체결 행위라 성격이 다르다(EDIT보다 좁힘).

**★`BRANCH_PROFILE_EDIT_ROLES`는 `UPLOAD_ROLES`와 값이 우연히
같지만 재사용 금지.** 관심사가 다르다(엑셀 업로드 vs 원 정보 수정).
`BRANCH_ACCOUNT_ROLES`를 `ADMIN_CREATE_ROLES`와 분리한 것과 같은
이유 — 하나를 고칠 때 다른 쪽이 조용히 따라 바뀌는 사고를 막는다.

**★GET은 손대지 않았다("라우트가 아니라 메서드를 막는다").** director
조회가 실물로 정상 확인됨(아래 검증 참고).

**브라우저 실물 검증 5종 — 전부 통과(3순위 때와 달리 미검증 항목
없음)**:
- super_admin: 상세 화면 저장 정상
- 저장 시 `audit_logs`에 변경된 필드명만 정확히 1개 기록 확인
- manager: 상세 저장 정상, 신규 등록 정상
- director: 상세 화면에 amber 배너 노출 + 저장 버튼 disabled(툴팁
  "원 프로파일 수정 권한이 없습니다") 확인
- director: 등록 화면 진입 시 폼 대신 "원 등록 권한이 없습니다" +
  목록 링크만 노출(차단) 확인

**★전수 점검 결과(코드 변경 없음, 확인만)** — `branch-profiles` 하위
쓰기 라우트 13개 전부 게이트 확인 완료. 게이트 없는 곳은 4곳
(목록 GET·상세 GET·담당자계정 GET·원 운영상태 GET)뿐이며 전부 조회용
GET이라 의도된 것(director 등 읽기 전용 역할도 조회는 되어야 함).
`diet-upload`(POST)는 이미 `UPLOAD_ROLES`로 보호돼 있었음을 재확인.

**★DELETE는 이번에도 손대지 않았다(신규 미해결, 이월)** — 161~165행에서
`contract_status`만 토글하는데도 이름이 DELETE라 오해 소지가 있다.
개명(예: `PATCH /contract-status`) 검토 필요. **추가 발견 —** 다른
12개 쓰기 라우트는 전부 `lib/roles.ts` 상수(`BRANCH_PROFILE_EDIT_ROLES`
등)를 쓰는데 이 DELETE만 `role !== 'super_admin'` **하드코딩
리터럴**이다. 개명 과제와 묶어서 상수화까지 함께 처리할 것.

남은 건 **5순위(고객사 공지)** 하나. 단, 착수 전 아래 "신규 과제 —
`ErpUserContext`" 섹션을 먼저 볼 것 — 같은 구조적 문제(클라이언트
페이지가 role을 못 읽음)가 5순위에서도 반복된다.

### 신규 과제 — `ErpUserContext` 신설 (다음 세션 최우선)

**문제**: 클라이언트 페이지가 `role`을 못 읽어 권한 게이트를 붙이려면
페이지마다 서버 컴포넌트로 분리해야 한다. 이번에 `branches/new`
(90행)는 했지만 `branches/page.tsx`(699행·훅 16개)는 비용이 커서
보류했다("신규 등록" 진입 버튼은 여전히 항상 노출). 5순위(고객사
공지)에서도 같은 문제가 반복될 것.

**해법**: `layout.tsx`가 이미 `admins`에서 `role`을 읽어
`ErpShell`(클라이언트)에 `erpUser`로 넘기고 있다. `ErpShell`에서
Context로 내려주면 모든 클라이언트 페이지가 파일 이동 없이
`useErpUser()`로 role을 쓸 수 있다. → 그 위에서 `branches/page.tsx`의
"신규 등록" 버튼 게이트를 한 줄로 처리 가능(3순위 원칙대로
disabled + 사유).

이 세션에서 "권한 로직이 페이지마다 흩어진다"고 이미 기록해뒀던
근본 해결책과 동일한 것 — 이번엔 더는 미루지 말 것.

### 신규 과제 — 역할×메뉴 노출 매트릭스

`lib/erp-nav.ts` 메뉴 12개 중 `allowedRoles`가 붙은 건 "관리자 관리"
1개뿐. 나머지 11개는 전원에게 보인다. 영양사가 계정을 받으면 브랜드
관리·CS 관리도 그대로 보인다.

**★원칙 구분 — 버튼과 메뉴는 다르게 처리한다.** 버튼은 "숨기지 않고
disabled+사유"(3순위 원칙), 메뉴는 "숨김". 버튼은 그 일을 하러 온
사람이 보는 것이고, 메뉴는 애초에 그 사람 업무가 아닌 영역이다.
모순이 아니라 성격이 다르다.

**★`allowedRoles`는 UX이지 보안이 아니다**(URL 직접 입력으로 우회).
실질 방어선은 API 게이트다.

**★역할 5종 × 메뉴 12개 매트릭스는 사람이 정해야 한다. 코드가 추측
금지.** 다음 세션에 유대표님께 확정받고 시작할 것.

### 신규 과제 — `audit_logs`가 쓰기 전용

- SELECT 정책 `audit_select_super`가 `role='super'`를 참조한다. 예전에
  정리한 그 오타이며 아무도 매칭되지 않는다. RLS 섹션의 "DB 정책
  이중관리"와 같은 부류.
- 앱 전체에 `audit_logs`를 읽는 코드가 없다(7곳 전부 insert). 현재는
  Supabase SQL Editor로만 조회 가능.
- 기록은 정상적으로 쌓이므로 급하지 않으나, 조회 화면이 없으면 감사
  로그의 가치가 절반이다.

### 확인 필요 — 실데이터 · 검증 계정

- **실데이터 점검 필요**: 노원E의 `contract_type`이 미입력 상태로
  확인됐다. 필수(*) 항목이 비어 있는 원이 얼마나 되는지 SQL로 전수
  확인 필요. `contract_type`은 임시원 필터가 쓰는 컬럼이라 방치하면
  안 된다.
- **검증 계정 비밀번호를 채팅에 남기지 말 것.** 현재 2개 대화에
  평문으로 남아 있어 정리 필요.

### 정정

"deploy route.ts에서 manager 일반배포 권한 제거 필요"는 **잘못된
기록이었다.** 현재 `DEPLOY_ROLES=['nutritionist_ck']`로 manager는 이미
권한이 없다. HANDOFF 전문을 재검색했으나 해당 문구 자체를 찾지 못해
삭제할 대상이 없었다 — 다른 세션의 구두 논의였거나 착오로 추정.

---

## ⏸️ 보류 — 크레오 임시계약 종료 (2026-08-20)

크레오(송파·대치 등) 임시계약이 종료됨에 따라 **크레오 계정/이메일 관련
후속 작업은 진행하지 않음**:

- ~~송파크레오 계정 이메일 원복~~ (`yuher826+songpacreo@gmail.com` →
  실제 이메일) — 계약 종료로 보류. 위치는 참고용으로만 남김:
  `/erp/branches/5cecbb7a-aaad-4f8b-b901-8ae4175c9475` → 담당자 계정
- ~~대치크레오 로그인 계정 생성~~ — 계약 종료로 보류
- ~~크레오 3개 원 실계정 초대~~ — 계약 종료로 보류

**단, 임시원(`contract_type='temporary'`) 처리 기능 자체는 크레오 전용이
아니라 범용 설계라 계속 유지·완성한다.** (`branch_filters.py`,
`lib/pptx-eligibility.ts`, `BranchDietUploadCard` 등 — 나중에 다른 원이
임시계약으로 들어와도 코드 수정 없이 재사용)

---

## 정리 대상 (테스트용으로 만든 것들)

- ~~**"테스트 매니저"** (`yuher826+erptest@gmail.com`) 관리자 계정 —
  삭제 검토~~ **용도 변경(2026-08-26): 삭제 대상에서 제외한다.**
  템플릿 관리 권한 게이트(`can_manage_templates=false`, `manager`) 브라우저
  실물 검증에 쓴 유일한 계정 — 지금 지우면 같은 검증을 다시 못 한다.
  **"권한 검증 전용"으로 용도를 바꿔 유지.** 상세는 아래 "템플릿 관리
  권한 게이트" 섹션 참고

---

## ⏸️ 확인 대기 — 실계정 데이터, 삭제 금지 (당사자 확인 전까지)

**2026-08-20 발견**: `weekly_menus`/`diet_templates`가 비어있는데도 10월
연/월 워크플로 스테퍼가 ①양식 준비 ②양식 받기까지 "완료"로 표시됨 — 판정
로직 버그 아님, 실제 데이터 있음(확인 완료):

```
Storage:  diet-files/2026/10/blank_form_26_10_v6.xlsx
DB:       form_downloads (권팀장, 2026-07-03 07:06:29 다운로드)
```

`form_downloads` 전체 이력 대조 결과, **같은 날(7/2~7/3) 7월→8월→10월
순으로 빈 폼을 훑은 흔적**(9월은 없음)이라 정황상 오타 가능성이 있어
보였으나, **권팀장 실계정이고 실제로 다운로드까지 받아간 데이터라 추측만으로
삭제하지 않기로 함**(유대표 결정 2026-08-20). 실행에도 지장 없음(파일이
남아있어도 실제 10월 작업 시작 시 `gen_form.py`가 새로 덮어씀) — 급하지 않음.

**→ 유대표가 권팀장에게 직접 확인 후 결정.** 의도적 사전 준비였다면 이
항목 삭제, 오타였다면 위 Storage 파일 + `form_downloads` 행(`id:
be97d22c-08d1-4c0b-bb85-ab8aa5059e46`) 정리.

---

## 오늘 완료된 것 (커밋 해시 포함)

- **크레오 3원 온보딩 + 임시원 배포 제외** — DB 온보딩(SQL) + `contract_type='temporary'` 적용
- **학부모 포털 게이트 완화** (`98377b8`) — pptx/pdf/jpg 중 하나라도 있으면 노출
  - ⚠️ **build만 통과, 실제 학부모(parent) 계정으로 크레오 원 PDF 노출은 브라우저 미검증.**
  - 확인 방법: `/parent` 학부모 계정 로그인 → 송파크레오 소속 아이 등록 → 식단표 메뉴 확인
- **크레오 식단표 직접 업로드 기능** (`1119f57`) — 원 포털(`board/customer`) 기준 **실전 검증 완료**
- **매니저 계정 생성 권한 확장** (`9cc9935`, `60f0926`) — UI 드롭다운 필터링까지 확인
  - ⚠️ 단, "관리자 계정 생성" 버튼을 끝까지 눌러 **실제 계정이 만들어지는지는 미검증**
- **/board/login 문구 수정** (`6a7cdd2`) — "학부모" 언급 제거
  - ⚠️ dev 재기동 후 실제 화면 반영 **[결과 확인 필요 — 세션 마지막 재기동 요청했으나 결과 미확인]**

### 2026-08-10

- **ERP 사이드바 메뉴 중복 활성화 버그 수정** (`7de1eb8`) — 하위 경로가 여러 메뉴 href의
  prefix에 동시에 걸릴 때 두 메뉴가 동시에 active 처리되던 문제. 가장 구체적인(긴) href
  하나만 active로 판정하도록 수정. **프로덕션 배포 확인 완료.**
- **next.config.mjs 로컬 dev 웹팩 캐시 경합(-4094 UNKNOWN) 이슈 해결** (`2d2e836`) —
  Windows에서 `next dev` 시 여러 워커가 웹팩 디스크 캐시(`.next/cache/webpack`)에 동시에
  쓰기 시도하며 나던 `-4094 UNKNOWN`, `pack.gz rename ENOENT` 에러. dev 전용으로
  `config.cache=false`(메모리 캐시만 사용) 적용, 프로덕션 빌드는 영향 없음(clean build로 확인).
  - 부수 조치: **알약 탐지 제외에 프로젝트 폴더 등록함**
- **`gen_form.py` 6월 서식 잔재 제거** (`cb82311`)
  - 기준폼이 6월 실데이터 파생이라 (a) 1주차 D열 선거일 회색 (b) 1·2·3주차 F열 🎂 +
    18행 생일 주황 (c) 5주차 D·E·F 해당없음 회색이 **다른 달에 눌어붙던** 문제
  - 각 주차 **B열(운영일)** · **5주차 D열(해당없음)** · **1주차 D열(공휴일)** 을 서식
    템플릿으로 캡처해 **열 통째 덮어쓰는** 방식. **색상 하드코딩 없음**
  - 검증: 7월(5주차 꽉참)·8월(경계) **6월 잔재 4종 0건**, 헤더 공백 2칸 코드포인트 대조
    일치, **실물 엑셀 확인 완료**
  - **공휴일 자동 판정은 의도적으로 넣지 않음** — 공휴일에 운영하는 원과 쉬는 원이 갈려
    코드가 일괄로 정할 수 없음. `holidays` 라이브러리 도입 검토했다 **철회**.
    대신 `--holiday 17:제헌절` 옵션 신설
- **연도 드롭다운 하드코딩 제거** (`2599bc9`)
  - `[2024~2030]` 등 하드코딩이 **4곳**에 흩어져 있었음. 범위 끝을 지나면 선택지가
    사라져 코드를 고쳐야 하는 구조
  - `lib/diet-utils.ts`에 **`getYearOptions()` 신설** → 현재 연도 기준 **작년~3년 후(5개)**
    동적 계산. 4곳 전부 이 헬퍼 호출로 통일
  - **예상 밖 발견**: upload 페이지 2곳은 `[2025,2026,2027]` 3개뿐이라 **2028년에**
    diet 화면(2030년)보다 **먼저 깨질 상태**였음. 함께 수정
  - URL `?year=2020` 같은 범위 밖 값 방어 추가
- **비상배포 역할 게이트 불일치 수정** (`7bf0a90`)
  - `app/erp/(protected)/review/page.tsx` **1213·1414행**
    `role === 'super_admin'` → `EMERGENCY_DEPLOY_ROLES.includes(role)`
  - 원인: 커밋 `1f76c91`(06-19)이 본문에 "비상배포 버튼 노출 조건 변경"을 명시했으나
    **board만 고치고 erp를 누락**. erp 게이트는 `17bb0d9`(06-12) 이후 blame 무변동
    → **의도적 축소가 아닌 실수로 확정**
  - 수정 전 상태: API(`deploy/route.ts:104`)는 manager 허용인데 erp UI에 버튼이 없어,
    **manager가 권한은 있고 실행 수단이 없었음**
  - **정책 확정**: super_admin 부재 시 아무도 비상배포를 못 하는 상황을 막기 위해
    **manager도 비상배포 가능**
  - 실물 확인: 매니저 계정으로 `/erp/review` 승인완료 항목에 🚨 비상배포 노출 확인,
    📧 배포는 미노출(일반배포 권한 없음, 의도대로)
  - 일반배포/일괄배포 게이트(`821·1203·1404`행 `DEPLOY_ROLES`)는 **무변동**

---

## ⚠️ 신규 미해결 — 오늘 발생, 운영 확정 필요

- ~~**프로덕션 12월 "양식 준비" 시 공휴일 팝업 미표시**~~ → **해결됨
  (2026-08-24) — 코드 버그 아니었음, 배포 타이밍 문제였음.**
  Vercel 대시보드 → Settings → Environment Variables 확인 결과
  `DATA_GO_KR_SERVICE_KEY`가 **Production and Preview**에 정상
  등록돼 있었고, "공휴일 설정 다시 보기"로 재확인하니 12/25
  기독탄신일이 정상 표시됨. **결론: 8/20 당시엔 환경변수 추가 후
  Redeploy가 아직 끝나기 전에 확인해서 구버전이 돌고 있었던 것**
  (아래 "반복 함정" 항목 참고. `/api/diet-automation/holidays` 라우트
  자체는 그때도 배포돼 있었다는 게 이미 확인돼 있었으니, "라우트는
  있는데 새 env가 아직 안 붙은 배포"라는 설명과 정합적).
  - ★**곁가지로 발견해 즉시 고친 버그(그대로 유지)** —
    `checkHolidaysBeforeGenerate()`가 실패해도 에러 없이
    `{openPopup:false}`만 반환해서 "공휴일 없음"과 "조회 실패"가
    화면상 구분이 안 됐던 문제. `error` 필드를 추가해 실패 시
    `alert()`로 반드시 사람에게 보여주도록 수정 완료(진행은 막지 않음
    — `gen_form.py`의 DB 조회 실패 시 경고만 내고 진행하는 설계와
    동일). 이번 사고의 직접 원인은 아니었지만, 다음에 진짜 조회
    실패가 나도 무신호로 넘어가던 구조 자체는 여전히 문제였어서
    수정은 그대로 남긴다.
- ~~**`generate-form.yml`이 `--holiday` 없이 `gen_form.py`를 호출함**~~
  → **해결됨 (2026-08-20, 4단계)**. CLI 인자로 넘기는 대신 `gen_form.py`가
  `public_holidays`를 직접 읽는다. **워크플로 수정은 필요 없었다** —
  `generate-form.yml:31-32`가 이미 `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`를
  넘기고 있어서, 그 자격증명으로 조회만 추가하면 됐다.
  - `closure_policy='all_closed'`인 날만 공휴일 서식 적용
  - `'all_operating'`(노동절·제헌절 등)은 운영일로 둠
  - 미분류(NULL)는 **적용하지 않고 경고** — 코드가 추측하면 원칙이 깨진다
  - `--holiday`는 DB 값 위에 덮어쓰는 수동 경로로 유지(긴급·로컬 테스트)
  - `--no-db-holiday` 플래그 신설(오프라인 테스트용)
- **엑셀 `✏️ 입력 가이드` 시트 r65~69 공휴일 처리 안내 보완 필요**
  - 헤더에 공휴일을 **어떤 형식으로 쓰는지가 빠져 있음**
    (형식: `수  7/17` 줄바꿈 `공휴일` 줄바꿈 `(제헌절)`)
  - r66 C열 **"시스템이 자동 잠금 처리"는 현재 코드와 불일치** — `gen_form.py`는
    `--holiday` 지정 시에만 잠금
- **배서영 영양사·권팀장에게 "공휴일은 영양사가 헤더에 직접 표시" 방식 공유**

### 식단표 준비 워크플로우 스테퍼 — 3건 (2026-08-10 발견)

- **① "양식 준비" 완료 판정이 디자이너 템플릿을 확인하지 않음**
  - 현재 판정: `app/api/diet-automation/check-form/route.ts` — Storage(`diet-files`)의
    `{year}/{mm}/blank_form_{yy}_{mm}_v6.xlsx` **존재 여부만 HEAD로 확인**
  - 그런데 카드 설명(`app/erp/(protected)/diet/page.tsx:43` `WF_STEPS[0].desc`)은
    **"디자이너 양식으로 빈 폼 생성"** 이라 **설명과 실제 판정이 어긋남**
  - 실제로 `gen_form.py`는 기준폼(`키즈밀_식단표_기준폼_v6.xlsx`)으로 **엑셀 빈 폼만**
    생성하며 **디자이너 PPTX 템플릿과 무관**. 디자이너 템플릿은 **④ PPTX 생성** 단계에서 사용됨
  - 확정 필요:
    - ⓐ 카드 설명 문구를 실제에 맞게 고칠지
    - ⓑ 판정에 그 달 디자이너 템플릿 존재 확인을 추가할지
      (ⓑ라면 `diet_templates` 테이블 조회 로직 신설 필요)
- **② "양식 받기" 다시 버튼 문구가 ①과 구분되지 않음**
  - 두 카드 모두 완료 시 `다시`로 표시되나 동작이 다름:
    **①의 다시 = 빈 폼 재생성 / ②의 다시 = 기존 파일 재다운로드**
  - 제안: **"다시 준비" / "다시 받기"** 로 구분
  - ⚠️ 구현 주의 — 라벨이 `diet/page.tsx:871`
    `{busy ? '진행 중...' : done ? '다시' : step.cta}` **한 줄에서 4단계 전부 공유**됨.
    따라서 ①②만 바꾸려면 `WF_STEPS`에 재실행용 라벨 필드를 추가해야 함
    (`aria-label`은 `:867`에 별도로 `'다시 실행'` 존재 — 함께 손봐야 함)
- **(참고, 버그 아님) ② "양식 받기"가 완료로 뜨는 것은 정상**
  - `form_downloads` 테이블에 해당 연월 다운로드 이력이 있으면 완료 처리
    (기록: `app/api/diet-automation/download-form/route.ts:52`,
     조회: `diet/page.tsx:263~267`)
  - 순서 강제도 정상 작동 중 — `diet/page.tsx:654` `wfProgress`
    (`!formExists ? 0 : !formDownloaded ? 1 : …`)

---

## board/erp 중복본 정리 — ✅ 완료 (2026-08-11, push까지 완료)

로컬 PC(.env.local 보유)에서 솔아 세션이 전달한 패치(`kizmeal-board-erp-cleanup.patch`)를
`git am`으로 적용 → 최종 검증 → push까지 마무리함. 패치 파일은 적용 후 삭제.

- 커밋 `d57a59b`(구 `2cb1d7d`): 선행작업①) erp→board 역참조 11곳 → erp 경로로 교체
  (`diet/page.tsx` 2곳, `history/page.tsx` 3곳, `review/page.tsx` 2곳, `upload/page.tsx` 4곳)
- 커밋 `cb5d21c`(구 `1c6a05e`): 선행작업②③④ 확인 완료 후 실제 삭제
  - ② `next.config.mjs` redirect 4줄 — 변경 없이 그대로 존치 확인
  - ③ `branch-profile/[branchId]/page.tsx:356,366` 링크도 `/erp/diet`로 교체
    (redirect 홉 제거. 이 페이지 자체는 삭제 대상 아니고 계속 살아있음)
  - ④ 삭제 직전 `curl -I`로 4개 redirect 경로 실측 — 전부 307, location 헤더 정확
  - **삭제**: `app/board/admin/diet-automation/{page.tsx,history/page.tsx,review/page.tsx,upload/page.tsx}`
- 커밋 `0ed8728`(구 `ffff5c8`): HANDOFF 갱신 (솔아 세션분)
- **로컬 PC 최종 검증 (2026-08-11)**:
  - `npm run build` — **정상 통과**. `/erp/diet`, `/erp/upload`, `/erp/history`, `/erp/review`
    등 전 라우트 빌드 성공, 삭제된 `/board/admin/diet-automation/*`는 산출물에서 제거 확인
  - 브라우저 확장(Claude in Chrome) 미설치 상태라 `next dev` 기동 후 curl로 대체 검증:
    - `/erp/diet`·`/erp/upload`·`/erp/history`·`/erp/review` → 404 없음, 미인증 시
      `/erp/login?next=...`로 307 (보호 라우트 정상 동작)
    - 구 URL 4종(`/board/admin/diet-automation`, `.../review`, `.../upload`, `.../history`)
      → 각각 대응하는 `/erp/*`로 307 리다이렉트 정상 확인
  - **⚠️ 로그인 후 화면 렌더 자체(실제 데이터 표시 등)는 브라우저 미검증** — 라우팅/빌드
    레벨 확인만 완료. 다음에 실사용 중 이상 있으면 보고
  - `git push origin master` 완료 (`a99c1b7..0ed8728`)

---

## (이력, 참고용) 정리 전 조사 메모

### 근거

- redirect 4개 ↔ board 파일 4개 **1:1 완전 대응, 누락 경로 없음**
  (board 트리에 하위 `[id]` 등 동적 경로 없음)
- **board 전용 API 호출 0건** — board만 부르는 백엔드 기능 없음
- board 전용 식별자 3개 전부 무해:
  - `pptxWeekNum`(주차 드롭다운) = **死 UI**. 선택해도 요청 본문에 안 실림
  - `fmt`(파일형식 뱃지) = erp에 `PDF_JPG_CODES` 방식으로 **재구현됨**
  - `progressSteps` = erp에 **4단계 워크플로우로 상위 구현 존재**
- 화면별 비교: **history 완전 동일**, upload diff 4줄, review는 **erp가 387줄 더 길고
  구조적 차이 0건**
- import하는 컴포넌트/상수는 다른 곳에서도 쓰므로 **고아 안 됨**

### 선행 작업 (삭제 전 필수)

1. **erp → board 역참조 11곳을 erp 경로로 교체 (최우선)**
   - `diet/page.tsx:666`(router.push), `883`(href)
   - `history/page.tsx:72`, `99`, `124`(hubHref 템플릿)
   - `review/page.tsx:2109`, `2145`(Link)
   - `upload/page.tsx:113`, `140`, `153`, `330`
   - ※ 파일 삭제만으로는 안 깨짐(redirect가 파일시스템 라우팅보다 먼저 평가됨).
     다만 불필요한 홉이 남고, **나중에 redirect를 걷어내면 전부 깨짐**
2. **`next.config.mjs` redirect 4줄은 유지** (외부 북마크·기존 이메일 링크 대비)
3. `app/board/admin/diet/branch-profile/[branchId]/page.tsx:356,366` 링크도 교체 검토
   (이 페이지는 **삭제 대상 아님**, 계속 살아있음)
4. 삭제 직전 `curl -I http://localhost:3000/board/admin/diet-automation` 로
   리다이렉트 실제 발화 실측

### 교훈

이 중복 구조 때문에 **실제 사고가 발생했음** — 위 비상배포 권한 버그(`7bf0a90`)가
**"살아있는 erp를 두고 죽은 board를 고쳐서"** 생긴 것. 오늘 사이드바·연도 수정 때도
매번 board가 따라붙었음.

---

## 식단표 자동화 — 디자이너 양식 전수 조사 (2026-08-11)

**조사만 한 것. 코드 변경 0건.** 근거와 재현 방법을 아래 남김.

### 조사 자료

- 디자이너(윤수경) 제공 **2026년 1~8월 양식 9개** (8월은 방학O/X 2종)
- 저장소 `pptx-server/templates/` 원별 47개 + 공용 1개 (6월분)
- 코드: `app_actions.py`, `validate_template.py`, `pptx_generator.py`, `gen_form.py`,
  `app/api/diet-automation/upload/route.ts`, `app/api/board/diet/templates/route.ts`

---

### ★발견① 폴백이 문지기를 무력화 — 디자이너 양식이 실제로 안 쓰이고 있음

**증상**: 디자이너 양식을 템플릿 관리에 올려도, PPTX 생성은 **로컬 6월 양식**으로 나감.
화면에 아무 표시 없이 조용히 대체됨.

**원인 (파일:줄번호)**
- `pptx-server/app_actions.py:333` `_resolve_template_path()`
  → 업로드본 검증 실패 시 `return TEMPLATE_PATH, '로컬(업로드검증실패: ...)'` (369행)
  → **로컬 6월 양식으로 조용히 교체**
- `pptx-server/app_actions.py:388~402` 문지기 블록
  → 검증 대상이 이미 로컬로 바뀐 뒤라 **항상 통과**, `sys.exit(1)`이 안 걸림
- `pptx-server/app_actions.py:41` `TEMPLATE_PATH = '.../2026년 6월 식단표(양식).pptx'`

**즉 문지기 앞에서 불량품이 정품으로 바꿔치기되어 문지기가 볼 게 없음.**
2026-06-23 설계 문서의 "이름표 누락 시 → 거부 + 재업로드 안내"가
실제로는 "누락 시 → 6월 양식으로 조용히 대체"로 구현됨.

**검증 결과 (실측)**: 디자이너 양식 9개 **전부 검증 실패**
```
1월~8월 9개 파일 × 전 슬라이드
누락 = ['MENU_TABLE','ALLERGY_BOX','ORIGIN_BOX','MATERIAL_BOX']  (4개 전부)
컬럼 = None  (MENU_TABLE을 못 찾아 표 구조 검사 자체 불가)
```
저장소 원별 템플릿 48개도 동일 — **이름표 부여된 것은 공용 양식 1개뿐(1/48)**.

**왜 아직 사고가 안 났나**: 현재 49개 배포본은 **수동 제작** 중이라 이 경로를 안 탐.
**자동 생성으로 전환하는 순간 터지는 지뢰.**

**재현 방법**
```bash
cd pptx-server
python3 -c "
from validate_template import validate_template
r=validate_template(open('<디자이너양식.pptx>','rb').read())
print(r['summary']); print(r['slides'][0]['missing'])
"
```

---

### ★발견② 디자이너가 박스를 이미 "그 달 빈칸"에 배치해서 줌

원산지 박스 top 좌표 실측:

| 양식 | ORIGIN top | 위치 |
|---|---|---|
| 6월 | 8,571,159 | 표 **하단** (5주차 빈칸) |
| **7월** | **1,304,882** | 표 **상단** (1주차 월·화 빈칸) |
| 8월 | 8,427,444 | 표 **하단** (5주차 빈칸) |

달력 규칙과 정확히 일치:
- 6월(6/1=월) → 5주차 수·목·금 빈칸 → 박스 하단
- 7월(7/1=수) → 1주차 월·화 빈칸, 5주차 꽉 참 → 박스 **상단**
- 8월(8/1=토) → 5주차 화~금 빈칸 → 박스 하단

**결론: 박스 위치를 코드가 계산할 필요 없음. 양식 그대로 쓰면 이미 제자리.**

**따라서 `pptx_generator.py:1187 _fix_allergy_only()`의 박스 이동 로직은 불필요.**
이 로직이 존재하는 이유는 발견①(6월 양식 강제 사용) 때문에 생긴 군더더기.
해당 함수의 하드코딩(전부 6월 실측값 기준):
```
ORIGIN_Y = 8_395_899          # 6월 절대좌표
BOX_DOWN_15 = 175_260         # 15행이면 그냥 이만큼 내림
ALLERGY_BOTTOM_MARGIN = 532946
SLIDE_HEIGHT = 10680700
'그림 3','그림 224','그림 193'  # 클립보드 아이콘 이름 나열
```
→ 발견① 해결 후 **제거 대상**. 단, 제거 전 실물 대조 필수.

---

### ★발견③ 방학 그림도 양식에 포함 — 8월은 방학O/X 2종

| 양식 | 방학 그림 | 위치 | 그림 수 |
|---|---|---|---|
| 7월 | `그림 25` + TextBox "여름방학" | top 8,318,736 (하단=5주차) | 5개 |
| 8월(방학O) | `그림 26` + TextBox "여름방학" | top 1,161,913 (상단=1주차) | 5개 |
| 8월(방학X) | **없음** | — | 4개 |

**운영 흐름 (대표 확인 완료)**
```
영양기획팀 → 디자이너에게 그 달 원별 방학 일정 전달
디자이너   → 방학O / 방학X 두 버전 제작
영양기획팀 → 원별로 O(방학 있음) / X(방학 없음) 골라 내용 채움
```

**결론: 방학 그림을 코드가 삽입·크기조정할 필요 없음. 원별로 양식만 선택하면 됨.**

**방학 종류 (대표 확인)**
- 여름(7·8월), 겨울 → O/X 양식 제공됨
- **봄 → CK엔 거의 없음. 양식 따로 안 받음. 고려 대상 아님**
- 겨울 O/X 형태는 12·1월 양식 나올 때 확인 필요 (미확인)

**주의**: 텍스트 탐색 시 `여름방학`이 아니라 **`방학`**으로 찾을 것.
겨울은 `겨울방학`으로 오므로 `여름방학` 하드코딩하면 깨짐.

---

### ★발견④ 도형 이름은 매번 바뀌지만 텍스트는 8개월 내내 불변

같은 8월 양식 안에서도 슬라이드마다 이름이 다름:
```
slide1: '그림 3'   / 'TextBox 8'   / 'TextBox 10'
slide2: '그림 169' / 'TextBox 170' / 'TextBox 171'
slide3: '그림 228' / 'TextBox 170' / 'TextBox 171'
```

반면 **텍스트는 1~8월 내내 동일**:
| 대상 | 불변 텍스트 |
|---|---|
| ORIGIN_BOX | `원산지 표기` |
| MATERIAL_BOX | `* 원재료 표시안내 *` |
| ALLERGY_BOX | `<알레르기 표시 안내>` (그룹 내부 FREEFORM) |
| 방학 | `여름방학` / `겨울방학` → **`방학`으로 탐색** |

**결론: 이름표 자동 부착은 도형 이름이 아니라 텍스트 기준으로 탐색해야 함.**

**주의**: `ALLERGY_BOX`는 **그룹(grpSp) 안에 로고와 함께** 들어 있음.
`python-pptx`로 `slide.shapes`만 순회하면 못 찾음 → 그룹 내부까지 재귀 필요.
(코드는 이미 대응됨: `pptx_generator.py:907` "ALLERGY_BOX는 그룹이라 grpSp까지 봐야 함")

> **이전 세션 오류 정정**: "8월 배포본 57개 전부 알레르기 박스 없음"은
> 분석 스크립트가 그룹 내부를 안 본 탓. **실제로는 48개 템플릿 전부 존재.**
> 이 항목은 미해결 목록에서 삭제할 것.

---

### 발견⑤ 양식 구조는 4월부터 정착

| 달 | 슬라이드 | 표 행수 | 원재료·알레르기 |
|---|---|---|---|
| 1·2월 | 5장 | 9~15행 | ❌ 없음 |
| 3월 | 2장 | 16행/10행 | ❌ 없음 |
| **4~8월** | **3장** | **15행/10행/5행** | ✅ 있음 |

슬라이드 3장의 정체 (표 1열 라벨로 확인):
- **15행** = 1주~5주 × (주/오전/오후) → 오전+오후 간식 원
- **10행** = 1주~5주 × (주/오전) → 오전 간식만
- **5행** = 1주~5주 → 간식 없이 중식만

**결론: 1~3월 호환 불필요. 4월 이후 구조를 기준으로 설계.**
슬라이드 크기는 9개 전부 동일 `7556500 x 10680700`.

---

## 이 조사로 인한 작업 재정의

### 불필요해진 것 (기존 계획에서 삭제)

| 기존 계획 | 삭제 사유 |
|---|---|
| 엑셀 v6에 "박스 위치 표시 칸" 신설 | 발견② — 디자이너가 이미 배치 |
| 박스 위치 자동 계산 로직 신설 | 발견② — 동일 |
| 방학 그림 삽입·크기조정 기능 | 발견③ — 양식에 이미 포함 |
| 엑셀에 원별 방학 기간 입력칸 | 발견③ — 원별 양식 O/X 선택으로 대체 |
| 알레르기 박스 실종 원인 규명 | 발견④ — 분석 오류였음, 문제 없음 |

### 신설·수정해야 할 것 (우선순위순)

**1. 템플릿 업로드 시 이름표 자동 부착** ← 최선행
- 위치: `app/api/board/diet/templates/route.ts` (POST)
- 현재: theme.xml에서 색·폰트만 추출, PPTX 내용은 손대지 않음
- 변경: 텍스트 기준(발견④ 표)으로 4개 도형 탐색 → `cNvPr name` 부여 → 저장
- 참고: `pptx-server/_apply_template_names.py`에 탐색 로직 이미 존재, 이식하면 됨
- 이유: 디자이너에게 이름표를 요구하는 방식은 **실제로 지켜지지 않음(0/9)**.
  사람 약속이 아니라 코드가 보장하는 구조로 전환.
- 확장: `VACATION_IMG`도 함께 부여하면 발견③ 토대 확보

**2. 폴백 시 화면 경고**
- 위치: `pptx-server/app_actions.py:369` + ERP 템플릿 관리 화면
- 현재: 로그에만 한 줄, 화면 표시 없음
- 변경: 폴백 발생 시 DB 기록 → 템플릿 관리 화면에 경고 배지
- **더 나은 방향**: 업로드 시점(1번)에 막으면 폴백 자체가 안 일어남.
  1번 완료 후에도 안전판으로 유지.

**3. `_fix_allergy_only` 박스 이동 로직 제거**
- 위치: `pptx_generator.py:1187`
- 조건: 1번 완료 후 (디자이너 양식이 실제로 쓰이게 된 뒤)
- 검증: 제거 전후 PowerPoint 실물 대조 필수

**4. 원별 방학 양식(O/X) 선택**
- 방학 있는 달에만 양식 2개가 올라옴
- 업로드 시 사람이 용도 지정 (파일명 의존 금지 — `여름방학O`/`겨울방학O` 등 변동)
- 원별로 O/X 지정 → `branch_profiles` 또는 월별 설정
- 방학 없는 달엔 UI 자체가 안 나타나게

**5. 위탁업장 `contract_type` 신설** (독립 작업, 작음)
- 현재: `permanent` / `temporary` 2종
- 추가: `consignment` (위탁업장 5곳 — 현장 직접 조리, CK 이동급식과 별개 사업)
- 처리는 크레오와 동일: 자동 생성 제외 + `BranchDietUploadCard`로 직접 업로드
- **★중요**: `branch_filters.py` / `pptx-eligibility.ts`가 현재
  **"temporary만 제외"** 방식 → `consignment` 빠뜨리면 위탁 5곳이 자동 생성에 섞임.
  **"permanent만 포함"으로 뒤집을 것.** 새 유형 추가 시 자동 제외되는 방향이 안전.
- 시점: 위탁 5곳 **등록 전에** 하면 SQL 한 줄, 등록 후엔 데이터 정리 작업

---

## 미확인 / 다음에 확인할 것

1. **7·8월에 디자이너 양식을 템플릿 관리에 실제로 업로드했는지**
   → 했다면 발견①이 이미 발생 중. 안 했다면 아직 지뢰 상태.
   확인: ERP 템플릿 관리 화면의 등록 이력
2. **겨울방학 양식이 O/X 2종으로 오는지** (12·1월 양식 나올 때)
3. **5행 슬라이드(중식만)를 실제로 쓰는 원이 있는지**
   → 8월 배포본 57개 실측에선 10행·15행만 나왔음
4. 위탁업장 5곳 원 정보 (등록 필요)

## 이번 조사에서 건드리지 않기로 한 것

- **원별 참조표를 DB에서 생성**: 현재 `gen_form.py`는 기준폼 스냅샷을 그대로 복사해
  DB와 어긋남(크레오 3원이 참조표에 없음). 다만 크레오는 `temporary`라 파이프라인
  제외 상태라 **당장 문제 없음**. 나중 과제로 기록만 함.
- **업로드 API의 원별 참조표 파싱**: 현재 시트 존재만 확인(`route.ts:23`),
  파싱은 주차 시트 5개 + 도시락 시트만(`route.ts:527~534`).
  발견③으로 방학 입력칸이 불필요해져 **지금은 파싱 신설 불필요**.

---

## 이름표 자동부여 (발견① 해결) — 단계1~3 진행 중 (2026-08-11)

`프롬프트_이름표_자동부여.md` 지시대로 단계별 진행. 각 단계 사용자 승인 후 다음 단계.

- **단계1 완료** — `pptx-server/template_namer.py` 신설(커밋 `4e841c4`). 텍스트 기준(도형
  이름 아님)으로 4개 도형 탐색. python-pptx(lxml) 기반 — ElementTree 재작성 방식
  (`_apply_template_names.py`)은 네임스페이스 훼손 확인되어 이식 안 함.
  저장소 templates/*.pptx 48개 검증: 47/48 통과(유일 실패는 목동POLY의 메뉴와 무관한
  부록 슬라이드 — 정상 동작), 콘텐츠 바이트 손실 없음(미디어 SHA-256 일치 확인).
- **단계2 완료** — 실제 디자이너 8월(방학O) 원본으로 이름표 부여 + 강동ECC 생성까지
  실물 PowerPoint 확인 완료(레이아웃 정상). ⚠️ **디자이너 원본 파일 자체와 산출물은
  커밋 금지** — `.gitignore`에 `/*.pptx`, `/_verify_output/` 추가해둠(커밋 `2c4b641`).
  - **발견(신규, 미해결)**: 생성 중 `remove_holiday_cover_pics()`(공휴일 셀을 가리는
    장식 그림 자동 제거 로직, `pptx_generator.py:788`)가 방학 그림("그림 26", 발견③의
    `VACATION_IMG` 후보)까지 함께 지울 수 있음이 확인됨. 테스트에 쓴 6월 픽스처
    데이터의 공휴일 셀 좌표가 우연히 방학 그림과 겹쳐서 걸린 것 — 실제 8월 운영에서도
    진짜 공휴일 셀이 방학 그림과 겹치면 똑같이 지워질 수 있음. **아래 "④ 원별 방학
    양식(O/X) 선택" 작업 때 `VACATION_IMG` 이름표를 부여하고, `remove_holiday_cover_pics`
    제거 대상에서 `sh.name == 'VACATION_IMG'`인 경우 제외하도록 반영 필요.**
- **단계3 완료** — `app_actions.py`(GitHub Actions)와 `app.py`(Render) 둘 다 연결.
  `_resolve_template_path()`에 이름표 자동 부여를 다운로드 직후·검증 직전에 연결.
  사전 검토 중 발견해 함께 반영한 5가지:
  1. **연/월 매칭 추가** — `diet_templates.is_active`는 테이블 전체 1개만 허용되는
     전역 플래그라, 필터 없이 `rows[0]`을 쓰면 "8월 생성인데 7월 템플릿이 active"
     상황을 그대로 썼음. `filters={..., year, month}` 추가로 해결.
  2. **active 다중 시 순서 보장** — `supabase_uploader.SupabaseREST.select()`에
     `order`/`limit` 파라미터 신설, `created_at.desc`로 최신 것만 사용.
  3. **임시 템플릿 파일 정리** — `main()`/요청 종료 시 `finally`에서 정리.
     GH Actions는 프로세스 단위라 원래 안전했지만, **app.py는 Render gunicorn
     워커가 장기 실행되므로 정리 안 하면 요청마다 임시파일이 실제로 누적됨**
     — Flask test_client로 요청 종료 후 파일이 실제 삭제되는지까지 확인함.
  4. **이름표 부여 결과 로그 출력** — 슬라이드별 부여/누락/경고를 그대로 출력.
  5. **폴백 시 `template_logs` 기록** — 다운로드 실패/이름표 부여 예외/검증 실패
     3가지를 `template_id` + `detail`(JSONB)로 구분 기록. 업로드 자체가 없는
     정상 상황은 기록 안 함. (신규 마이그레이션 불필요 — `template_logs.action`
     CHECK에 `'validate_fail'`이 2026-06-23 마이그레이션에 이미 있었음)
  - **공용화**: 로직을 `app_actions.py`/`app.py` 양쪽에 복붙하면 나중에 한쪽만
    고치는 사고가 난다(board/erp 중복본 정리 때 겪은 것과 동일 패턴) →
    `pptx-server/template_resolver.py`로 추출, 양쪽 다 이 모듈만 호출.
    (`resolve_template_path`/`log_validate_fail`/`cleanup_template_path`)
  - 검증: `app_actions.py`는 client(select/download_file/insert) 몽키패치로 6개
    시나리오(정상 사용/연월불일치/다중active/파싱실패/다운로드실패/컬럼불일치) 전부
    통과. `app.py`는 Flask test_client로 `/generate-from-json` 실제 호출 —
    "최종 사용 템플릿"이 업로드본으로 찍히는 것 + **요청 종료 후 임시 템플릿
    파일이 실제로 삭제되는 것**(장기 실행 프로세스 핵심 요구사항)까지 확인.
  - 커밋: `b14b329`(app_actions.py 최초 연결) → `67a5211`(공용 모듈 분리) →
    `79692d2`(app.py 이식, `/generate`·`/generate-from-json` 둘 다)
  - ⚠️ **`diet_templates.is_active`가 전역 1개 플래그라는 제약이 다음 항목(④ 원별
    방학 O/X 선택)의 전제를 깬다** — 자세한 내용은 아래 "④ 원별 방학 양식(O/X)
    선택" 항목 참고.
- **단계4(선택, 마지막)** — 업로드 시점 거부. `app/api/board/diet/templates/route.ts`
  POST가 **이미 부분 구현되어 있음**(JSZip으로 cNvPr name 4개 확인, `validation_result`
  DB 저장) 발견. 다만 (a) 실패해도 업로드는 그냥 진행(거부 없음), (b) 텍스트가 아니라
  "이미 이름이 붙어있는지"만 봐서 디자이너 원본은 지금도 무조건 "실패"로 뜸. "신설"이
  아니라 "기존 로직 업그레이드"로 재정의 필요 — 착수 시점에 다시 결정.
- **별도로 발견, 이번엔 손 안 댐**: 로컬 폴백 원본(`TEMPLATE_PATH`, 6월 공용 양식)
  자체가 예전 `_apply_template_names.py`(ElementTree 방식)로 이미 한 번 손댄 이력
  있음(`.bak` 파일 존재, 2026-06-23). 크기 차이가 -251KB로 예상보다 훨씬 큼(이미지
  재압축 가능성 높지만 미확인). 시간 될 때 실물로 한 번 열어볼 것.

---

## `_fix_allergy_only` 박스 이동 로직 제거 — ✅ 완료·실물 확인 통과 (커밋 `b0ec666`)

**제거한 것**
- `ALLERGY_BOX` 동적 배치
- `ORIGIN_BOX`/`MATERIAL_BOX` 강제 이동 (15행 `+175,260` 하향 / 10행 `8,395,899` 고정)
- 클립보드 아이콘 위치 보정 (`ORIGIN_BOX` 기준 정렬이라 기준점 소멸 → 함께 제거,
  단 리사이즈는 유지)

**남긴 것** (이번 범위 밖, 실측 근거 없음)
- 15행 행높이 정규화 + `graphicFrame` 크기 동기화
- 박스 폰트 크기 보정
- 로고 리사이즈 + 재배치

**좌표 검증** (8월 방학O 디자이너 원본 이름표부여본 + `test_data.json` 6월 픽스처)

| 원 | 박스 | 제거 전 | 제거 후 | 차이 |
|---|---|---|---|---|
| 강동E(15행·C) | ORIGIN | 8,603,277 | **8,428,017** | −175,260 |
| | MATERIAL | 9,400,463 | **9,225,203** | −175,260 |
| | ALLERGY | 10,150,203 | **9,900,789** | −249,414 |
| 목동E(10행·A) | ORIGIN | 8,395,899 | **8,382,815** | −13,084 |
| | MATERIAL | 9,180,001 | 9,180,001 | 0 |
| | ALLERGY | 9,989,340 | **9,853,671** | −135,669 |

제거 후 좌표 = 디자이너 원본 그대로. 코드가 더 이상 안 건드림.

**★ 실물 대조로 제거가 옳다는 것 확인** (같은 원끼리 비교, 사용자 PowerPoint 확인 완료)

목동ECC 실제 배포본 vs 우리 생성물:

| | 표 bottom | 알레르기 top | 원재료↔알레르기 겹침 |
|---|---|---|---|
| **실물(정답)** | 10,107,376 | 9,900,789 | **+52,410** (0.057in) |
| 우리 AFTER | 9,959,340 | 9,853,671 | +34,216 (0.037in) |
| 우리 BEFORE | 9,959,340 | 9,989,340 | −101,453 (안 겹침) |

- **실물도 겹친다. 우리보다 더 많이.** 그 상태로 49개 원에 배포되어 왔고 문제 없었음
  → 이 정도 겹침은 텍스트 내부 여백에 묻혀 화면상 무해
- **BEFORE는 실물과 방향이 반대**였음. 실물은 알레르기가 표를 파고드는데
  (표bottom 대비 −206,587) BEFORE는 표 아래로 밀어냈음(+30,000)
  → 밀어내던 코드가 오히려 실물과 다르게 만들고 있었다

강동ECC 실물도 동일 경향(원재료↔알레르기 겹침 +78,020 = 0.085in).

---

## 표 높이 실물 맞추기 — ✅ 완료 (커밋 `4549fe7`, 2026-08-12)

**작업 시작 전 커밋**: `223a5ecd7027a4975ad6ea1ef84e09c3bc8b500d` (롤백 지점).
`_fix_allergy_only`는 `b0ec666`에서 이미 한 차례 축소된 함수 — 이번 변경으로
문제 생기면 `4549fe7` 전/후로 되돌릴 것.

**근본 원인 (확정)**: 영양사는 행 높이를 지정하지 않는다. 기준 엑셀 식단표에서
중식·오전/오후 간식 칸을 드래그 복사해 디자이너 PPTX 표에 붙여넣는 방식이며,
그 과정에서 엑셀 쪽 행 높이가 딸려온다(영양사 실제 편집 과정 확인, 2026-08-12).
즉 배포본의 행 높이는 사양이 아니라 복사-붙여넣기의 부산물이고, 그 달 기준
엑셀에 따라 매달 달라진다 — **6·7·8월 세 달 연속 확인**:

| 달 | 15행 실물 본식/간식 h | 비고 |
|---|---|---|
| 6월 | 1,180,800 / 306,000 | 옛 `_H_LUNCH_15`/`_H_SNACK_15`와 동일값. 디자이너 원본 간식(262,508)과도 다름 |
| 7월 | 디자이너 원본 값 그대로(제각각) | 실물=원본 100% 일치 |
| 8월 | 1,152,000 균일 | 6월과도, 디자이너 원본과도 다름 |

**코드가 재현해야 할 대상이 아니다.**

**이전 코드의 잘못 (정정됨 — 최초 진단 오류 있었음)**: `_H_LUNCH_15=1,180,800` /
`_H_SNACK_15=306,000`을 15행 전 원·전 달에 강제 적용하고 있었음. 이 값의
출처인 `pptx-server/templates/(강동ECC).pptx`는 **최초엔 "오염 fixture(우리
출력물을 정답으로 오인한 순환 참조)"로 오판했으나, `_samples/26년 6월
식단표/.../강동ECC.pptx`(6월 실제 배포본)와 SHA-256 완전 일치(`094b28876f...`)로
확인되어 정정함 — 이 파일은 진짜 6월 정답본이다.** 잘못의 성격은 "오염
fixture 역산"이 아니라 **"6월 한 달의 실측값을 영구 상수로 굳혀 7·8월 등
다른 달 생성에도 그대로 적용한 것"**이었다.

**★ `pptx-server/templates/*.pptx` 48개 전수 SHA-256 검증 완료 (2026-08-12) —
"오염 의심"에서 "6월 배포본 스냅샷"으로 성격 재확정**

`_samples/26년 6월 식단표` 하위에서 같은 원 이름 파일을 찾아 SHA-256 대조:

| 분류 | 개수 | 내용 |
|---|---|---|
| ① 6월 배포본과 해시 완전 일치 | **47/48** | 원별 템플릿 47개 전부(강동ECC·목동ECC 포함) |
| ② 6월 폴더에 대응 파일 없음 | 0/48 | — |
| ③ 대응 파일은 있으나 해시 불일치 | 1/48 | 공용 양식 `2026년 6월 식단표(양식).pptx` |

**③ 상세**: 공용 양식 파일만 해시가 다름(templates판 1,513,035 bytes vs
6월샘플판 1,764,799 bytes, 정확히 −251,764 bytes). 그러나 **표 기하(top/
행별 h/ext.cy)는 15행·10행·5행 슬라이드 전부 두 버전이 완전히 동일** —
차이는 표 구조가 아니라 다른 부분(이미지 재압축 등)에서 발생. 이는 위
"이름표 자동부여" 섹션에 이미 기록된 `_apply_template_names.py`(ElementTree
방식, 2026-06-23)의 재처리 이력과 정확히 일치하는 크기 차이로, 신규
이상 징후가 아니라 기존에 알려진 사실의 재확인.

**결론**: `pptx-server/templates/*.pptx`는 오염된 우리 산출물이 아니라
**원별 47개 + 공용 1개 = 48개 전부 6월 실제 배포본(스냅샷)**이다. 단,
**성격이 무엇이든 이 값들은 6월 한 달의 실측값이므로 다른 달(7·8월 등)
생성 결과의 정답 근거로 쓰지 말 것** — 이번 조사(위 표)로 6월 자체 값도
디자이너 원본과 다르고(간식 306,000 vs 원본 262,508), 7·8월과도 다르다는
것이 재확인되었으므로 "달마다 다르다"는 결론과 이 경고는 그대로 유지된다.

**수정**: `_fix_allergy_only()` 15행 블록에서 행 높이 강제 재설정과
`graphicFrame` ext.cy 재계산을 통째로 제거. 디자이너 원본의 `a:tr/@h`, ext.cy를
그대로 통과시킨다. 죽은 함수 `_fix_row_heights()`(호출부 0건, 리포 전체 확인)와
그 안에서만 쓰이던 `_H_LUNCH`/`_H_SNACK`/`_H_LAST`, 미사용 import `Emu`도 함께 삭제.

**10행 조사 결과** (같은 방식으로 실측 — 10행은 코드가 원래도 안 건드리는
영역이라 코드 변경은 없음, 15행 결론과 같은 양상인지 확인용):
- 7월: 실물 10행 본식·간식이 디자이너 원본과 **표별 1:1 완전 일치**(25/25).
  15행 7월과 동일 양상(원본 보존).
- 8월: 간식은 원본 그대로(25/26, 1건은 900,000 균일 — 아래 미해결 항목 참고),
  **본식만** 1,368,000(=3.8cm)으로 균일화(125/130). 디자이너 원본 본식 5값
  중 어느 것과도 일치하지 않음. 15행 8월(1,152,000 균일화)과 동일 양상 —
  간식 보존·본식만 균일화.

**검증** (7월 디자이너 원본 → 광명POLY(15행, 강동ECC 아님) 생성 / 8월 방학X
원본 → 목동ECC(10행)+광명POLY(15행) 생성, `_verify_output/`에 결과물 보관):
- 7월: 생성물이 원본과 표 top(1,156,203)/left(194,988)/ext.cx(7,166,525)/
  행별 h 15개 전부/ext.cy(8,878,415, 원본=생성물 기준) **완전 일치**.
  참고: 실물 쪽 ext.cy(9,015,527)는 원본·생성물과도 다르고 자기 자신의
  sum(h)(8,878,415)와도 다름 — 아래 "ext.cy 검증 기준" 항목 참고, 재현 대상 아님.
  원산지 top 1,304,882 / 원재료 top 2,184,883 / 알레르기 top 9,900,789도
  원본=생성물 일치(참고로 실물은 오히려 원본에서 벗어나 있음: 원산지 −68,580,
  원재료 +38,100 — 영양사 수작업 흔적, 재현 대상 아님).
- 8월 방학X: 목동ECC(10행)·광명POLY(15행) 둘 다 원본과 표 기하(top/left/
  ext.cx/ext.cy/행별 h) 완전 일치 — 코드가 방학X 원본을 전혀 안 건드림 확인.

**★ ext.cy를 향후 검증 기준에서 제외할 것**: `a:tr/@h`는 고정 높이가 아니라
최소 높이. 실물 15행 29/29건이 `sum(h) != ext.cy`인 것도 PowerPoint가 표
편집 후 ext.cy를 갱신하지 않고 행 높이 기준으로 렌더링하기 때문 — 실물
자체가 자기 안에서 안 맞는 값이다. **필수 일치 항목은 top/left/행별 h 전부/
sum(h)뿐. ext.cy는 "디자이너 원본 == 생성물"만 확인하고 실물과의 일치는
요구하지 말 것.** (이번 세션에 검증 기준을 "ext.cy 전부 일치"로 잘못 세워
한 차례 중단이 발생했었음 — 기준 자체가 부정확했던 것으로 정정됨.)

**⚠️ 미검증 항목 (다음 세션 확인 필요) — "겹침 육안 확인 완료"라고 쓰지 말 것**:
검증용 생성물에 실제 업로드 엑셀(`키즈밀_식단표_26_07_v6.xlsx`,
`_26_08_v6.xlsx`)을 연결했으나 **메뉴 텍스트가 0건 삽입**됨(채워진 셀은
날짜 숫자·주차/오전/오후 라벨뿐). `read_excel.load_excel()` 결과를
`pptx_generator.generate()`에 바로 넣으면 이렇게 되고, 6월 픽스처
(`test_data.json`)로는 정상 삽입됨 — 차이 원인 미확인(아래 "기타 미해결"
항목 참고). 따라서 **"표가 내용으로 최대한 늘어난 상태의 박스 겹침"은
이번에도 검증 못 함.** PowerPoint 육안 확인(버전 16.0.20228.20158,
Microsoft 365 Click-to-Run)은 표 기하·박스 위치 일치만 확인했고, 겹침
스트레스 테스트는 여전히 미검증 상태.

---

## 신규 발견 — 날짜 표기 방식이 실물과 정반대 (미해결)

| | 날짜를 어디에 |
|---|---|
| **실물 (사람이 만든 것)** | 표 위에 떠 있는 **텍스트박스 유지** (`03` `04` … `31`, 19개) |
| **우리 코드** | 그 텍스트박스를 **전부 제거**하고 표 셀 안에 삽입 (`1⏎흑미밥…`) |

`replace_date_group()`이 날짜 텍스트박스를 비운다. 어느 쪽이 맞는지 결정 후 통일 필요.
실물이 정답이라면 우리 방식을 바꿔야 함.

---

## ✅ PDF/JPG 변환 신설 — **완료·검증 종료** (2026-08-14 착수 → 2026-08-18 최종 검증)

**계기**: "실제 배포되는 PDF는 왜 완전하냐"는 질문에서 출발한 조사 (claude.ai 세션).
위 701행 "LibreOffice 렌더링 신뢰 안 함 원칙 재검토"보다 구체적인 실무 갭.

**발견했던 문제 (요약)**:
1. `app_actions.py`(GitHub Actions)·`app.py`(Render) 둘 다 PDF 변환 호출 전무.
   `render.yaml`은 순수 Python이라 Render에 LibreOffice 자체가 안 깔림.
   `read_excel.py`의 `convert_pptx()`는 로컬 전용 죽은 코드였음. 지금 실제
   배포되는 PDF가 완전한 이유는 49개 파일이 아직 100% 수동 제작(영양사가
   PowerPoint로 직접 만들어 PDF로 내보내기)이기 때문 — 자동화가 사람 손을
   실제로 대체하는 순간 이 갭이 터질 지뢰였음.
2. `deploy/route.ts:210`에 `pptx_url`을 PDF로 속여 보내던 지뢰
   (`diet_review_items`에 `pdf_url` 컬럼 자체가 없었음).

**조치 (완료)**:
- `.github/workflows/generate-pptx.yml`: `libreoffice-impress` 설치 스텝
  추가(GitHub Actions 선택 — Render는 메모리 512MB라 위험). timeout
  30분→45분 확대(변환 추가분 감안).
- `app_actions.py`: `read_excel.convert_pptx()` 재사용해 메인 루프에 통합.
  `upload_pptx()`→`upload_file()`로 일반화(확장자별 mime 자동판별).
  `cfg['file_fmt']`(PDF/JPG/PDF+JPG/PPT) 기준으로 변환·업로드해
  `weekly_menus.pdf_url`, `diet_review_items.pdf_url`/`jpg_url`에 실제로
  기록. LibreOffice 미설치 감지 시 1개 원에서만 확인하고 이후 48개는
  변환 시도 자체를 건너뜀(PPTX 생성은 정상 진행).
- `supabase/migrations/add_pdf_url_to_diet_review_items.sql`: 신규
  마이그레이션(`diet_review_items.pdf_url` 컬럼 추가). `jpg_url`은 이미
  있었으나 채우는 코드가 없어 항상 NULL이었음. `weekly_menus.pdf_url`은
  기존부터 있던 컬럼이라 안 건드림.
- `deploy/route.ts`: 진짜 `pdf_url`이 있으면 그걸 쓰고, 재생성 전이라
  아직 없는 과거 행만 `pptx_url`로 폴백하되 라벨을 정직하게 "PPTX
  다운로드(PDF 변환 전)"로 표시.
- `diet-review/route.ts`: `ReviewItemRow` 타입에 `pdf_url` 추가(`select('*')`로
  이미 조회되고 있었으나 타입 선언 누락 상태였음).

**✅ 검증 완료 (2026-08-14, 실제 GitHub Actions 실행)**:
1. Supabase 마이그레이션 실행 완료 (SQL Editor에서 직접 실행, `Success. No rows returned`)
2. 실제 GitHub Actions 실행 검증 완료 — LibreOffice 설치 성공, 49개 원
   전부 성공(`[완료] 성공 49개 / 실패 0개 / 전체 49개`), 소요 4m 24s(45분
   여유 충분). 중간에 걸렸던 기존 버그 2건도 이 과정에서 발견·수정:
   - `app_actions.py`에 `validate_template` import 누락 → 수정(커밋 `1f2fcdb`)
   - **한글 CJK 폰트 자체가 GitHub Actions 러너에 없어서 PDF 텍스트가
     전부 깨져 나옴**('엘엘엘', '쌀쌀쌀' 등 엉뚱한 글자) → `fonts-noto-cjk`
     설치로 해결(커밋 `5d4950b`). 로컬 샌드박스에서 생성→변환→PDF 렌더링을
     직접 재현해 정상 출력 확인 완료. "LibreOffice 렌더링 신뢰 안 함"
     원칙(701행)이 정확히 경고했던 상황이 실제로 터진 사례.

**✅ 2026-08-18 세션 — 타임아웃 사고 원인 파악·수정**:
지난 세션에서 한글 폰트 수정(`c875db6`) 반영 후 재실행한 #44가
**45분 타임아웃으로 강제 종료됨** 발견. 원인: `Generate PPTX` 스텝
자체는 2m16s만 썼고 나머지 43분을 전부 `fonts-noto-cjk`(61.2MB, 한중일
CJK 전체) 설치에 소모. 한글만 필요하므로 `fonts-nanum`(10.3MB, 한글
전용)으로 교체(커밋 `d5cecf1`) — 6배 작음. 로컬에서 Noto CJK 완전 제거
후 나눔고딕만 남긴 상태로 재현 검증, 한글 정상 렌더링 확인(품질 저하
없음). `actions/cache`로 apt 패키지 캐싱도 추가 — 두 번째 실행부터는
재다운로드 없이 설치되어 반복 실행 안정성 확보.

**✅ 2026-08-18 최종 검증 완료 — PDF/JPG 변환 작업 종결**:
`fonts-nanum` 교체 + apt 캐싱 반영 후 재실행(#45) 결과:
- **5m 10s만에 성공**(직전 #44는 45m 21s 타임아웃) — 폰트 크기 6배 축소
  효과 확인
- 생성된 PDF(수지P 지점, 2026-06) 실물 열어 확인 — **한글 완벽 정상**:
  제목("키즈밀 튼튼 식단"), 메뉴명("닭고기찹스테이크", "훈제오리볶음밥"),
  알레르기 번호(①②⑤⑥), 칼로리, 원산지 표기, 2페이지 간식표까지 전부 정상.
- 잔여 무해 경고: `Post Cache apt packages` 스텝에서 캐시 저장 실패
  (`Permission denied` — /var/cache/apt/archives 권한). 작업 자체는 성공하고
  다음 실행에서 캐시 혜택만 못 받는 정도. 우선순위 낮음, 나중에 정리.

**남은 확인 사항 (우선순위 낮음)**:
1. **폰트 스타일 차이 허용 여부 판단** — 텍스트는 정상이지만 디자이너
   원본 폰트(Pretendard)가 서버에 없어 나눔고딕으로 대체 렌더링됨.
   `_samples/26년 8월 배포 식단표/`의 영양사 실물과 나란히 놓고 글꼴 모양
   차이가 허용 가능한지 판단 필요. 허용 불가 시 Pretendard 폰트 파일을
   저장소에 넣고 설치하는 방식으로 전환.
2. 검토 화면(`/erp/review`)에서도 pptx_url을 PDF처럼 쓰는 곳 있으면 추가 점검
3. apt 캐시 저장 권한 문제 정리(위 무해 경고)

---

## ★ 권팀장 요청 8건 대응 (2026-08-18 크레오 온보딩 테스트 피드백)

크레오 온보딩 기능 테스트 가이드를 권팀장에게 전달 → 테스트 후 받은
요청사항 8건. 원문은 `크레오_온보딩_기능_테스트_가이드_260818_.doc`
마지막 페이지("사용자 페이지 요청사항").

| # | 요청 | 상태 | 커밋 |
|---|---|---|---|
| 1 | 문의 유형 재정의 + 제목·내용 자유기재 | ✅ 완료 | `643a37f` |
| 2 | 식단표 메뉴 → 파일보관함으로 확장 | ✅ 완료 | `67676c9` 외 4건 |
| 3 | PDF/PPTX/JPG 형식 표기 제거 | ✅ 완료 | 2번에 포함 |
| 4 | 팝업 공지 기능(예: 폭설로 배송 지연) | ✅ 완료 | `c6c321f` |
| 5 | 홈 하단을 식단표 대신 공지사항으로 | ✅ 완료 | `47efe02` |
| 6 | 문의 스테퍼 '처리중' 라벨 정렬 오류 | ✅ 완료 | `aa38d3f` |
| 7 | 발송 후 상대방 수신 전이면 메시지 수정 가능 | ✅ 완료 | `8ae0dbc` |
| 8 | 문의 내역 클릭 시 전체 대화에서 해당 지점으로 점프 | ✅ 완료(재정의) | `6537c58` 외 |

### 요청 8 — 재정의 경위 + 대화 내용 검색 (완료)

- 원래 문구("문의 내역 클릭 시 전체 대화에서 해당 지점으로 점프")가
  ERP 내부 관점으로 오해될 수 있어 권팀장에게 재확인 요청 → 실제
  의도는 "카톡 대화 검색처럼, 한 원의 과거 모든 문의의 대화 내용을
  검색해서 찾아주는 기능"이었음(제목만으론 특정 안 되는 경우가
  많았다는 피드백 — 이메일 제목이 "re: re: 광명폴리입니다" 등으로
  내용을 특정 못 하는 경우 다수)
- 검색 결과 클릭 시 이동은 "해당 문의로 이동"이면 충분함을 권팀장이
  확인(메시지 단위 스크롤까지는 불필요 — 카톡과 달리 문의 단위로
  대화방이 분리된 구조라는 차이 인지하고 진행)
- 착수 전 진단: messages 10건, pg_trgm 미설치 확인 → 이 규모에선
  별도 인덱스 없이 단순 ILIKE로 충분해서 스키마 변경 없이 진행
- 대화 내용 검색 3곳 구현:
  - ERP 메인 탭(`/erp/inquiries`): 기존 지점명·제목 검색을 대화
    내용까지 확장(300ms 디바운스)
  - ERP 이력 검색(`/erp/inquiries/history`): 마찬가지로 확장
  - 원 담당자 화면(`/board/inquiries`): 검색창 자체가 없어서 신설.
    이미 로드된 messages로 클라이언트에서 검색(추가 DB 조회 없음)
- 3곳 모두 대화 내용으로만 걸린 경우 매칭 부분 미리보기(💬) 표시
- 내부메모(`is_internal=true`)는 검색 대상 제외
- `InquiryCard`에 `matchPreview` 옵션 prop 추가(기존 사용처엔 영향 없음)
- 8-1(덤 요청): "3분 전" 같은 상대시간에 정확한 년/월/일/시간 추가.
  처음엔 호버 툴팁으로 했다가 "캡처가 번거롭다"는 피드백으로 상시
  노출로 변경. 원 담당자 목록(InquiryCard) + ERP 메인 탭 목록 +
  원 담당자 메시지 버블(MessageBubble, 관리자 쪽엔 이미 있었는데
  원 담당자 쪽만 빠져있던 것 발견해 통일) 총 3곳 반영
- 강동E 테스트 계정 + ERP 양쪽에서 "표시"(제목엔 없고 대화 내용에만
  있는 단어)로 실물 검색 확인 완료

### 요청 7 — 발송 후 미수신 메시지 수정·삭제 (완료)

- 읽음 기준은 기존 `unread_count_branch` 리셋과 동일하게 "원 담당자가 그
  대화방 페이지를 열었는지"로 통일(별도 정교한 열람 추적 인프라는 만들지
  않음 — 유대표 확인)
- `inquiries.branch_last_read_at` 컬럼 신설, 원 담당자가 대화방 여는
  시점에 기록
- `canEditDeleteMessage`: 그 시각 이후 온 관리자 메시지만 수정·삭제 가능.
  내부메모(`is_internal=true`)는 이 제한 예외
- 수정·삭제 버튼을 호버 대신 항상 표시로 변경(확인 편의)
- 원 담당자 화면에 `messages` UPDATE/DELETE 실시간 리스너 신설
- 이미 읽힌 메시지엔 인디고 톤 "✓ 읽음" 배지 표시
- DB 단 보강 완료: `messages` 테이블에 RESTRICTIVE 정책 2개 추가
  (`messages_block_edit_after_read`, `messages_block_delete_after_read`)
  — 관리자가 API를 직접 호출해도 이미 읽은 메시지는 수정·삭제 불가.
  기존 3개 정책(`messages_update_admin/read`, `messages_delete_admin`)은
  안 건드림.
- 착수 전 진단(`pg_policies` 실측)에서 `messages_update_read`라는 정책이
  이름과 달리 "읽음"과 무관(원 담당자 본인 메시지 수정 허용용)임을
  확인 — 이름만 보고 오해하지 않도록 기록.
- RLS 테스트: `SET LOCAL ROLE authenticated` + `request.jwt.claims`로
  관리자 가장 후 이미 읽은 메시지 UPDATE 직접 시도 → content 안 바뀜
  확인(`BEGIN`~`ROLLBACK`로 실제 반영은 안 되게 진행)
- 마이그레이션
  `supabase/migrations/restrict_message_edit_after_read_260819.sql`,
  Supabase에서 실행 완료
- 마이그레이션 `supabase/migrations/add_branch_last_read_at_260818.sql`,
  Supabase에서 실행 완료
- 강동E 테스트 계정으로 실물 확인 완료
- 부수 작업: `npm run dev:fresh` 스크립트 추가(포트 3000 좀비 프로세스 +
  `.next` 캐시 충돌을 자동 정리, Windows 전용)

### 요청 1 — 문의 유형 재정의 (완료)

**문제**: 제목을 사용자가 쓸 수 없었음(insert 시 `title`에
`CATEGORY_LABELS[category]`를 넣어 카테고리명이 곧 제목). 내용도
카테고리별 고정 양식(품목명·발생일·인원수)을 채우는 방식.

**확정 체계** (권팀장 확인):
- 최상위 6종: 일정/운영·배송/납품·컴플레인·회계·알러지·기타
  (라벨에서 '문의' 접미사 제거)
- 컴플레인만 2단계 하위 7종(A안 채택): 메뉴/수량/위생·안전/배송/
  주문·시스템/고객응대/기타 → `inquiries.subcategory` 컬럼 신설
- 폐기: `MEAL_COUNT`(KOS에서 처리), `PHOTO`(파일보관함으로 이관),
  `CONTRACT`(→기타 흡수), `STAFF_MEAL`(불필요), `MENU`·`HYGIENE`
  (→컴플레인 하위로 흡수), `SCHEDULE`(→`SCHEDULE_OPS`)
- 발생일·품목명·인원수 칸 전부 제거. 권팀장 판단: *"문의 유형을 통일한
  상황에서 발생일이란 항목이 전체를 다 포괄하기엔 애매. 어차피 작성일이
  기록되고, 특정일이 언급된다면 고객사에서 내용란에 표시해줄 것"*
- 파일 첨부는 유지(권팀장 요청)

**마이그레이션**: `inquiry_category_restructure_260818.sql`
→ **Supabase에서 실행 완료**(2026-08-18). 실행 전 조회로 실제 데이터가
테스트 2건뿐임을 확인해 마이그레이션 리스크 없었음.

### 요청 2·3 — 파일보관함 (완료)

**메뉴명**: 좌측 '식단표' → **'파일보관함'**. URL(`/board/customer/diet`)은
그대로 둠 — 기존 북마크·이메일 링크 보호.

**★설계 결정 — A안 (식단표는 옮기지 않음)**:
식단표는 `weekly_menus`에 그대로 두고, 화면에서 `file_archive`와 두
소스를 합쳐 보여준다. 식단표 자동생성 파이프라인이 2026-08-18에 막
안정화된 직후라(PDF 변환·한글폰트·타임아웃 연쇄 수정) 얻는 이득(화면
코드 단순화) 대비 건드리는 위험이 크다고 판단. 나중에 옮겨도 막히지
않는 구조.

**★배포 범위 3단계** (`file_archive.scope`):
- `all`    : 모든 원
- `group`  : 같은 `diet_type` 원만. **CK 영양사팀 건강정보지와 위탁
  영양사 건강정보지가 다르다**는 운영 현실 반영(유대표 확인)
- `branch` : 지정 원 하나만. 임시원(크레오)은 그룹에 안 묶여 있어 그룹
  공통 파일이 기본적으로 안 보이는데, 나중에 "우리도 받고 싶다"고 하면
  이 방식으로 그 원에만 올려주면 되므로 **코드 수정 없이 운영으로 대응
  가능**(유대표 판단)

**식대청구서는 제외** — CK는 KOS에서, 그 외는 이메일로 이미 나가고 있음.
권팀장도 *"식대청구서 기능은 KOS 상에서 전달되니 해당 기능에서
제외하셔도 좋겠습니다"* 확인. 덕분에 PRIVATE 버킷+임시링크 같은 보안
구조를 안 만들어도 됐음(`diet-files`는 PUBLIC이라 금액 문서엔 부적합).

**화면 구성**:
- 고객: 검색 + 종류 탭(전체/식단표/건강정보지/유인물/식단사진/기타)
  + 연·월 필터 + 최신순 목록 + 페이지네이션(20건). 파일명 클릭 시 열림
  → 형식 버튼 3개와 '우리 원 파일형식' 문구가 사라져 요청 3번도 해결
- ERP: `/erp/files`(목록·검색·필터 4종·페이지네이션·삭제) +
  `/erp/files/new`(업로드). 고객사 공지와 같은 목록/작성 분리 구조

**마이그레이션**: `create_file_archive_260818.sql`
→ **Supabase에서 실행 완료**(2026-08-18, RLS 정책까지).

### 요청 6 — 스테퍼 정렬 (완료)

원 행과 라벨 행을 별도 flex로 그려서, 라벨 행의 각 칸이 [원+연결선]
전체 폭을 차지하는 바람에 가운데 라벨('처리중')만 `text-center`로
밀려 보였음. 원+라벨을 한 덩어리(`flex-col items-center`)로 묶고
연결선을 형제로 분리해 해결. 홈페이지 공개 문의(`app/inquiry/[id]`)는
원래 이 구조라 같은 버그가 없었음 — 이번에 방식이 통일됨.

### 요청 5 — 홈 하단 공지사항 교체 (완료)

`app/board/(customer)/dashboard/page.tsx`의 "이번달 식단표" 카드를
"공지사항" 카드로 교체. `parent_notices`에서 고정 우선 + 최신순 3건을
미리보기로 보여주고, 클릭 시 `/board/customer/notices`로 이동.

**부수 효과**: 제거된 섹션이 안고 있던 `weekly_menus.branch_id`를
`branches.id`로 잘못 조회하던 버그(아래 "기타 미해결" 목록 항목) 중
`dashboard/page.tsx:96` occurrence는 섹션 자체가 사라지며 함께 해소됨.
단, 같은 항목의 `app/api/branch-profiles/[id]/route.ts:65,73` occurrence는
이번 patch가 건드리지 않아 **여전히 남아있음** — 목록에서 취소선 처리만
하고 항목 자체는 유지.

강동E 테스트 계정으로 실물 화면 확인 완료.

### 요청 4 — 팝업 공지 기능 (완료)

착수 전 진단(`pg_policies` 실측)에서 예상보다 근본적인 버그 3건을 같이
발견해 수정:
1. `/erp/notices` 목록이 하드코딩 SAMPLE 데이터였음 → `/api/notices`
   GET 신설해 실제 연동
2. `parent_notices` RLS에 **원 담당자(고객사) 조회 정책 자체가 없었음**
   (전체 공지·학부모용 정책만 있었음) → 원 담당자용 SELECT 정책 추가
   (기존 정책은 안 건드림)
3. `/erp/notices/new` "특정 원 선택"이 `branch_profiles.id`를 보내고
   있었는데 `parent_notices.branch_id`는 `branches.id` 참조 컬럼 →
   수정(이 프로젝트 반복 패턴)

**신규 기능**:
- `parent_notices`에 `is_popup`, `popup_until`(선택, 종료시각) 컬럼 추가
- `/erp/notices/new`: 팝업 노출 체크 + 종료 시각 UI
- `/erp/notices` 목록: 팝업 노출 중 배지 + 끄기 버튼(PATCH)
- `CustomerPopupNotice` 컴포넌트 신설, `board/customer/layout.tsx` +
  `board/(customer)/layout.tsx` 양쪽에 삽입 — 고객 포털이 두 레이아웃으로
  나뉜 기존 구조라 양쪽 다 필요(아래 "반복 함정" 항목과 동일 패턴)

**마이그레이션**: `supabase/migrations/add_popup_notices_260818.sql`
→ **Supabase에서 실행 완료**.

강동E 테스트 계정으로 실물 확인 완료(팝업 노출, 확인 후 재노출 안 됨,
특정 원 타겟팅 정상 작동).

---

## ⚠️ 반복 함정 — 고객사 메뉴는 파일이 2개

고객사 좌측 메뉴가 **두 컴포넌트에 각각 정의**돼 있다:
- `components/board/BoardSidebar.tsx` — **데스크톱**
- `components/board/CustomerMobileNav.tsx` — **모바일**

2026-08-18에 '식단표'→'파일보관함' 변경 시 모바일만 고쳐서 데스크톱
화면에 반영이 안 되는 사고가 있었음(커밋 `6eba95f`로 수정). 두 파일
모두에 상호 참조 주석을 남겨뒀지만, **메뉴 라벨·아이콘·경로를 바꿀
때는 반드시 둘 다 확인할 것.**

---

## ⚠️ 반복 함정 — Supabase 프로젝트가 2개 (SQL 실행 전 이름 확인)

**kizmeal 조직 안에 프로젝트가 둘 있다: `icanmeal` / `kizmeal-renewal`.**
SQL Editor는 마지막에 열었던 프로젝트를 기억하므로, **실행 버튼을 누르기 전
좌측 상단 프로젝트 이름을 반드시 눈으로 확인할 것.**

2026-08-20 `add_holiday_exceptions_260820.sql` 실행 시 `icanmeal`에서 돌려
`relation "admins" does not exist` 에러가 났음. SQL 자체는 멀쩡했고
프로젝트만 잘못 고른 것이었다.

**→ 에러가 "relation ... does not exist" 형태로 나오면 SQL 문법을 뜯어보기
전에 프로젝트 오선택부터 의심할 것.** 테이블이 통째로 없다는 건 대개
스키마 문제가 아니라 접속 대상 문제다.

### 함께 기억할 것 — 레포 SQL 파일에 있다 ≠ 실제 DB에 있다

양방향으로 다 어긋나 있다:

- **레포에 있는데 DB엔 없을 수 있다** — `supabase/migrations/*.sql`은 자동
  적용되지 않는다. 사람이 SQL Editor에서 실행해야 반영된다.
  (그래서 이 문서는 실행한 마이그레이션에 "→ Supabase에서 실행 완료"를
  붙여 기록해 왔다. 새 마이그레이션도 같은 표기를 남길 것)
- **DB에 있는데 레포엔 없을 수 있다** — `branch_profiles.contract_type`,
  `branch_profiles.sort_order`는 실사용 중이지만 **DDL 마이그레이션 파일이
  레포에 없다**(수동 생성 추정). `diet_settings` 테이블도 마찬가지.
  **레포 grep 결과만 보고 "이 컬럼 없다"고 단정하지 말 것.**

---

## ⚠️ 반복 함정 — 마이그레이션 파일명 규칙이 두 가지다

`supabase/migrations` 안에 이름 계열이 두 가지 섞여 있다:
- ① 타임스탬프 접두사형 — `20260623000000_extend_diet_templates_and_logs.sql`
- ② 접두사 없는 이름형 — `add_holiday_exceptions_260820.sql`,
  `diet_template_tables.sql` 등

파일명을 추측해서 grep/조회하면 **한쪽 계열을 통째로 놓친다.**

**2026-08-25에 실제로 이 사고가 났다** — `diet_templates`의
`year`/`month`/`group_tag`/`validation_result`/`org_id` 5개 컬럼이
"레포 어느 마이그레이션에도 없다"고 기록했으나(이 문서와
`fix_diet_templates_active_trigger_260824.sql`에 남아 있었음, 2026-08-26
정정), 실제로는 ①계열 파일 `20260623000000_extend_diet_templates_and_logs.sql`
(커밋 `098631e`, 2026-06-23)이 이 5개를 전부 `ADD COLUMN` 하고 있었다.
원인은 파일명 추측 조회만 하고 **폴더를 나열하지 않은 것.**

→ 스키마 이력을 확인할 때는 파일명을 추측하지 말고 폴더 전체를 나열할 것:
```sh
ls supabase/migrations
grep -rn "ADD COLUMN" supabase/migrations
```

→ **여전히 유효한 원칙**: 스키마 판단은 DB 직접 조회가 최종 근거다
(`information_schema.columns`). 레포는 이력 파악용이다.

---

## ⚠️ 반복 함정 — sync/confirm처럼 "일부 컬럼만 갱신"은 upsert row 공유 금지

**2026-08-20, `holidays/route.ts` 실물 검증 중 실제로 재현된 사고.**

`POST /api/diet-automation/holidays`의 `sync` 모드(자동 대조, "변경 없음"
전제)와 `confirm` 모드(사람이 확정)가 처음엔 **같은 upsert row 조립 함수를
공유**하며, sync에서는 `confirmed_by`/`confirmed_at` 키만 조건부로 빼는
방식이었다. 설계상 "sync는 넘긴 컬럼만 UPDATE되니 안전하다"고 생각했지만:

- 프론트(`checkHolidaysBeforeGenerate`)가 sync payload를 만들 때
  `closurePolicy` 필드를 빠뜨림 → `rawPolicy == null ? null : ...` 로직이
  이걸 **명시적 null로 해석** → upsert가 기존에 분류돼 있던
  `closure_policy='all_closed'`를 **null로 덮어씀**
- 재현: 1단계에서 추석 3일 확정(all_closed) → DB 확인 정상 →
  "양식 준비" 재클릭(2단계, 변경 없어 sync 경로 예상) → **팝업이 다시
  뜨고 방금 고른 값이 사라져 있음** → DB 조회 결과 `closure_policy=NULL`,
  `confirmed_by/at`은 보존(그쪽은 애초에 키를 안 넣었으니 안전했음)

**"넘긴 컬럼만 건드린다"를 프론트 payload 구성(어떤 필드를 빼먹었는지)에
의존하면, 다음에 필드가 하나 추가될 때마다 똑같은 사고가 반복된다.**

**→ 해결한 방식**: sync 모드를 confirm과 완전히 분리된 **좁은 `UPDATE`**로
바꿈 — `synced_at`/`updated_at` 딱 두 컬럼만 쓰고, payload의 다른 필드는
아예 안 읽는다. "일부 필드만 갱신하는 모드"가 필요하면 처음부터 **그 모드
전용의, 건드릴 컬럼이 코드에 하드코딩된 좁은 update 문**으로 만들 것 —
"모드별로 넣을 필드를 다르게 하는 공유 upsert 함수"를 짜지 말 것.

**교훈**: 이런 종류의 버그는 코드 리뷰로 못 잡는다(로직이 그럴듯해
보인다). **실물 검증(실제 확정 → 실제 재조회 → DB 값 대조)을 안 했으면
그대로 배포됐을 것.** "confirmed_by/at 보존" 같은 명시적으로 의심받는
필드만 검증하고 넘어가지 말고, **저장되는 모든 컬럼을 확정 전/후로
대조하는 습관**을 남길 것.

---

## ⚠️ 반복 함정 — Vercel 환경변수는 저장 즉시가 아니라 Redeploy 완료 후 적용

**2026-08-20→08-24, 12월 공휴일 팝업 미표시를 하루 넘게 "원인 미확정"으로
남긴 실제 원인.**

Vercel 대시보드에서 Environment Variables에 값을 추가/수정해도 **이미 떠
있는 배포는 그 값을 모른다.** 새 값은 **다음 Redeploy가 끝난 시점부터만**
반영된다. `DATA_GO_KR_SERVICE_KEY`를 Production에 추가한 직후,
Redeploy가 마무리되기 전에 "공휴일 설정 다시 보기"로 재현 테스트를 해서
여전히 안 뜨는 것처럼 보였고, 이 때문에 코드 쪽 원인(라우트 미배포,
API 데이터 없음, 배열/객체 엣지케이스 등)을 계속 의심하며 하루를 썼다.
실제로는 전부 정상이었고 단지 새 배포가 아직 안 올라간 상태에서 확인한
것뿐이었다.

**→ 환경변수를 추가/수정한 뒤에는 Vercel 대시보드에서 해당 Redeploy가
"Ready"로 끝난 것을 먼저 확인하고 나서 재검증할 것.** 저장 버튼을
누르자마자 바로 확인하러 가지 말 것 — 배포 직후 몇 분 안의 재현 테스트는
"버그처럼 보이는 배포 지연"과 진짜 버그를 구분하지 못한다.

---

## ⚠️ 반복 함정 — dev 서버 켜진 채로 `npm run build` 돌리지 말 것

**2026-08-24, 원별 예외 아코디언(②) 실물 검증 중 실제로 재현된 사고.**

`next dev`가 떠 있는 상태에서 커밋 전 검증 습관대로 `npm run build`(프로덕션
빌드)를 돌렸더니, 빌드가 `.next` 디렉토리를 프로덕션 산출물로 덮어써서
dev 서버가 참조하던 웹팩 청크가 사라졌다. 그 직후 브라우저는 `layout.css`,
`main-app.js` 등 정적 자산 전부가 500으로 뜨고, dev 서버 콘솔엔
`Error: Cannot find module './8948.js'`(`MODULE_NOT_FOUND`)가 반복 출력됐다
— **코드 컴파일 에러가 아니라 dev/build 산출물 충돌**인데, 증상만 보면
방금 고친 코드가 전부 깨진 것처럼 보여 혼란스럽다.

**→ dev 서버를 띄워 브라우저로 실물 검증하는 도중에는 `npm run build`를
같이 돌리지 말 것.** 최종 빌드 검증(CLAUDE.md의 "커밋 전 npm run build
검증")은 dev 서버를 완전히 내린 뒤 한 번만 돌린다. dev 서버 실행 중 코드가
정상 컴파일되는지는 그 서버 자체의 터미널 출력(`✓ Compiled` / `⨯ Error`)으로
확인하면 충분하다.

**복구 방법**: dev 서버 프로세스 종료 → `.next` 디렉토리 삭제 →
dev 서버 재시작. `rm -rf .next` 후 `npm run dev`로 바로 복구된다(별도
캐시 정리 스크립트 불필요).

**부가 교훈**: 이번에 background bash 도구로 `TaskStop`을 불러도 `npm run
dev`가 내부에서 spawn한 자식 `node.exe` 프로세스가 살아남는 경우가 있었다
(Windows에서 부모-자식 프로세스 트리 종료가 항상 깔끔하지 않음). `TaskStop`
후에도 해당 포트가 여전히 응답하면, `Get-NetTCPConnection -LocalPort <포트>
-State Listen`으로 PID를 찾아 `Stop-Process -Id <PID> -Force`로 직접
정리해야 한다.

**★2026-08-25 재발** — 여기 이미 기록돼 있고 같은 날 작업지시서 0절에도
경고가 있었는데도 **똑같은 사고가 또 났다**. 커밋 전 빌드 검증으로
`npm run build`를 여러 번 돌린 뒤, dev 서버를 `npm run dev:fresh`가 아니라
그냥 `npm run dev`로 재시작해 `.next`가 다시 오염됐다(`errno: -4094 UNKNOWN`,
`open '...\.next\static\chunks\app\layout.js'` 반복). 그 상태에서 브라우저로
"레거시 그룹이 안 보인다"는 **가짜 버그 리포트**가 나왔다 — 실제로는
API도 렌더링 로직도 정상이었고, 원인은 오직 깨진 dev 서버 상태였다.
서비스롤 키로 GET 라우트와 동일한 쿼리를 직접 재현해 API가 정상 응답함을
먼저 확인하고 나서야 dev 서버 로그의 반복 에러를 발견해 원인을 좁혔다.

→ **사람(이 문서 포함)이 기억해야 하는 규칙은 언젠가 또 깨진다.** 문서에
적어두는 것만으로는 재발을 막지 못한다는 게 오늘 다시 증명됐다. **근본
해결 아이디어(다음 과제, 지금 구현하지 말 것)**: `package.json`의 `dev`
스크립트가 시작 시 `.next`의 오염 여부를 스스로 감지해서(예: 마지막
`next build` 산출물 타임스탬프와 `next dev` 캐시 형식이 섞였는지, 또는
`.next/BUILD_ID`처럼 build 전용 산출물이 남아있는지 확인) 자동으로 지우고
시작하도록 만들 것. 그러면 사람이 `dev`와 `dev:fresh` 중 무엇을 쓸지
매번 기억할 필요 자체가 없어진다. `scripts/dev-fresh.cjs`를 `dev-fresh`
전용이 아니라 `dev` 스크립트 자체의 사전 점검 단계로 흡수하는 방향으로
검토할 것.

---

## ✅ 메뉴 텍스트 미삽입 — 원인 규명 완료 (2026-08-20)

**결론: 코드 버그 아님. 어댑터 누락 가설도 틀렸음.**

- `_adapt_ts_menu_data()`는 실제 엑셀 업로드 경로(`/generate`)와 **무관**한 함수.
  DB `weekly_menus.menu_data`(TS 파서 결과)를 재생성할 때만 쓰는 변환기이며,
  `read_excel.load_excel()`은 처음부터 `pptx_generator` 호환 형식을 직접 출력하므로
  어댑터가 필요 없음. **HANDOFF의 기존 가설은 폐기.**
- 진짜 원인: 테스트에 쓴 7·8월 `키즈밀_식단표_26_0X_v6.xlsx`가 **영양사가 아직
  안 채운 빈 템플릿**이었음. `load_excel()`은 빈 셀을 정직하게 빈 값으로 파싱한 것.
- 곁가지 발견(미수정): `read_excel.py:347` `menu_data = {'year': 2026, 'month': 6}`
  하드코딩. `generate()`가 이 값을 안 쓰고 실제 연월은 request.form에서 별도로
  받으므로 **결과에 영향 없는 죽은 값**이나 위생상 정리 대상(CLAUDE.md 위반).

### ★ 이 조사로 드러난 더 큰 사실

**v6 양식은 아직 영양사가 실제로 한 번도 채워본 적이 없다.** 지금까지의 모든
검증은 솔아가 만든 테스트 데이터(`키즈밀_식단표_6월_테스트데이터_v6.xlsx`) 기준.
`_samples/26년 7월 배포 식단표/`의 진짜 배포 데이터
(`0. CK 2026년 7월 식단표(최종) (5).xlsx`)는 시트명 `7월식단표` 단일 구조로
**v6 포맷(1주차~5주차 시트 + 고정 행번호)과 완전히 다름** — 실무팀은 여전히
기존 디자이너 원본 포맷으로 작업 중.

→ **파이프라인 코드보다 "기준 엑셀폼이 실무자 입장에서 완결적인가"가 선결 과제.**

---

## ★ 다음 세션 최우선 — 월별 원별 예외설정 공통 기능 (설계부터)

**배경 (유대표 방향 확정, 2026-08-20)**: 권팀장 요청의 본질은 *"기존 엑셀폼에
입력하면 자동으로 짠 하고 생성"*. 실무자가 같은 일을 두 번 하는 느낌이 없어야
하며, **"관리자가 원별로 하나하나 찾아 들어가서 설정"하는 방식은 ERP를 만든
의미가 없다.** 확인만 하면 되는 팝업 + 저장 1번으로 일괄 반영이 되어야 함.

### 공통 UX (2가지 용도를 하나의 기능으로)

```
폼 준비 시점 자동 감지 → 팝업 1개
  → 영향받는 원 전체를 한 화면에 리스트 + 토글로 표시
  → 전년도 동시기 값을 기본값으로 미리 채움 (대부분 그대로 통과)
  → 예외인 원만 토글 수정 → 저장 1번으로 일괄 반영
```

**용도 ① 공휴일 원별 운영여부**
- 공휴일 날짜 자체는 사람이 입력하지 않는다 → **data.go.kr 특일 정보 API**
  (한국천문연구원)로 연초 자동 수집, 관리자는 **연 1회 승인 클릭 1번**만
- 원별로 공휴일 운영/휴무가 갈리는 것이 진짜 변수 → 이 부분만 팝업으로 확인
- 기존 `gen_form.py --holiday 일:사유` 옵션과 어떻게 연결할지 설계 필요
- ⚠️ 과거 `holidays` 라이브러리 도입 검토 후 철회한 이력 있음(위 `cb82311` 항목).
  그건 "공휴일 운영여부까지 코드가 일괄 판정"을 철회한 것이지,
  **"원별 설정을 사람이 미리 해두는 방식"까지 부정한 것은 아님** — 재확인 완료

**용도 ② 방학 O/X 양식 배정**
- 디자이너는 방학O·방학X **공용 양식 2벌**만 제공. **어느 원이 O/X인지는
  디자이너가 모르는 우리 쪽 정보** → 원별 배정표는 우리가 만들어야 함
- 기존 계획("업로드 시 사람이 용도 지정")대로 두면 매달 49개 원을 하나하나
  판단하는 반복 수작업 발생 → ①과 동일 UX로 처리
- 한 원의 방학 유무는 학기 단위로 거의 고정 → 전년도 동시기 기본값이 잘 맞음
- 연계 작업: `VACATION_IMG` 이름표 부여 +
  `remove_holiday_cover_pics()`(`pptx_generator.py:788`) 제거 대상에서 제외
  (단계2에서 발견된 미해결 건 — 진짜 공휴일 셀이 방학 그림과 겹치면 삭제됨)

### 자동화 대상이 아닌 것 (검토 후 제외 확정)

| 항목 | 제외 사유 |
|---|---|
| 생일주(🎂) | 그 주 생일 아동 유무는 그때 결정되는 실제 데이터. 미리 채울 수 있는 설정값이 아님. 칸의 실제 입력 여부가 진실 기준(`cb82311` 결정 유지) |
| 대괄호 표기(`[원명-대체메뉴]`) | 매달 메뉴 자체가 바뀌는 실제 콘텐츠 결정. 반복되는 설정값이 아님 |
| `contract_type` | 한 번 설정하면 유지되는 값. 매달 반복 입력 문제 아님. 이미 처리됨 |

### 착수 순서

1. ~~**data.go.kr 특일 정보 API 조사**~~ → **완료 (2026-08-20)**. 아래 조사결과 참고
2. ~~**DB 스키마 설계**~~ → **완료 (2026-08-20)**.
   `supabase/migrations/add_holiday_exceptions_260820.sql`
   → **Supabase(`kizmeal-renewal`)에서 실행 완료**, 시드 2행 확인
3. ~~**API 수집 코드 + diff 감지**~~ → **완료 (2026-08-20)**
   `lib/holidays.ts` + `app/api/diet-automation/holidays/route.ts`
4. ~~`gen_form.py` / `generate-form.yml` 연결~~ → **완료 (2026-08-20)**
   `gen_form.fetch_holiday_map()`이 `public_holidays`를 읽는다.
   워크플로 수정 불필요(자격증명이 이미 있었음)
5. ~~`template_resolver.py` 원별 방학양식 분기~~ → **완료 (2026-08-20)**
   `resolve_template_set()` + `pick_template()` + `fetch_vacation_map()`.
   `app.py`(2곳)·`app_actions.py` 배선 완료
6. 팝업 UI (①③④⑤⑥) → **완료 + 실물 검증 완료 (2026-08-20)**.
   `holidays/route.ts` GET/POST 실물 확인도 이때 완료.
   → 상세는 아래 "★6단계 상세" 참고
7. ~~**② 원별 예외 아코디언**~~ → **구현 + 실물 검증 완료 (2026-08-24).**
   - 엔드포인트는 별도 라우트로 결정: `app/api/diet-automation/holidays/branches/route.ts`
     (GET=조회, POST=저장). `/api/diet-automation/holidays` 밑 하위 경로가
     아니라 형제 라우트로 뺐다 — 공휴일 자체(1단계)와 원별 예외(2단계)는
     저장 테이블도 다르고(`public_holidays` vs `branch_holiday_operations`)
     mode 분기(confirm/sync)까지 겹치면 한 파일이 너무 커진다고 판단
   - "전 원 휴무"(`closure_policy='all_closed'`)로 분류된 날짜만 대상.
     `branch_holiday_operations`에 `source='manual'`로 저장(항상 사람이
     이 화면에서 확정한 값이므로)
   - 프리필 우선순위 3단(설계 결정 3 그대로 구현): 이번 라운드 기존 결정값 >
     전년도 동일 이름 공휴일의 그 원 결정(승계) > `branch_profiles.operates_on_holidays`
     기본값. 승계는 `lib/holidays.ts`의 `inheritPolicy()`와 같은 발상이지만
     정책값이 아니라 "그 날짜" 자체를 찾아 그날의 원별 결정을 그대로 가져옴
   - 대상 원은 PPTX 생성 자격과 동일 기준(`lib/pptx-eligibility.ts`
     `filterEligibleBranches()`, 임시계약 제외) — 결국 이 화면의 소비자가
     PPTX 생성기이므로 대상이 다르면 의미가 없다는 논리(설계 결정 2와 같은 맥락)
   - **아코디언 축은 기존 ERP의 E/P/R/MB/SLP/AO 원 계열 그룹이 아니라
     "운영 여부" 2그룹**으로 새로 정의: "공휴일에 운영하는 원"(현재
     덕양P·광교SLP, 기본 펼침) / "공휴일에 쉬는 원"(나머지, 기본 접힘).
     계열 그룹 아코디언은 "어느 계열을 볼지"가 목적이라 이 화면(대부분
     그대로 두고 예외만 확인)엔 안 맞는다고 판단 — HANDOFF 문구
     ("`operates_on_holidays = true` 그룹은 기본 펼침")를 계열이 아니라
     운영여부 기준으로 재해석해 구현. 원별 정렬은 `group_tag`→`sort_order`
     그대로(계열 배지만 행에 작게 표시)
   - 새 컴포넌트: `app/erp/(protected)/diet/BranchHolidayExceptionPopup.tsx`
     + `checkBranchExceptionsNeeded()`(대상 날짜 없으면 pre-generate 흐름은
     조용히 건너뜀 — `checkHolidaysBeforeGenerate()`와 같은 원칙)
   - 배선: `handlePrepareClick()`과 `HolidayConfirmPopup`의 `onConfirmed`
     둘 다 `proceedToFormGeneration()`을 거치도록 통일(신설한 게이트 함수) —
     기존엔 두 경로 다 `handleGenerateForm()`을 직접 불렀던 것을 여기서
     한 곳으로 모음. "원별 예외 확인" 다시보기 링크도 신설(기존 "공휴일
     설정 다시 보기" 옆)
   - **★실물 검증 완료 (2026-08-24, 9월 추석 3일 — 9/24~26, 이미 all_closed로
     분류돼 있던 실데이터로 진행). 어제 sync 모드 버그(HANDOFF 위 항목)의
     교훈대로 커밋 전에 dev 서버로 직접 클릭해서 검증했고, 실제로 빌드
     통과와 무관한 버그를 하나 잡았다.**
     - **1차 시도 — 버그 발견**: 화면에 "공휴일에 운영하는 원 (0개)" /
       "공휴일에 쉬는 원 (49개)"로 떠서 덕양P·광교SLP가 통째로 안 보임.
       원인은 `route.ts`가 아니라 `BranchHolidayExceptionPopup.tsx` 쪽 —
       `load()`에서 `initChoices`를 다 계산해놓고 **`setChoices(initChoices)`
       호출을 빠뜨려서** `choices` state가 계속 빈 객체(`{}`)로 남아 있었다.
       그룹 분리 로직(`choices[c.date]?.[b.id]`)이 항상 `undefined`를 읽어
       "운영" 필터는 전부 빠지고 "쉬는" 필터는 전부 걸린 것. 서버 응답
       자체는 처음부터 정상이었다(`operates_on_holidays` 값도, `operations`
       맵도 맞게 내려주고 있었음 — dev 서버 로그로 확인).
     - **원인 진단 중 별도 사고**: 진단하다가 dev 서버가 켜진 채로
       `npm run build`(프로덕션 빌드)를 돌려서 `.next`가 덮어써지는 바람에
       dev 서버가 `Cannot find module './8948.js'`로 전체 500을 뿜은 부수
       사고가 있었다. `.next` 삭제 후 dev 재시작으로 복구(아래 "반복 함정"
       항목 참고 — dev와 build를 동시에 돌리지 말 것).
     - **수정 후 재검증**: `setChoices(initChoices)` 한 줄 추가 →
       "공휴일에 운영하는 원 (2개)"에 덕양P·광교SLP만, "공휴일에 쉬는 원
       (47개)"에 나머지 — 9/24·9/25·9/26 세 날짜 전부 정상 확인
     - **저장 후 DB 대조**(서비스키로 직접 REST 조회, 화면과 별도 경로):
       - `branch_holiday_operations` 9/24~26 총 **147건**(3일×49원) —
         날짜별로 정확히 49건씩
       - **전부 `source='manual'`**(sync 경로로 잘못 들어간 행 0건),
         `decided_by`/`decided_at` 빠진 행 0건
       - `is_operating=true`는 정확히 **6건**(덕양P·광교SLP × 3일)이고,
         그 외 원이 `true`로 잘못 들어간 행·덕양P·광교SLP가 `false`로
         잘못 들어간 행 **둘 다 0건**
       - 임시계약 원(크레오 3곳·테스트P)이 저장에 섞인 행 **0건** —
         `filterEligibleBranches()` 필터가 저장 경로에서도 정확히 작동
     - 화면 표시값 = DB 저장값 = 기대값, 세 축 전부 일치 확인하고 커밋함
8. ~~**방학 O/X 배정 UI**~~ → **구현 + 실물 검증 완료 (2026-08-24)**.
   - **판정 신호 확인 — 착수 전 DB 조사에서 예상 밖 사실 발견**: 원래
     설계된 판정 신호인 `diet_templates.year`/`month`/`vacation_variant`가
     **DB에 전혀 채워져 있지 않음**(전부 0행). 템플릿 관리 화면
     (`/erp/diet/templates`, `app/api/board/diet/templates/route.ts`)이
     아직 "전역 active 템플릿 1개" 구식 모델이라 업로드 시 이 세 컬럼을
     지정하는 UI 자체가 없다 — **별개 미완성 작업**(이 항목 범위 밖으로 확정).
   - 그래도 **판정은 달을 하드코딩(7·8월, 12·1월 등)하지 않고
     `diet_templates.vacation_variant IN ('vacation_on','vacation_off')`
     존재 여부로 동적으로 감**(CLAUDE.md 원칙 + `closure_policy` 설계와
     같은 "사람이 표시 → 코드가 읽는다" 원칙 유지). 지금은 실데이터가
     없어 이 팝업이 항상 스킵되지만, 위 미완성 작업이 채워지는 순간
     코드 수정 없이 바로 동작한다
   - API: `app/api/diet-automation/vacation/route.ts` (GET=판정+조회,
     POST=저장). 저장 대상 `branch_monthly_vacation`, `source='manual'`
   - 프리필 우선순위: 이번 라운드 결정값 > **전년도 동일 월**(year-1, 같은
     month)의 그 원 결정(승계 — 공휴일과 달리 "이름" 매칭이 필요 없음,
     방학엔 이름이 없고 학기 단위로 고정된다는 전제) > 기본값
     `has_vacation=false`(방학X). `branch_profiles`엔 방학 기본정책
     컬럼이 없어서, 기본값을 `template_resolver.py`가 미배정 원을
     방학X로 보수 처리하는 것과 **의도적으로 일치**시켜 UI·파이프라인
     불일치를 방지함
   - 아코디언: 오늘 만든 원별 공휴일 예외 화면과 대칭 — "방학O 배정"
     (기본 펼침) / "방학X 배정"(기본 접힘) 2그룹. 대상 원은 동일하게
     `filterEligibleBranches()`(임시계약 제외)
   - 새 컴포넌트: `app/erp/(protected)/diet/VacationAssignmentPopup.tsx`
     + `checkVacationAssignmentNeeded()`. `page.tsx`의
     `proceedToFormGeneration()` 뒤에 3번째 게이트 `proceedToVacationCheck()`
     로 연결(공휴일 분류① → 원별 예외② → 방학 배정④ → 폼 생성). "방학
     배정 확인" 다시보기 링크도 신설
   - **★state 초기화 버그 재발 방지**: 오늘 오전 원별 예외 화면에서
     `setChoices(initChoices)` 호출 누락으로 난 버그를 교훈 삼아, 이번엔
     `load()`에서 `initChoices` 계산 직후 바로 `setChoices()`를 호출하고
     그 옆에 재발 방지 주석까지 남김. 코드 작성 후 실제로 이 줄이
     있는지 `grep`으로 직접 확인하고서야 검증 단계로 넘어감
   - **실물 검증(2026-08-24, 2026년 8월 — SQL로 임시 시드 후 검증, 검증
     끝나고 전부 삭제)**: DB에 실데이터가 없어 9월 공휴일 때처럼 기존
     데이터로 검증할 수 없었음. `diet_templates`에 2026-08 방학O/X
     테스트 행 2개(가짜 `file_path`) + `branch_monthly_vacation`에
     **2025-08**(전년도) 시드 2행(강동E=방학O, 목동E=방학X, 승계 로직
     검증용으로 임의 지정) 삽입 → 화면에서 "방학O 배정 (1개)=강동E
     (작년 승계 라벨)" / "방학X 배정 (48개)=목동E(작년 승계)+나머지
     47개(기본값)" 정상 확인 → 저장 → DB 대조 결과 `branch_monthly_vacation`
     2026-08에 정확히 49건, 전부 `source='manual'`, `decided_by`/`at`
     빠짐없음, `has_vacation=true`는 정확히 1건(강동E) 확인, 임시계약
     원 섞임 0건
   - **테스트 데이터 전부 삭제 완료** — `diet_templates` 테스트 2행,
     `branch_monthly_vacation` 2025-08 시드 2행 + 2026-08 저장분 49건
     전부 삭제. 삭제 후 재조회로 `diet_templates`는 원래 실템플릿 1건만,
     `branch_monthly_vacation`은 0행으로 원상복구 확인
     - **왜 저장된 49건까지 지웠는지**(사용자 질문·확인 거쳐 진행) — 9월
       공휴일 검증과 달리, 이번엔 트리거 신호(가짜 템플릿)도 승계 근거
       (가짜 2025-08 시드)도 전부 검증용으로 지어낸 것이라 저장
       메커니즘은 실물 검증됐어도 **값 자체는 실데이터가 아님**. 특히
       강동E=방학O로 남겨두면, 나중에 진짜 8월 방학O/X 템플릿이 올라와도
       프리필 우선순위 1번(이번 라운드 기존 결정값)이 이 가짜 `manual`
       행을 최우선으로 읽어 **엉뚱한 서식이 붙는** 실제 위험이 있었음
9. **별도 커밋**: `pptx_generator.py:49` `_HOLIDAY_OPERATING` 하드코딩 제거
   → `cfg['operates_on_holidays']` 참조로 교체(소비 지점 990/1008/1023/1034행).
   49개 원 전부에 영향 가는 파일이라 스키마 안정 확인 후 착수하기로 분리

### 템플릿 관리 화면(year/month/vacation_variant 업로드 UI) 착수 전 조사 — 2026-08-24

**코드 변경 없음(의도된 범위 — 이번 조사는 스키마·트리거 정비까지만).
SQL 마이그레이션은 2026-08-25에 검토·보완·실행·실물검증 전부 완료.**

착수하려고 `/erp/diet/templates` + `template_resolver.py`를 대조하다가
당초 알려진 것보다 범위가 큰 문제 2개를 발견함:

- **★신규 발견 — 지금 "활성 템플릿"이 실제 PPTX 생성에 전혀 영향이 없음.**
  DB에 유일하게 존재하는 실템플릿(v1 `2026년6월식단표폼`, `is_active=true`)의
  `year`·`month`가 **둘 다 NULL**. `template_resolver.py`의
  `resolve_template_set()`은 항상 구체적 `year`/`month`로 필터링해서
  조회하는데(`filters={'is_active':'true','year':year,'month':month}`),
  NULL은 어떤 구체값과도 매칭이 안 됨 → **v1을 "활성화"해도 파이프라인은
  항상 로컬 6월 폴백(`TEMPLATE_PATH`)만 씀.** 8/11 기록된 "발견①(폴백이
  문지기를 무력화)"과는 다른 원인(그땐 검증 실패, 지금은 필터 자체가
  안 맞음)이지만 결과는 동일 — 디자이너 업로드본이 여전히 실사용 안 됨.
  → **결정(유대표, 2026-08-24)**: v1은 그대로 두고 삭제 판단 보류.
  화면에 **"레거시(효과없음)"** 라벨만 표시하기로 확정. 다음 세션
  업로드 UI 작업 범위에 이 라벨 표시를 포함할 것.
- **트리거 충돌 — 방학 O/X 동시 active가 구조적으로 불가능함.**
  `diet_template_tables.sql`의 `ensure_single_active_template` 트리거가
  `WHERE id != NEW.id`로 **테이블 전체에서 무조건 1개만** active를 허용.
  `template_resolver.py`는 "같은 (year,month) 안에서 vacation_variant별로
  여러 개 동시 active"를 전제로 설계돼 있어(5단계, HANDOFF 참고) —
  방학O 활성화 시 같은 달 방학X가 트리거에 의해 자동으로 꺼짐.
  **이 트리거를 안 고치면 어제 만든 방학 O/X 배정 UI(8번)가 실전에서
  못 돌아간다.**
  → **초안 작성**: `supabase/migrations/fix_diet_templates_active_trigger_260824.sql`
  — 트리거 함수를 "같은 `(year, month, vacation_variant)` 조합 안에서만
  단일 active"로 좁힘. `IS NOT DISTINCT FROM`으로 NULL(레거시 행)끼리는
  기존처럼 "통틀어 1개만" 동작을 그대로 보존, year/month가 채워진 새
  행들만 그 값 기준으로 정확히 묶이게 함
  → **Supabase에서 실행 완료 (2026-08-25)**. 검토 중 초안 대비 `org_id`
  축(멀티테넌시 대비)과 `SET search_path = public`(린터 대응)을 추가했다.
  검증 스크립트: `supabase/migrations/verify_diet_templates_trigger_260825.sql`
  - **검증 결과 요약(2099년 샌드박스, 전부 ✅)**:
    1) 같은 달 방학O·방학X 동시 active 공존 확인(핵심 목적 달성)
    2) 같은 (연,월,variant) 안에서는 여전히 단일 active(방학O만 교체,
       방학X 무변동)
    3) UPDATE 경로도 INSERT와 동일하게 동작(껐다 켜도 옆 variant 무변동)
    4) 다른 달끼리 비간섭(2099-11이 2099-12에 영향 없음)
    5) 레거시(year/month NULL) 그룹은 종전대로 "통틀어 1개만" 동작 보존
    6) 정리 후 원상복구 스냅샷 일치, 잔여 테스트행 0
  - 같은 날 `diet_templates_year_month_pair` CHECK 제약도 실행 완료
    (`supabase/migrations/add_diet_templates_year_month_pair_check_260825.sql`)
    — year/month 반쪽 채움 방어
- **★업로드 폼 착수 전 코드 조사(2026-08-25 오전) — 다음 작업 전 기록**:
  - **권한 게이트에 역할 필터가 없다** — API 3곳(`route.ts`/`[id]/route.ts`/
    `activate/route.ts`) 전부 `admins.is_active=true`만 확인하고 `role`
    컬럼은 안 본다. `lib/erp-nav.ts`의 "템플릿 관리" 항목도 `allowedRoles`가
    없어 전 역할에 노출(대조: 같은 파일 "관리자 관리" 항목은
    `allowedRoles: ADMIN_CREATE_ROLES` 지정돼 있음). 즉 `director`(허이사,
    읽기전용 취지)도 업로드·활성화·삭제가 전부 가능 — 권한 설계와 어긋남.
    이번 작업 범위 밖이나 정리 대상으로 기록.
  - **version은 테이블 전체 `MAX(version)+1` 전역 일련번호**다(그 달 안의
    번호 아님). 락·유니크 제약이 없어 동시 업로드 시 충돌 가능성이 있다.
    급하지 않으므로 아래 "기타 미해결"에 기록.
  - **`validation_result`는 저장만 되고 화면엔 전혀 안 보인다** — POST
    응답 JSON엔 포함되지만 프론트 `DietTemplate` 인터페이스에 필드가 없고,
    GET 목록도 이 컬럼을 select 안 한다. 검증 실패해도 업로드는 그대로
    진행됨(막는 코드 없음).
  - **board/erp는 중복 구현이 아니다** — `app/board/admin/diet/templates/page.tsx`는
    `router.replace('/erp/diet/templates')` 4줄짜리 리다이렉트뿐이고, 실제
    화면·API는 `/erp/diet/templates` + `app/api/board/diet/templates/route.ts`
    1벌뿐(API 경로명에 `board`가 남아있는 것뿐).
- **★업로드 폼 작업 방향 확정(2026-08-25)**:
  - **version은 전역 일련번호로 그대로 둔다** — 목록 UI를 연월로
    그룹핑할 것이므로 화면이 version에 의존하지 않는다. 지금 바꾸면 v1의
    "버전 1"이라는 의미가 소급해서 깨진다.
  - **`validation_result`는 "표시만" 한다. 업로드를 막지 않는다** —
    디자이너 원본은 이름표가 없는 게 정상 상태라, 검증 실패를 업로드
    차단 조건으로 쓰면 정상적인 업로드까지 막힌다.
- **다음 세션 순서**: ① 업로드 폼에 연도(`getYearOptions()` 재사용)·
  월·방학O/X/무관 선택 추가 → ② 목록 UI를 연월별로 그룹핑(현재는
  "활성 1개 vs 나머지" 이분법이라 여러 달·variant가 동시에 active인 새
  모델을 못 담음) → ③ v1에 "레거시(효과없음)" 라벨 표시
  - **코드 변경은 지금 당장 불필요** — 트리거 범위가 넓어졌지만 현재
    실템플릿이 v1(NULL/NULL) 1건뿐이고 레거시 그룹은 종전대로 단일
    active가 유지되므로 기존 UI 동작에 변화 없음. 목록 UI의 "활성 1개
    vs 나머지" 이분법은 여러 달·variant가 동시에 active가 되는
    **다음 작업(①②)에서** 깨진다.
  - 업로드 폼은 **연·월을 반드시 함께** 저장해야 한다. 한쪽만 채우면
    `diet_templates_year_month_pair` CHECK에 걸려 INSERT가 거부된다
    (의도된 동작).
  - `group_tag`를 나중에 실제로 살릴 때는 **트리거도 함께 수정**해야
    한다. 안 그러면 계열별 템플릿이 서로를 꺼버린다.
- **이번엔 범위에 안 넣기로 한 것**: `group_tag`(원 계열별 템플릿) 축.
  `resolve_template_set()`이 아직 이 컬럼을 안 읽어서(dead column,
  향후 확장용으로 추정) 이번 작업과 무관 — 손대지 않음

### pick_template() 방학 variant 오배정 수정 — 2026-08-25 (커밋 `3d4a81d`, `f8be067`)

`template_resolver.py`의 `pick_template()`이 자기 주석과 정반대로 동작하고
있었다. 157~163행 주석은 "없는 그림이 빠지는 쪽이 엉뚱한 방학 그림이 붙는
쪽보다 덜 틀리다"고 선언하는데, 5줄 아래 order 배열이 반대편 variant까지
폴백 후보에 넣고 있었다 — 방학O만 업로드되고 방학X가 아직 안 올라온 달에
비방학 원이 방학O를 받는 경로가 있었다.

- **수정**: order 배열에서 반대편 variant 제거(`True→(ON,NONE)` /
  `False→(OFF,NONE)`), `else` 분기는 `(NONE,)`으로 축소. 옛 177행
  "남은 것 아무거나" 폴백을 로컬 폴백으로 교체(`'로컬(방학양식 미비)'` —
  `'로컬(업로드없음)'`과 구분됨) + 경고 로그. 호출부 3곳에서 버리던 출처
  설명을 받아 이상 신호만 로그하고(정상 매칭=무로그, none 폴백=원별 로그,
  로컬 폴백=경고) 배치 끝에 variant별 배정 요약 1줄을 출력. 반환값을
  2-튜플→3-튜플로 확장해(`(경로, 출처, 사용된 variant)`) 호출부가 경로
  문자열로 variant를 역추적하던 코드도 제거(`f8be067`)
- **로그 읽는 법**: Actions/Render 로그에서 원 하나하나는 원하는 variant를
  정상적으로 받으면 아무것도 안 찍힌다. `none`으로 폴백된 원만 원별로
  한 줄, 로컬로 빠진 원은 경고로 한 줄. 배치 끝에
  `[템플릿] 배정 요약 — 방학O N원 / 방학X N원 / none N원 / 로컬폴백 N원`
  이 항상 한 줄 찍힌다.
- **★검증 한계** — 방학 템플릿이 아직 DB에 없어 실물 생성으로는 이 코드
  경로에 닿지 않는다. `pick_template()`을 순수 함수로 직접 호출해 18가지
  조합을 확인했을 뿐이다(전부 통과). **방학O/X 템플릿이 처음 실제로
  올라가는 달에 반드시 실물로 재검증할 것** — Actions 로그의
  "[템플릿] 배정 요약" 한 줄과 "방학 양식 미비 → 로컬 폴백" 경고 유무를
  확인할 것.

### 템플릿 업로드 폼 + 목록 UI 재구성 — 구현 + 실물 검증 완료 (2026-08-25 오후, `f926b96`/`02f3f6c`/`238f7e7`)

**왜 목록 UI를 연월 그룹으로 바꿨는가** — 기존 화면은
`templates.find(t => t.is_active)`로 활성 템플릿을 **하나만** 집어 상단에,
나머지는 `!t.is_active` 필터로 하단에 뿌리는 이분법이었다(`page.tsx` 옛
93~94행). 트리거 범위가 `(year, month, vacation_variant)` 조합으로 좁아져
같은 달에 방학O·방학X가 **동시에** active일 수 있게 되면서, 이 이분법이
깨진다 — `find()`는 하나만 집고, 나머지 활성 템플릿은 `is_active=true`라서
`!t.is_active` 필터에도 안 걸려 **화면 어디에도 안 뜨고 통째로 사라진다.**
그래서 1차 축을 연·월로 바꾸고, 활성 여부는 축이 아니라 행마다 붙는 배지로만
표시하도록 재구성했다(`page.tsx` `groupTemplates()`).

**그룹 헤더에 active variant 현황을 그대로 노출하는 이유** — "⚠️ 쌍 미완성"
배지만 띄우면 판정 기준이 렌더링 코드 안에 묻혀서, 왜 경고가 떴는지
실무자가 코드를 안 보면 알 수 없다. `방학O ✅ · 방학X ❌` 처럼 근거를
같이 보여주면 "방학X를 아직 안 올렸구나"를 화면만 보고 바로 안다. 이
판정은 `pptx-server`의 `resolve_template_set()`이 그 달 active 템플릿을
variant별로 모으는 것과 같은 기준(active인 것만 집계)이라 — 화면이 파이프라인이
실제로 보는 것과 같은 것을 보여주는 셈이다.

**`validation_result`는 표시만 하고 업로드/활성화를 막지 않는다** — 디자이너
원본 pptx는 이름표(cNvPr name)가 없는 게 정상 상태이고, 이름표는 생성
파이프라인(`template_namer.apply_template_names()`)이 나중에 자동으로
붙인다. 지금까지 `validation_result`는 DB에 저장만 되고 화면 GET select에도
없어 아무도 못 봤다 — 이번에 `TemplateValidation.summary` 문자열을 그대로
목록 각 행에 표시하도록 추가했다(레거시 행처럼 값이 `{}`면 "검증 정보 없음"
폴백).

**★브라우저 실물 검증 결과 (2029-11/12 샌드박스, 전부 ✅)** — 실제 파이프라인이
조회하지 않는 좌표를 써서 실데이터에 안 닿게 함:
1. 레거시 그룹 표시 ✅ (dev 서버 `.next` 오염이 1차 원인이었던 오탐 — 아래
   "반복 함정" 항목 참고. `dev:fresh` 재시작 후 정상 확인)
2. 업로드 + variant 라벨(방학O/방학X/무관) 표시 ✅
3. **★방학O·방학X 동시 활성화 — 두 행 모두 화면에 보이고 둘 다 활성 배지**
   ✅(이번 작업 전체의 판정 지점 — 기존 구조에서는 두 번째 활성 템플릿이
   사라졌었다). 헤더 `방학O ✅ · 방학X ✅`, "쌍 미완성" 배지 자동 소멸 ✅.
   방학O만 활성일 땐 `방학O ✅ · 방학X ❌ ⚠️쌍 미완성` 정확히 표시 ✅
4. 무관(평월) 템플릿 활성화 → 헤더 `공용 ✅`, 경고 배지 없음 ✅. 다른 달
   그룹 무변동 확인(month 축 실증) ✅
5. 정리 — 테스트 템플릿 전부 삭제, DB `diet_templates` 1행(v1, `is_active=true`)
   원상복구 확인 ✅

⚠️ **모델**: 스키마·데이터모델 설계 단계는 **Opus**, 확정 후 구현은 Sonnet 복귀.

### 1단계 조사결과 — data.go.kr 특일 정보 API (2026-08-20)

- **무료 · 자동승인**(심의 없음). 개발계정 **일 10,000건**, 운영계정은 확장 가능
- 엔드포인트: `https://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService/getRestDeInfo`
  - 5개 오퍼레이션 중 **`getRestDeInfo`(공휴일)** 를 쓸 것. "관공서 공휴일에 관한
    규정" 기준이라 국경일·대체공휴일·임시공휴일·선거일을 모두 포함한다.
    `getHoliDeInfo`(국경일)는 3·1절·광복절 등만 나와 범위가 좁다
- 파라미터: `serviceKey`, `solYear`(필수), `solMonth`(생략 시 연 단위 조회),
  `numOfRows`, `_type=json`
- 응답: `locdate`(**YYYYMMDD 정수**, 문자열 아님), `dateKind`, `isHoliday`(Y/N),
  `dateName`, `seq`
- **함정: `numOfRows` 기본값이 10.** 연 단위 조회 시 공휴일이 20건 안팎이라
  기본값 그대로 두면 **에러 없이 조용히 절반이 잘린다.** 반드시 키울 것
  - 실측(2026-08-20): **2026년 전체 22건**. 기본값이었으면 12건이 소실됐다.
    `lib/holidays.ts`는 `NUM_OF_ROWS=100` + `totalCount > NUM_OF_ROWS`면
    조용히 흘리지 않고 에러를 던지도록 방어해 둠
- ⚠️ **API가 주는 isHoliday='Y'가 곧 "원이 쉰다"는 뜻은 아니다.** 2026년
  응답에 **노동절(5/1)·제헌절(7/17)** 도 `Y`로 포함돼 있다. 어린이집 실제
  운영 여부와 다를 수 있으므로 **팝업 첫 사용 때 영양기획팀과 대조할 것.**
  (원별 운영여부 설정이 있는 이유 자체가 이 간극이다)
- **함정: 인증키 이중 인코딩** — data.go.kr는 인증키를 두 벌 준다.
  - **Decoding 키** = 원본(`+`, `/`, `=` 포함) ← **이걸 저장한다**
  - **Encoding 키** = 그걸 URL 인코딩한 것(`%2B`, `%2F` …)

  `URLSearchParams`(TS)나 `requests`의 `params=`(Python)는 값을 **자동으로
  한 번 인코딩**한다. 여기에 Encoding 키를 넣으면 `%2B` → `%252B`로
  이중 인코딩되어 아래 에러가 난다:

  ```json
  {"errMsg":"SERVICE_KEY_IS_NOT_REGISTERED_ERROR","returnAuthMsg":"등록되지 않은 서비스키","returnReasonCode":"30"}
  ```

  **키가 멀쩡한데 이 에러가 나오면 키를 의심하기 전에 이중 인코딩부터 볼 것.**
  (이 에러 메시지는 키가 아예 없을 때도 똑같이 나와서 구분이 안 된다 —
  2026-08-20 무인증 생존확인 테스트에서 동일 응답을 받았음)

  **2026-08-20 실측 확인** — 실제 발급키로 세 방식 비교:

  | 전달 방식 | 결과 |
  |---|---|
  | `URLSearchParams`에 Encoding 키 그대로 | ❌ code=30 |
  | 원문 그대로 이어붙임 | ✅ |
  | `decodeURIComponent` 후 `URLSearchParams` | ✅ |

  → **`lib/holidays.ts`의 `normalizeServiceKey()`가 둘 다 흡수**하도록 처리함.
  포털에서 어느 쪽을 복사해와도 동작한다. 형태를 사람에게 강제하는 대신
  코드가 받아내는 쪽을 택함
- **키 이름은 `DATA_GO_KR_SERVICE_KEY`**, 서버 전용(`NEXT_PUBLIC_` 금지).
  `.env.local` + Vercel 환경변수에만 둔다. **GitHub Actions Secrets에는 넣지
  않는다** — 팝업은 Actions에서 띄울 수 없으므로 API 호출은 Next.js가 맡고,
  `gen_form.py`는 사람 확인을 거친 `public_holidays`를 읽기만 한다.
  덕분에 "`generate-form.yml`이 `--holiday` 없이 호출한다"는 기존 미해결
  건도 CLI 인자 대신 DB 조회로 자연히 풀린다
- ⚠️ **연초 1회 수집으로는 부족한 이유** — 임시공휴일·재보궐선거는 사후 지정되고,
  API 데이터도 천문연구원 수기 입력 기반이라 "앞으로 약 1년치"만 노출된다.
  → **월별 diff 감지 방식으로 확정**(설계 결정 4). 매달 폼 생성 시 해당 월
  재조회 → 저장값과 diff → 변경 있을 때만 팝업. 평소엔 diff가 없어 팝업이
  안 뜨므로 "확인만 하면 되는" UX가 유지된다

### 2단계 확정 스키마 — 테이블 3개 + 기존 확장 2건

| 대상 | 역할 |
|---|---|
| `public_holidays` | 공휴일 마스터(API 캐시 + 승인대장). `source='manual'`로 임시공휴일 수동 입력 |
| `branch_holiday_operations` | 원별·**날짜별** 운영여부 |
| `branch_monthly_vacation` | 원별·**월별** 방학 양식 O/X |
| `branch_profiles.operates_on_holidays` | 원별 기본 정책(팝업 프리필). 하드코딩 이관분 |
| `diet_templates.vacation_variant` | `none`/`vacation_on`/`vacation_off` |

- **원 참조는 전부 `branch_profiles(id)`로 통일**(설계 결정 2). 레포에 선례가
  갈려 있으나(`weekly_menus`·`diet_review_items`는 `branches(id)`) 이 기능의
  소비자가 PPTX 생성기이므로 후자 기준. 새로 만드는 것부터 통일해
  `weekly_menus.branch_id` 오조회 버그 계열이 더 번지지 않게 한다
- 통합 1테이블은 검토 후 폐기 — 공휴일(날짜, 원)과 방학(연, 월, 원)은 자연키
  축이 달라 CHECK + 부분 유니크 2개로 갈라야 해서 묶는 이득이 없다
- 상세 설계 근거는 마이그레이션 파일 상단 주석에 전부 기록해 둠

### ✅ 5단계 완료 (2026-08-20) — `template_resolver.py` 방학 분기

"연·월당 active 템플릿 1개" 전제를 깼다. API가 둘로 나뉜다:
- `resolve_template_set()` — 그 달 템플릿을 variant별로 **한 번씩만** 준비
  (다운로드·이름표·검증 비용이 원 수와 무관)
- `pick_template()` — 원별 방학 여부로 그중 하나 선택
- `fetch_vacation_map()` — `branch_monthly_vacation` 조회. 키가
  **`branch_profiles.id`** 이고 `cfg['branch_uuid']`·`_get_branch_id_map()`
  값과 그대로 맞물린다

배선: `app_actions.py`, `app.py` **2곳**(`/generate`, `/generate_from_json`).
호출자 0이 된 `resolve_template_path()`는 제거했다 — 이 모듈 docstring이
말하는 "로직 두 벌" 그 자체가 되기 때문.

⚠️ **미배정 원은 방학X로 보수 처리하고 원 이름을 로그에 찍는다.** 49개 원
생성을 통째로 세울 수 없고, 없는 그림이 빠지는 쪽이 엉뚱한 방학 그림이
붙는 쪽보다 덜 틀리다고 판단. 로그에서 추적 가능.

⚠️ **생성 전 문지기가 준비된 variant를 전부 검증한다** — 한 벌만 깨져도
그 원들이 조용히 잘못 생성되므로.

아래는 착수 당시 배경 기록:

### ★ 5단계 배경 — `template_resolver.py`가 진짜 걸림돌이었던 이유

`template_resolver.py:52-56`이 **"연·월당 active 템플릿 1개"** 를 전제하는데
방학 O/X 2벌과 정면 충돌한다. `vacation_variant` 축으로 원별 분기하도록
고쳐야 한다. 다행히 `app.py`·`app_actions.py` 양쪽이 이 모듈만 거치므로
**수정 지점은 한 곳**이다.

방학 그림 자체는 **코드가 삽입·크기조정할 필요 없다**(발견③ — 원별로 양식만
선택). 다만 `remove_holiday_cover_pics()`가 표 위 최상위 PICTURE를 공휴일 셀
좌표 겹침으로 지우므로, **방학 그림이 공휴일 셀과 겹치는 달에는 실수로
삭제될 수 있다**(7월은 하단 5주차, 8월은 상단 1주차 위치). 이 방어는
여전히 필요하다.

### ★6단계 상세 — 팝업 UX (유대표 결정 2026-08-20, 6건)

**①②③④⑤⑥ 전부 구현 + 실물 검증 완료 (①③④⑤⑥은 2026-08-20, ②는
2026-08-24 — 9월 추석 3일 실데이터로 검증하다 프리필 버그 하나를 잡고
고쳤다. 상세는 위 "착수 순서" 7번 참고).**

- 신규 파일: `app/erp/(protected)/diet/HolidayConfirmPopup.tsx`
  (팝업 자체 + `checkHolidaysBeforeGenerate()` — "양식 준비" 클릭 시 먼저 호출)
- 배선: `app/erp/(protected)/diet/page.tsx`의 `handleWfStep(0)` →
  `handlePrepareClick()` → 팝업 필요 시 오픈 / 아니면 조용히 sync + 다음 단계

**① 2단 필터** — 구현 완료(스키마·API·UI). 1단계(공휴일 자체 분류)만 됨,
2단계(원별 예외 확인 화면=②)는 미착수라 지금은 "전 원 휴무"를 선택해도
그 다음 화면이 없음 — 팝업에 안내 문구만 넣어둠("휴무로 지정하면 다음
단계에서 예외 원을 확인합니다")

- ★추가 결정(구현 중 반영): **renamed도 분류 필요 영역에 포함**
  (기존엔 added/unclassified만 고려). `closure_policy` 승계가 이름 기준이라
  이름이 바뀌면 승계가 끊기고, 임시공휴일이 정식 명칭으로 바뀌는 경우처럼
  성격 자체가 달라질 수 있어서다. 기존/추정 정책을 프리필해 "그대로 저장"만
  눌러도 되게 해서 부담은 줄임

**② 원별 화면은 기존 ERP 표준 아코디언** → **구현 + 실물 검증 완료(2026-08-24)
— 상세는 위 "착수 순서" 7번**
- `operates_on_holidays = true` 그룹(현 덕양P·광교SLP)은 **기본 펼침** ✅
  (단, 계열(E/P/R…) 아코디언이 아니라 "운영여부" 2그룹으로 재해석해 구현 —
  이유는 위 7번 참고)
- 나머지 원은 기본 접힘, 필요할 때만 펼쳐 추가 ✅
- 정렬은 기존 화면과 동일하게 `group_tag` / `sort_order` ✅
  (⚠️ `sort_order`는 레포에 DDL이 없고 DB에만 있는 컬럼 — 위 함정 참고)
- 저장 대상은 `branch_holiday_operations`(원별·날짜별) ✅. API는 별도
  라우트로 확정: `app/api/diet-automation/holidays/branches/route.ts`
- ① 팝업에서 "전 원 휴무"로 분류된 날짜만 이 화면의 대상이 됨 ✅

**③ 팝업 표시 시점** — **"양식 준비" 버튼을 누르는 순간**(구현·검증 완료).
로그인 직후 아님

**④ 다시 열 수 있는 진입점** — "공휴일 설정 다시 보기" 텍스트 링크(구현·검증
완료). `mode: 'confirm'`으로 저장, **저장 버튼을 안 누르고 닫으면 아무
요청도 안 나감**(취소로 닫는 경로 실물 확인함 — 결정 이력이 안 덮임)

**⑤ 결정 이력 표시** — "마지막 확인: {이름} {날짜}"(구현·검증 완료).
`confirmed_by`/`confirmed_at` + `admins.name`(GET의 `confirmedByName`)

**⑥ 변경 없을 때도 확인 결과 표시** — 토스트 문구(구현·검증 완료):
> 공휴일 확인 완료 — 이번 달 변경 없음(추석 9/24, 전 원 휴무 유지)

"작년 동일 공휴일엔 이렇게 하셨습니다"도 팝업에 표시(`policySuggestions`,
`inheritPolicy()` 이름 기준 승계)

#### ✅ ⑤↔⑥ 충돌 — 코드로 해결 + 실물 검증 완료

`confirmed_by`/`confirmed_at`(사람이 정한 시점) vs `synced_at`(마지막 대조
시점)을 분리하고, `POST`의 `mode`(`confirm`/`sync`)로 강제하는 설계.
2026-08-20 실물 검증: confirm 1회 → sync 1회 순서로 실행 후 DB 대조,
`confirmed_at`이 **밀리초 단위까지 완전히 동일**, `synced_at`만 나중
시각으로 갱신됨을 확인. 아래 "반복 함정"에 이 검증 중 잡은 버그 기록.

---

## 그 다음 (기준폼 확정 → 실무 투입)

1. **가이드 시트 문구 vs 실제 코드 동작 전수 대조** — `✏️ 입력 가이드` r65~69의
   "시스템이 자동 잠금 처리"가 현재 코드와 불일치. 예외설정 기능 확정 후 문구 재작성
2. **날짜 표기 방식 결정** — 실물은 표 위 텍스트박스 유지, 코드는 제거 후 셀 삽입.
   영양사가 익숙한 방식(실물)을 코드가 따라가는 방향이 유력
3. **폰트 스타일 차이 허용 여부** — 서버 나눔고딕 vs 디자이너 Pretendard,
   `_samples/26년 8월 배포 식단표/` 실물과 나란히 비교 후 판단
4. **v6 최종본을 배서영 영양사 1명에게 시범 작성 요청** — 49개 원 일괄 투입 전
   1명·1개 원 규모로 먼저. 실무자가 헷갈리는 지점은 써본 사람만 알 수 있음
5. 통과 시 정예진·이유라까지 확대 → **v6 확정 선언**
6. 원별 개별 이슈: 분당P 원산지 박스 겹침, 덕양P·광교SLP 공휴일 메뉴 미출력

---

## 로드맵 아이디어

- **고객사 포털 디자인 정리** — ERP처럼 색상·간격·타이포 기준 먼저
  정하고 홈/문의/파일보관함/공지사항/마이페이지 일괄 정비 (유대표 요청,
  2026-08-18)

---

## 기타 미해결 (급하지 않음, 존재만 기록)

- **`diet_templates.version` 전역 카운터 동시성 미보호** — 업로드 시
  `SELECT MAX(version) ... LIMIT 1` 후 `+1`해서 INSERT
  (`app/api/board/diet/templates/route.ts:192-199`). 락·유니크 제약이
  없어 동시에 두 번 업로드하면 같은 version이 나올 수 있다.
  2026-08-25 업로드 폼 착수 전 조사에서 발견, 급하지 않음.
- **`weekly_menus.branch_id`를 `branches.id`로 잘못 조회하는 버그**
  - `app/api/branch-profiles/[id]/route.ts:65,73`
  - ~~`app/board/(customer)/dashboard/page.tsx:96`~~ → 권팀장 요청 5번
    처리(커밋 `47efe02`)로 해당 섹션 자체가 제거되며 해소
- **세션 충돌 storageKey 분리** 미착수 (ㄹ)
- **대치크레오 로그인 계정 미생성**
- **분당P 원산지 박스가 4주차 24일 식단칸과 겹침** (2026-06-25 발견, 미해결)
- **디자이너 양식은 매달 새로 제작되며 디자인뿐 아니라 구조도 바뀜**(방학 유무 등)
  → **특정 양식 하드코딩 금지**
- **덕양P·광교SLP 공휴일 운영 메뉴 미출력** (도시락·대체식 시트 미연결)
- **`read_excel.load_excel()` → `pptx_generator.generate()` 직결 시 메뉴 텍스트
  미삽입** (2026-08-12 발견) — 7·8월 실제 업로드 엑셀로 생성해보니 채워진 셀이
  날짜 숫자·주차/오전/오후 라벨뿐, 음식명이 0건. `test_data.json`(6월 픽스처)로는
  정상 삽입됨. `app_actions.py`엔 `_adapt_ts_menu_data()` 변환 단계가 있는데
  이번 테스트는 그걸 안 거쳤음 — 검증 스크립트 문제인지 `read_excel` 자체
  문제인지 미확인. 다음 세션에서 원인 조사할 것.
- **PowerPoint 버전별 렌더링 차이** (2026-08-12 관측) — 같은 PPTX가 PowerPoint
  버전에 따라 다르게 렌더링됨(영양사 화면 정상 / 대표 화면 박스 겹침). 판정
  기준을 어느 버전 기준으로 확립할지 미해결. 이번 세션 육안 확인은 버전
  16.0.20228.20158(Microsoft 365 Click-to-Run)에서 수행.
- **"LibreOffice 렌더링 신뢰 안 함" 원칙 재검토 필요** — 지금은 사람이
  PowerPoint로 육안 확인하지만, 자동화 완성 후 실제 변환 주체는 서버
  LibreOffice(`read_excel.convert_pptx()`)가 된다. LibreOffice 렌더링을
  판정 근거로 안 쓰는 현재 원칙과 실제 배포 경로가 어긋남 — 다음 세션에서
  재검토할 것.
- **8월 실물 송파ECC(slide 2) 10행이 본식·간식 전부 900,000 균일** (2026-08-12
  발견) — 다른 25건과 전혀 다른 형태(다른 25건: 본식 1,368,000 균일 + 간식
  디자이너 원본 그대로). 원인 불명, 이번 작업 범위 아님, 기록만.
