// =============================================
// 키즈밀 식단표 자동화 — 검증 레이어 완전판
// B-2~B-3에서 실제 로직 구현 예정
// =============================================

export type ValidationResult = {
  valid: boolean
  errorCode: string
  message: string
  field?: string
}

// ── 1층: 입력 단계 검증 (웹폼B 저장 차단) ────────────
// B-01: 알레르기 숫자 1~18 범위 외 → 저장 불가
// B-02: 알레르기 칸에 숫자 외 문자 → 저장 불가
// B-03: 메뉴명에 대괄호 직접 입력 → 저장 불가
// B-04: 밥/국/주반찬 필수항목 빈칸 → 저장 불가
// B-05: 칼로리 0 이하 또는 3000 초과 → 저장 불가
// B-06: 같은 년월 식단 중복 입력 → 확인 팝업
// B-07: 존재하지 않는 날짜 → 달력 비활성
// B-08: 대괄호 원 약자 DB 불일치 → 저장 불가 ★핵심
// B-09: 대괄호 닫힘 누락 → 저장 불가 ★핵심
// B-10: 동일 날짜 동일 원 요청 충돌 → 저장 불가 ★핵심
// B-11: 동탄P 오전간식 유기농우유 아닌 것 → 경고
// B-12: 수지P 간식 표기명 혼용 → 자동 교정
// B-13: 잉파X 체크 시 대체메뉴 빈칸 → 저장 불가
// B-14: P-그룹 선택 시 동탄P 포함 여부 → 경고
// B-15: 영문 원 영문 메뉴명 누락 → 저장 불가
// B-16: 중복 원 요청 → 저장 불가
// B-17: 공휴일 운영 원 메뉴 없음 → 경고
// B-18: 임시저장 없이 닫기 시도 → 브라우저 경고
export const INPUT_VALIDATIONS: ValidationResult[] = [] // B-2에서 구현

// ── 2층: PPTX 생성 단계 검증 ─────────────────────────
// C-01: 활성 원 프로파일 미완성 → 생성 차단
// C-02: 이메일 미설정 원 → 배포 전 경고
// C-03: 위탁 식단 미완성 원 → 생성 차단
// C-04: PPTX 템플릿 미설정 → 생성 차단
// C-05: 엘란 4종류 세트 완전성 확인
// C-06: 정발P JPG / 덕양P PPT 파일형식 강제
// C-07: PPTX 생성 실패 → 자동 재시도 3회
// C-08: 생성 중 타임아웃 → 해당 원만 재시도
// C-09: 생성 파일 용량 0KB → 재생성 트리거
export const GENERATION_VALIDATIONS: ValidationResult[] = [] // C단계에서 구현

// ── 3층: 배포 단계 검증 ───────────────────────────────
// D-01: 복수 이메일 원 전체 발송 확인
// D-02: 이메일 반송 자동 감지 (Resend webhook)
// D-03: 재발송 버튼 활성화
// D-04: 원별 수신확인 기능
// D-05: 미확인 원 N일 경과 알림
// D-06: 배포 완료 관리자 이메일 발송
// D-07: 학부모 포털 동시 반영 확인
// D-08: 소통허브 고객사 동시 반영 확인
export const DEPLOY_VALIDATIONS: ValidationResult[] = [] // D단계에서 구현

// ── 확장 레이어 (미래 추가용) ─────────────────────────
// 전월 대비 메뉴 중복 감지
// 영양소 균형 자동 체크
// 컴플레인 이력 연동 경고
export const EXTENDED_VALIDATIONS: ValidationResult[] = []
